# 研发日报

- 日期：2026-06-11
- 成员：张润

## 工作项

- 清理 Auto Collection 生产库中的设备、日志、采集记录、心跳、命令和版本事件脏数据。
- 修复移动端设备认证逻辑，普通上报接口不再自动创建设备。
- 部署生产 API，阻止旧版 `android_001` 脚本重新注册和写入脏数据。
- 按 GitHub 成果提交规范补齐 README、项目说明、验收口径、部署说明、排期计划和证据清单。

## 思考性工作

- 判断截图后台数据和本地开发库数据不一致，定位到生产 Docker 内部 PostgreSQL。
- 区分公网 5432 的其他项目数据库和 Auto Collection 内部生产库，避免误清其他项目数据。
- 将设备注册和普通上报分离，减少脏设备继续产生的风险。

## AI 协作

- 使用 AI 协助梳理设备认证链路、定位自动创建设备入口、整理 GitHub 提交材料结构。

## 提示词方向

- 让 AI 按 GitHub 成果提交规范检查项目材料缺口。
- 让 AI 将运行事实转成 README、验收口径、部署说明和证据清单。

## 人工采纳结果

- 采纳“只允许注册接口创建设备，普通上报必须校验 token”的方案。
- 采纳“所有新增文档放入 docs 目录”的整理规则。

## 重复工作

- 反复核对本地库和生产库计数，避免清错数据库。
- 反复检查旧 APK 是否继续写入数据。

## 改进建议

- 后续将截图、录屏、清库命令、APK 打包路径沉淀为标准验收清单。
