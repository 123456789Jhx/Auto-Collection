# 研发日报

- 日期：2026-07-28（覆盖 7-27 晚远程居家 + 7-28 晨公司在岗）
- 成员：张润

## 今日总览

一天内完成"远程脚本功能模块"从零到全链路：**基座 8 个节点 + 环境收敛 1 个节点 + 发布视频业务 6 个节点全部交付（代码级）**，后台链路经 curl 模拟手机端到端验证通过；剩余真机验收与真实联调。

## 工作项（按时间顺序）

### 7-27 晚 · 远程居家

1. **项目解读与模块设计** — 完成 Auto Collection 全项目架构解读；输出《远程脚本功能模块方案》（三表模型 + JSON Schema 子集一处三吃 + 8 节点拆解）；建立 Codex 分节点交接机制（方案/进度台账/AGENTS.md 三件套，状态进文件不进对话）。
2. **基座节点 1-7 全部完成** — 数据表与迁移（0023）、共享契约、Repository、类型注册+Schema 校验器、配置 CRUD 接口、Web 管理页面 MVP、Schema 动态表单；期间确立三项长期规则：400 行约束先例模式（新功能进新文件/存量最小接线/超限记卡点）、台账铁律（commit 前 git status 必见进度台账）、存量问题另行立项。
3. **节点 4.5 环境指向收敛** — 全项目 API/Web/手机端统一指向当前标准：API `localhost:3012`、Web `localhost:3022`、手机端 `192.168.7.170:3012`；旧线上地址清零（历史文档除外）。
4. **节点 8a 下发与移动端联动（离线部分）** — 命令下发/拉取/ACK 全链路 curl 模拟手机验证通过（幂等、离线 PENDING 均有实证）；`remote-script-config-handler.js` 新增，`control-loop.js` 仅接线。
5. **发布视频业务方案** — 消化业务流程与 wecom 外部接口文档；定案四项核心规则：业务脚本热更新（双层脚本架构）、单文件 ≤400 行、中文动作/流程命名、动作间双时间闸口（三段式随机语义）。

### 7-28 晨 · 公司在岗

6. **节点 9-12 发布视频后台链路** — `publish_video` 类型注册（Web 配置闭环）；设备账号绑定（accountProfile 结构化 + 设备页编辑 + 移动端拉取）；wecom 外部接口接入与账号匹配（契约/client/任务落库/mock 测试）；30 秒调度下发 + 移动端结果回传 + 外部状态 PATCH + Web 汇总页（统计头 + 话题补全入口）。
7. **节点 12H 业务脚本热更新通道** — 复活旧 ZIP 链路并收窄为 `biz-scripts` 独立 channel：发布幂等、逐文件 SHA-256、路径白名单、备份回滚、APK 基线兜底；`biz-script-updater.js`（267 行）+ 5 个单测通过；发布记录 ID `2971c502-...`，版本 1.0.0，curl 下载校验一致。
8. **节点 13/14 双平台执行器（代码）** — 抖音发布流（中文动作文件：选择发布素材/编辑封面/填写标题描述话题/执行发布与结果判定，最大 291 行）+ 视频号流程（启动校正、验证弹窗强特征标记上报不绕过）+ 共享 `domain/动作时间闸口.js`（三段式：轮询等目标 → 睡响应随机 → 睡等待随机，每动作独立）；合并 Node 测试 15 通过。

## 思考性工作 / 关键决策

- **400 行冲突先例模式**三次复用（schema.ts 656 行、api-client.ts 529 行、control-loop.js 1441 行）：新功能进新文件，存量仅最小接线，超限统一记卡点另行拆分。
- **热更新架构**：APK 基座层（launcher/app/core/platforms/config.js）与业务热更新层（features/+domain/）分离；更新包绝不含 config.js；中文文件名在 manifest/URL 中 URI 编码。
- **安全边界**：wecom token 不出后台（配置只存环境变量名）；API 注册密钥缺失时为宽松模式（代码确认 mobile.service.ts 逻辑）。
- **接口策略**：wecom 简化接口未上线，全部按契约 + mock 先行，真实联调独立成节点 15。

## 验证证据

- 全量 `bun test` 75 通过（仅卡点 49 两个既有 Web 断言失败，已豁免立项）；`typecheck`/`lint` 退出码 0。
- 8a：curl 模拟链路注册→绑定→命令(rev1/2)→拉取(hash 匹配)→ACK(幂等)→离线 PENDING 全绿，测试数据已清理。
- 12H：虚拟设备检查 `channel=biz-scripts` 返回 updateAvailable；ZIP 48680 字节下载 SHA 与 manifest 一致；updater 5 单测（版本比较/URI 路径/回滚）。
- 台账：`docs/项目开发/远程脚本模块/进度台账.md` 节点 1-14 状态齐全；项目记录含各节点验收明细。

## 未完成事项与后续计划（按优先级）

| # | 事项 | 说明 | 预计 |
|---|---|---|---|
| 1 | **git 初始化与首次提交** | 项目副本无 .git（ZIP 快照）；先补 .gitignore 嵌套 env 规则再 init（操作见 tonight-4-tasks-guide） | 今天上午 |
| 2 | **API 注册密钥配置** | 创建 `apps/api/.env`（API_PORT=3012 + MOBILE_REGISTRATION_SECRET），重启后验证无密钥 curl 应 403 | 今天上午 |
| 3 | **防火墙 3012 入站规则** | 管理员 PowerShell 一条命令（已核实当前不存在该规则） | 今天上午 |
| 4 | **APK 打包 1.0.62** | 现有 dist 只有 1.0.61；现在打包可含 8a+12H+13/14 全部基线代码；记得 project.json bump 98 + 同窗口设密钥 | 今天上午 |
| 5 | **8b 真机验收** | 按清单：ipconfig 对 IP → 手机浏览器探 `/ready` → 装 APK → 注册上线 → 下发链路 → 离线补发 | 今天 |
| 6 | **12H/13/14 真机验证** | 热更新覆盖生效、抖音/视频号真实发布流、话题待补人工链路、验证弹窗标记 | 今天/明天 |
| 7 | **节点 15 真实联调** | 等 wecom 简化接口上线后端到端跑通一条任务 | 待对方 |
| 8 | 遗留立项 | 卡点 49 两个 Web 测试修复；schema.ts/api-client.ts/control-loop.js 拆分 | 另行排期 |

## 重复工作识别与改进建议

- 节点完成后的"台账补录"曾两次遗漏（节点 2、5），已立铁律：**git commit 前 git status 必须包含进度台账.md，否则不许提交**。
- 凡涉及存量超限文件的节点，开工提示词应直接写明"新功能进新文件"先例，减少 Codex 请示往返。
- 后续新增业务类型（非发布视频）时，节点 9/10 模式可整体复用：类型注册 + 设备绑定是固定前置。

## AI 协作

- 使用 AI（WorkBuddy + Codex 双助手模式）：WorkBuddy 负责方案设计、节点拆解、提示词工程、授权决策与验收监督；Codex 按节点提示词实施代码，全程 14 个节点无返工性重写。
