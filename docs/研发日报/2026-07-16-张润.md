# 研发日报

- 日期：2026-07-16
- 成员：张润

## 工作项

- 根据 `docs/info` 排查 `1.0.55` 手机端无法向后台注册和发送心跳的问题。
- 修复热更新 ZIP 打包目录项兼容问题，并增强手机端解压兜底。
- 提升手机端版本到 `1.0.56/92`，重新打包、上传并发布 stable Agent。
- 收口后台 `商品卡养号`、`目标直播评论` 两个功能的页面文案和日志识别，不修改手机端协议。
- 部署后台 API/Web 到生产，并验证健康接口和前端资产。
- 修复 App 图标入口偶发黑屏问题，打包 `1.0.57/93` APK 到本地 `dist`。

## 思考性工作

- 对比本地打包配置和服务器生产配置，确认 `1.0.55` 的根因是打包时注入了本地旧注册密钥，不是后台没有版本记录。
- 结合旧包自动更新日志，确认 `ENOTDIR` 来自热更新 ZIP 目录项与旧解压器兼容不足，和商品卡业务逻辑无关。
- 区分内部协议字段和运营页面业务名：`commerce_card_live_comment` 继续作为兼容字段，页面和日志按阶段展示为 `商品卡养号` 或 `目标直播评论`。
- 将 App 黑屏现象拆成启动页 Activity 问题处理，只清理重复 `launcher.js`，避免误伤正在运行的采集脚本和守护脚本。

## 人工采纳结果

- `1.0.55` 不再作为测试包使用。
- `1.0.56` 使用服务器生产配置重新打包；文档和提交只记录哈希、版本、URL 和验证结果，不记录密钥明文。
- 后台页面以 `商品卡养号`、`目标直播评论` 为准，旧日志文案由后台识别和归一化。
- 已采纳“先不改手机端”的约束，只调整后台识别、页面展示和 API 提示。
- 用户确认可以修改后，采纳“清理重复启动页实例”的最小修复方案；本轮不发布 stable Agent，只生成本地 APK 供安装验证。

## AI 协作

- 使用 AI 辅助梳理日志链路、定位打包配置差异、修改打包脚本和解压兜底逻辑，统一后台业务命名，并补充项目记录、卡点记录和风险记录。

## 重复工作识别

- 后续发布手机端包时，应把“生产配置注入来源”和“ZIP 是否含目录项”纳入固定发布核对项，避免同类注册失败和旧包热更新失败重复出现。
- 后续新增阶段型任务时，应先定义“内部任务类型”和“页面业务功能名”的映射规则，避免页面把协议字段直接暴露给运营。
- APK 图标入口类问题应和脚本热更新区分处理，涉及 `launcher.js` 时优先用完整 APK 安装回归。

## 改进建议

- 给打包脚本增加正式的生产配置来源校验和 ZIP 结构测试，避免只靠人工比对哈希。
- 在发布脚本里记录“包内版本、后台最新版本、manifest SHA、APK 版本号”的统一验收摘要。
- 日志中心可继续抽出统一的业务阶段字典，减少页面各处重复维护 phase 到中文名的映射。
- 后续可补一个专门的 launcher 源码断言测试集合，覆盖入口脚本、主脚本和守护脚本之间的重复实例保护。

## 验证证据

- `node --check mobile-agent/autojs/core/uploader.js`
- `node --check mobile-agent/autojs/platforms/douyin/commerce-card-browser.js`
- `node --check mobile-agent/autojs/features/commerce-card-live/runner.js`
- `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`
- `node mobile-agent/autojs/tests/control-loop.test.js`
- `node mobile-agent/autojs/tests/live-comment-runner.test.js`
- `apksigner verify --verbose --print-certs`
- `zipalign -c -p 4`
- `aapt dump badging`
- `http://106.54.41.106:18080/downloads/agent/AgriVideoCollector-autojs-1.0.56.json`
- `http://106.54.41.106:18080/downloads/apk/liaoyuanxinghuo-1.0.56-inrt.apk`
- `bun test apps/api/src/services/task-assignment-types.test.ts`
- `bun run typecheck`
- `bun run build`
- `http://106.54.41.106:18080/health`
- `http://106.54.41.106:18080/ready`
- `/assets/index-mA7IFNaG.js`
- `node mobile-agent/autojs/tests/control-loop.test.js`
- `node --check mobile-agent/autojs/tests/control-loop.test.js`
- `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`
- `apksigner verify --verbose --print-certs dist/apk/燎原星火-1.0.57-inrt.apk`
- `zipalign -c -p 4 dist/apk/燎原星火-1.0.57-inrt.apk`
- `aapt dump badging`：`com.agri.video.collector`，`1.0.57/93`

## 追加：商品卡养号主循环异常修复

- 根据 `docs/info` 复核 `01:59:44` 到 `02:00:36` 的任务链路，确认后台 `START` 已回执成功，暂停来自手机端本地 `ReferenceError` 后的主循环保护逻辑。
- 审查 `mobile-agent/autojs` 入口、商品卡 runner、抖音适配器、商品卡浏览器上下文装配、热更新包结构和现有测试，确认本次阻断点是 `isSearchResultPageText` 拆分后漏接。
- 将商品卡浏览器上下文中的 `isSearchResultPageText` 绑定到已有 `isSearchResultPage`，并补充测试断言，避免同类上下文装配缺口再次漏过。
- 本次未重新打包或发布 stable Agent，后续真机验证需要使用包含本次提交的新包或热更新版本。

补充验证证据：
- `node --check mobile-agent/autojs/platforms/douyin/adapter.js`
- `node --check mobile-agent/autojs/platforms/douyin/commerce-card-browser.js`
- `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`
- `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`
- `node mobile-agent/autojs/tests/control-loop.test.js`
- `node mobile-agent/autojs/tests/live-comment-runner.test.js`

## 追加：商品卡养号修复包打包

- 将手机端版本提升到 `1.0.58/94`，避免和修复前的 `1.0.57/93` APK 混淆。
- 使用既有已验证安装包中的生产注册密钥完成本地 APK 打包，未记录密钥明文。
- 生成 `dist/apk/燎原星火-1.0.58-inrt.apk`，SHA256 为 `DEEB09C706762D821064398A260141D61428E8356F4E63022142B2A0F1B78357`。
- 校验通过：`apksigner verify --verbose --print-certs`、`zipalign -c -p 4`、`aapt dump badging`，包名 `com.agri.video.collector`，版本 `1.0.58/94`。
- 本轮未上传服务器下载目录，未发布 stable Agent。

## 追加：后台停止离线任务修复

- 复核线上 assignment，确认当前卡住状态为 `RUNNING + STOP/PENDING + stop_requested`，属于后台等待手机 ACK 的状态机问题。
- 修改后台 STOP 语义：管理员停止后立即把 assignment 置为 `CANCELLED`，同时保留 STOP 命令给在线手机执行本地停止。
- 打包脚本增加本机已打包配置 fallback，后续未设置环境变量时也能自动注入注册密钥，密钥不进入代码提交。
- 验证通过：后端定向测试、`bun run typecheck`、打包脚本语法检查、热更新打包和 APK `-SkipBuild`。
## 追加：1.0.58 APK 重新打包与后台部署

- 重新执行 APK 打包，生成 `dist/apk/燎原星火-1.0.58-inrt.apk`，版本 `1.0.58/94`，SHA256 为 `FBA9EBDDA9DC1C4E411C61C3761AE66A8B80958089C4ADB73C6C4154C81070B9`。
- 校验 APK v2/v3 签名、zipalign、包名 `com.agri.video.collector`、应用名 `燎原星火` 和版本号均通过。
- 将后台停止离线任务修复提交 `857ff0f` 同步到服务器 `/opt/auto-collection/account-data-platform`，重建并重启 `api` 容器。
- 公网 `/health` 与 `/ready` 返回 200；旧卡住 assignment `e29134db-655e-4edf-8307-b8d34906e9af` 重新 STOP 后已变为 `CANCELLED`。
- 本轮 APK 未上传服务器下载目录，未发布 stable Agent。

追加验证证据：
- `powershell -ExecutionPolicy Bypass -File .\scripts\package-autojs-apk.ps1`
- `apksigner verify --verbose --print-certs dist/apk/燎原星火-1.0.58-inrt.apk`
- `zipalign -c -p 4 dist/apk/燎原星火-1.0.58-inrt.apk`
- `aapt dump badging`：`com.agri.video.collector`，`1.0.58/94`
- `http://106.54.41.106:18080/health`
- `http://106.54.41.106:18080/ready`
- 后台 assignment `e29134db-655e-4edf-8307-b8d34906e9af`：`RUNNING -> CANCELLED`
## 追加：商品详情推荐区点击修复

- 根据真机日志确认：脚本已识别推荐区，但在 `详情 已选中` 状态下误把顶部未选中的 `推荐` 文案当成推荐区信号，首次点击落在当前商品标题区域，未打开新商品。
- 将推荐 tab 识别收紧为 `推荐已选中`，同时保留 `你可能还会喜欢` 等推荐区标题识别。
- 移除仅凭当前详情页价格、已售、店铺等商品信号激活推荐列表的结构兜底，避免当前主商品标题被当成推荐商品卡。
- 推荐区 active 但未找到关键词商品卡候选时，新增 `strategy/topY/keywordNodeCount` 日志，便于下一轮真机判断是关键词节点缺失还是候选过滤。
- 手机端版本提升到 `1.0.59/95`。
- 已按“手机端改动后必须重新打包”的口径生成 `dist/apk/燎原星火-1.0.59-inrt.apk`，SHA256 为 `B4D0117F0D4691530BCAFCFBD1B95AA7FE439344C780C440C58848CE41CE29CE`。
- 本轮没有新的后台代码改动，后台不重复部署。

追加验证证据：
- `node --check mobile-agent/autojs/platforms/douyin/commerce-card-browser.js`
- `node --check mobile-agent/autojs/platforms/douyin/commerce-card/candidate-detector.js`
- `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`
- `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`
- `node mobile-agent/autojs/tests/control-loop.test.js`
- `powershell -ExecutionPolicy Bypass -File .\scripts\package-autojs-apk.ps1`
- `apksigner verify --verbose --print-certs dist/apk/燎原星火-1.0.59-inrt.apk`
- `zipalign -c -p 4 dist/apk/燎原星火-1.0.59-inrt.apk`
- `aapt dump badging`：`com.agri.video.collector`，`1.0.59/95`

## 追加：商品详情推荐卡 OCR 兜底识别

- 根据 `1.0.59/95` 真机日志确认，脚本已经能识别 `你可能还会喜欢` 和 `推荐 已选中`，但推荐区关键词候选一直是 `keywordNodeCount:0`。
- 判断问题不是后台任务、注册密钥或网络，而是抖音推荐商品卡标题没有稳定暴露到无障碍文本树，现有 `textMatches/descMatches` 无法拿到 `夏橙/秭归` 节点。
- 将共享 OCR 引擎注入商品卡浏览器，并在推荐区 active 且无障碍关键词节点为 0 时启动 OCR 兜底识别。
- OCR 兜底按推荐区下方两列商品卡区域截图识别，命中商品关键词后点击对应区域；正常无障碍节点存在时仍走原来的商品卡容器候选逻辑。
- 手机端版本提升到 `1.0.60/96`，后续需要安装新 APK 做真机回归。

追加验证证据：
- `node --check mobile-agent/autojs/platforms/douyin/commerce-card-browser.js`
- `node --check mobile-agent/autojs/platforms/douyin/adapter.js`
- `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`
- `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`

## 追加：1.0.60 OCR 修复包打包校验

- 已执行 `powershell -ExecutionPolicy Bypass -File .\scripts\package-autojs-apk.ps1`，生成 `dist/apk/燎原星火-1.0.60-inrt.apk`。
- APK SHA256：`03DA66DD91D96D0029458BCB77D989B740555A93D90DCAA9D8D16357D62526BA`。
- `apksigner verify --verbose --print-certs` 通过 v2/v3 签名校验。
- `zipalign -c -p 4` 通过。
- `aapt dump badging` 确认包名 `com.agri.video.collector`、版本 `1.0.60/96`、应用名 `燎原星火`。
- 本轮没有后台代码改动，不部署 API/Web，不发布 stable Agent。
## 追加：商城页内搜索入口与推荐商品卡兜底点击

- 根据用户截图和 `docs/info` 复核，确认商城商品卡养号搜索入口应使用商城页内搜索框，右上角放大镜会进入“综合/精选”并混入视频、直播卡。
- 修改抖音适配器，新增商城页内搜索框定位和点击逻辑，商品卡浏览器优先调用该入口；同时收紧商品卡搜索上下文，不再把通用“综合”搜索页作为有效商品卡结果。
- 针对 `keywordNodeCount:0` 且 OCR API 不可用的问题，推荐区点击增加“可见商品卡结构兜底”和“推荐区双列布局兜底”，避免识别到推荐区后一直下滑。
- 手机端版本提升到 `1.0.61/97`，本轮没有后端代码变更，不部署 API/Web。

追加验证证据：
- `node --check mobile-agent/autojs/platforms/douyin/adapter.js`
- `node --check mobile-agent/autojs/platforms/douyin/commerce-card-browser.js`
- `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`
- `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`
- `node mobile-agent/autojs/tests/control-loop.test.js`
- `node mobile-agent/autojs/tests/live-comment-runner.test.js`
- `powershell -ExecutionPolicy Bypass -File .\scripts\package-autojs-apk.ps1`
- `apksigner verify --verbose --print-certs dist/apk/燎原星火-1.0.61-inrt.apk`
- `zipalign -c -p 4 dist/apk/燎原星火-1.0.61-inrt.apk`
- `aapt dump badging`：`com.agri.video.collector`，`1.0.61/97`
- APK SHA256：`ED7B37106BF571FC7D087BF7995281D1D4DA880861966087F6762A119740F169`
