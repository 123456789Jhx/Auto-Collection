# 商品卡养号与直播评论后台改造方案

> 状态：后台专项实施方案，待确认，尚未实现。
> 关联业务方案：`商品卡养号与直播评论组合任务方案.md`、`商品卡片直播间评论.md`。
> 配合规则：组合任务方案定义业务流程和运行安全底线，本文定义后台落地方式；两者冲突时以组合任务方案为准。
> 范围：只定义后台、Web 管理端、API 下发、运行归属、日志审计和兼容迁移，不展开手机端页面操作细节。

## 1. 目标与结论

本文回答“后台怎么改”，不是重新写手机端执行流程。

后台需要承担五件事：

1. 作为商品卡养号、目标直播间评论、直播养号2的配置来源。
2. 在保存配置时完成字段校验和旧配置归一化，避免把错误 JSON 直接下发到手机。
3. 在手机拉取当前任务时下发结构化 `commerce_card_live_comment` 配置，并处理目标中心配置和旧设备配置的优先级。
4. 为每次运行冻结目标、配置修订和完整配置快照，不能让运行中配置修改改变已启动任务。
5. 记录 assignment 状态、阶段事件、真实评论副作用和跳过原因，使后台能判断任务卡在哪个阶段并安全恢复。

核心结论：

1. 继续使用现有任务类型和特性类型 `commerce_card_live_comment`，不新增新的任务类型。
2. “夏橙”只是默认搜索关键词，后台字段必须支持替换为任意关键词。
3. 配置准备可以先部署，但只能保存、校验和预览第二版配置；在运行安全门槛完成前，`commerceCardWorkflowV2Enabled` 必须保持关闭。
4. 第二版无评论灰度前必须完成同一 assignment 状态流转、不可变配置快照、阶段事件、checkpoint v2、版本能力门禁和在途任务停止语义。
5. 真实评论开放前必须完成评论幂等唯一约束、评论副作用状态、审批绑定配置修订和全局急停开关，不能推迟到上线后补。
6. 新配置以直播目标中心为主，旧的 `p3ExtensionsConfig.commerceCardLiveComment` 只作为显式第一版兼容，不自动把第二版流程降级成第一版执行。

两份方案的对应关系：

| 组合任务方案要求 | 后台实现责任 |
|---|---|
| `enabledStages` 与阶段参数 | 严格 schema、阶段化表单、保存归一化 |
| 目标直播间门禁 | 目标主档、别名、绑定、选中目标快照 |
| PAUSING/PAUSED/RESUMING | 同一 assignment 状态流转和稳定检查点回执 |
| 评论幂等 | assignment 归属、服务端预占、唯一幂等键和动作状态 |
| 配置不可变 | `revision`、`config_hash`、`config_snapshot` |
| 旧 APK 兼容 | 能力上报、版本门禁、V1/V2 单一有效口径 |
| 灰度和回退 | 功能开关、设备 allowlist、在途任务取消和审计保留 |

## 2. 当前后台基础

| 模块 | 当前能力 | 问题 |
|---|---|---|
| `live_targets` | 已能维护目标直播间主档、名称、平台、相似度和启用状态 | 可以复用 |
| `live_target_aliases` | 已能维护目标直播间别名 | 可以复用 |
| `live_target_feature_configs` | 已能按 `live_comment`、`commerce_card_live_comment` 保存目标特性配置 | `runtime_config_json` 目前过松，缺少第二版字段校验 |
| `live_target_device_bindings` | 已能把目标配置绑定到设备 | 可以复用 |
| `device_task_assignments` | 已有任务下发、状态、期望 payload 和时间字段 | 配置准备可不动；V2 执行前必须支持同一运行、快照和完整终态 |
| `live_comment_actions` | 已能记录评论发送、跳过和失败 | 无评论灰度可复用；真实评论前必须补运行归属、动作状态和幂等键 |
| `live-target-config.service.ts` | 已有目标、别名、特性配置 schema 和旧任务配置转目标配置函数 | `runtimeConfig: z.record(z.unknown())` 无法约束配置 |
| `live-target.repository.ts` | 已能保存目标配置并向移动端组装 `liveTargets` | 需要统一保存前归一化和下发前归一化 |
| `mobile.service.ts` | 手机拉取当前任务时会合并旧配置生成的 `deviceLiveTargets` 和目标中心 `publicLiveTargets` | 当前直接拼接，容易重复下发同一商品卡任务 |
| `LiveTargetsPage.tsx` | 已有直播目标、搜索直播评论、商品卡直播评论、设备绑定 tab | 商品卡页面目前只覆盖少量一版字段 |
| `live-comment-config.ts` | 已有 P3 默认配置和 JSON 解析 | 默认字段仍是一版口径，需要兼容第二版 |

## 3. 分阶段后台改造边界

### 3.1 阶段 A：配置准备

阶段 A 可以独立部署，但不能创建可执行的第二版 assignment。

必须完成：

1. 商品卡组合任务配置结构化和严格校验。
2. Web 页面从少量字段或高级 JSON 改成阶段化表单。
3. 生成 `configVersion=2` 的预览 payload，并显示字段级错误。
4. 目标中心配置与旧设备 JSON 建立明确来源优先级。
5. 补齐共享 `MobileLiveTargetConfig` 和第二版运行配置契约。
6. `commerceCardWorkflowV2Enabled=false`，任务启动接口拒绝 V2 START/RESUME。

### 3.2 阶段 B：无评论灰度

阶段 B 用于关闭真实评论的真机流程验证，必须完成：

1. START 创建 assignment 并冻结 `selectedTargetId`、`targetCode`、`configRevision`、`configHash` 和 `configSnapshot`。
2. PAUSE、RESUME、STOP 引用同一 assignment，不再为控制命令创建新运行。
3. 完成 assignment 状态机、阶段事件、progress/complete 回执和 checkpoint v2 对账。
4. 设备上报第二版能力，后台按版本、capability 和 allowlist 决定是否允许启动。
5. `commerceCardRealCommentEnabled=false`，服务端和手机端均强制真实发送无效。
6. 关闭 V2 或移出 allowlist 时拒绝新 START/RESUME，并停止在途 V2 运行。

### 3.3 阶段 C：受控真实评论

阶段 C 开启前必须完成：

1. `live_comment_actions` 关联 assignment、目标、阶段和稳定幂等键。
2. 评论动作至少支持 `planned`、`submitting`、`submitted`、`sent`、`unknown`、`failed`、`skipped`。
3. 点击发送前完成本地 pendingSideEffect 和服务端幂等预占。
4. `manualExecutionApproved` 绑定配置哈希、设备、目标、评论池、单房上限和有效期；敏感配置变化后自动失效。
5. 全局 `commerceCardRealCommentEnabled`、任务双开关、审批哈希和 assignment 状态在每次副作用前动态检查。

### 3.4 后续运营增强

可以延后：

1. 历史 JSON 批量迁移和旧字段清理。
2. 跨运行统计报表、自动重试控制台和高级筛选。
3. 更丰富的运行证据归档和趋势分析。
4. 高级 JSON 继续只读保留一个稳定 APK 版本周期。

## 4. 配置模型

配置继续落在直播目标中心：

- `live_target_feature_configs.feature_type = commerce_card_live_comment`
- `search_keywords`：商城搜索词，默认可以是 `["夏橙"]`，但必须可编辑。
- `product_keywords`：商品卡关键词，例如 `["秭归", "夏橙"]`。
- `required_keywords`：进入目标直播间前必须命中的词。
- `forbidden_keywords`：禁止进入或禁止评论的词。
- `live_signals`：只服务第一版兼容，第二版目标入口不再依赖商品卡直播标识。
- `runtime_config_json`：第二版运行参数。

`live_target_feature_configs` 增加单调递增的 `revision`。每次保存目标功能配置时在同一事务中递增 revision，并计算排除审批元数据后的稳定 `config_hash`。assignment 保存 revision、hash 和完整快照。

推荐的 `runtime_config_json`：

```json
{
  "configVersion": 2,
  "enabledStages": ["product_nurture"],
  "executeEnabled": false,
  "manualExecutionApproved": false,
  "recommendationSignals": ["你可能还会喜欢"],
  "productCardDwellSeconds": 120,
  "productNurtureRoundMinutes": 15,
  "productNurtureMaxRounds": 3,
  "targetLiveMaxRoomsPerRefresh": 25,
  "targetCommentSearchMaxActiveMinutes": 60,
  "maxCommentsPerRoom": 1,
  "commentPool": [],
  "manualApprovalConfigHash": "",
  "manualApprovalExpiresAt": "",
  "liveNurtureKeywords": [],
  "liveNurtureRefreshAfterRooms": 10,
  "liveNurtureWatchMinMinutes": 10,
  "liveNurtureWatchMaxMinutes": 20,
  "liveNurtureTotalMinMinutes": 70,
  "liveNurtureTotalMaxMinutes": 100,
  "taskMaxActiveMinutes": 240
}
```

字段说明：

| 字段 | 用途 | 默认值 |
|---|---|---|
| `configVersion` | 配置版本，第二版固定为 `2` | `2` |
| `enabledStages` | 启用阶段，可选 `product_nurture`、`target_comment`、`live_nurture` | `["product_nurture"]` |
| `executeEnabled` | 是否允许真实执行评论动作 | `false` |
| `manualExecutionApproved` | 是否已经人工确认真实执行范围 | `false` |
| `recommendationSignals` | 商品详情页推荐区识别词 | `["你可能还会喜欢"]` |
| `productCardDwellSeconds` | 单个商品卡详情浏览时长 | `120` |
| `productNurtureRoundMinutes` | 商品卡养号单轮时长 | `15` |
| `productNurtureMaxRounds` | 商品卡养号最大轮次 | `3` |
| `targetLiveMaxRoomsPerRefresh` | 目标直播板块单轮候选扫描上限 | `25` |
| `targetCommentSearchMaxActiveMinutes` | 仅评论阶段主动查找目标的最长时间 | `60` |
| `maxCommentsPerRoom` | 同一房间本轮最多评论数 | `1` |
| `commentPool` | 商品卡目标评论话术池，无业务默认值 | `[]` |
| `manualApprovalConfigHash` | 人工批准时绑定的敏感配置哈希 | 空 |
| `manualApprovalExpiresAt` | 人工批准失效时间 | 空 |
| `liveNurtureKeywords` | 直播养号2标题关键词 | `[]` |
| `liveNurtureRefreshAfterRooms` | 直播养号2连续扫描多少个候选后刷新 | `10` |
| `liveNurtureWatchMinMinutes` | 直播养号2单房间最短观看 | `10` |
| `liveNurtureWatchMaxMinutes` | 直播养号2单房间最长观看 | `20` |
| `liveNurtureTotalMinMinutes` | 直播养号2总观看最短 | `70` |
| `liveNurtureTotalMaxMinutes` | 直播养号2总观看最长 | `100` |
| `taskMaxActiveMinutes` | 整体主动执行时长上限，不含人工暂停时间 | `240` |

## 5. 校验规则

后台保存 `commerce_card_live_comment` 时必须做服务端校验，不能只依赖前端。

基础规则：

1. `enabledStages` 至少包含一个阶段。
2. `enabledStages` 只能包含 `product_nurture`、`target_comment`、`live_nurture`。
3. 启用 `live_nurture` 时必须同时启用 `product_nurture`。
4. 启用 `product_nurture` 时，`searchKeywords`、`productKeywords`、`recommendationSignals` 不能为空。
5. 启用 `product_nurture` 或 `target_comment` 时，必须绑定有效目标直播间主档。
6. 启用 `target_comment` 且 `maxCommentsPerRoom > 0` 时，`commentPool` 必须人工填写、去空、去重并限制单条长度和条数。
7. 启用 `live_nurture` 时，`liveNurtureKeywords` 必填。
8. `targetLiveMaxRoomsPerRefresh` 必须是 20 到 30 之间的整数。
9. `productCardDwellSeconds` 限制为 60 到 180；`productNurtureRoundMinutes` 限制为 5 到 30；`productNurtureMaxRounds` 限制为 1 到 6。
10. `liveNurtureWatchMinMinutes <= liveNurtureWatchMaxMinutes`，范围分别受 1 到 30、1 到 60 限制。
11. `liveNurtureTotalMinMinutes <= liveNurtureTotalMaxMinutes`，范围分别受 10 到 180、10 到 240 限制。
12. `taskMaxActiveMinutes` 限制为 30 到 360，并覆盖当前阶段组合可计算出的最小完整流程时长。
13. `enabledStages` 保存时去重并按 `product_nurture -> target_comment -> live_nurture` 固定顺序归一化。
14. 两个任务开关可以独立保存，但有效发送值只能由全局急停、双开关、审批哈希、审批有效期和运行状态共同派生，后台不得把任一开关自动改成 true。

旧字段兼容规则：

| 旧字段 | 新字段 |
|---|---|
| `searchKeywords` | `search_keywords` |
| `scanMinutesPerRound` | `productNurtureRoundMinutes` |
| `maxRounds` | `productNurtureMaxRounds` |
| `watchMinutesPerLive` | 保留给旧 APK；第二版不把它当成直播养号2随机观看时长 |
| `matchKeywords` | `productKeywords` |
| `targetRoom` | 迁移到目标主档、别名和功能配置，不复制进运行 JSON |
| `maxCommentsPerRoom` | 原义保留，并增加 assignment 级幂等约束 |
| `commentPool` | 原义保留，无默认话术 |
| `liveSignals` | 只用于第一版兼容 payload |

## 6. API 与服务改造

### 6.1 `live-target-config.service.ts`

新增严格 schema：

1. `commerceCardStageSchema`
2. `commerceCardWorkflowRuntimeConfigSchema`
3. `normalizeCommerceCardWorkflowRuntimeConfig(value)`
4. `parseCommerceCardFeatureConfig(payload)`
5. `mobileLiveTargetConfigSchema`
6. `commerceCardAgentCapabilitiesSchema`

改造点：

1. `liveTargetFeatureConfigSchema` 不能继续对所有 `runtimeConfig` 使用 `z.record(z.unknown())`。
2. 当 `featureType === "commerce_card_live_comment"` 时，必须按第二版 schema parse 和归一化。
3. 第二版 schema 不包含 `liveSignals`；该字段只在生成第一版兼容 payload 时保留。
4. 旧字段必须在 parse 阶段转换为 canonical 配置，但不能把无等价语义的 `watchMinutesPerLive` 自动映射为阶段三观看时长。
4. `buildMobileLiveTargetConfig()` 输出时，`runtimeConfig` 必须是归一化后的对象。
5. `buildDeviceLiveTargetsFromTaskConfig()` 只作为显式第一版兜底，输出 `configVersion=1`，不能覆盖适用且启用的目标中心第二版配置。
6. 保存时计算敏感配置 `configHash`；目标、设备范围、评论池、上限、阶段或有效期变化时清空人工批准元数据。

### 6.2 `live-target.repository.ts`

改造点：

1. `upsertLiveTargetFeatureConfig()` 写库前调用归一化函数，并在同一事务中递增 revision、更新 configHash，避免脏 JSON 落库。
2. `listMobileLiveTargetsForDevice()` 下发前再次归一化，兼容历史已保存配置。
3. 如果历史数据解析失败，接口应返回可定位的错误日志，并跳过该条配置，不应让整台设备拉取任务失败。
4. 设备绑定 priority 只用于候选目标排序；创建 assignment 时必须明确选择一个目标，不能让手机 runner 自行取第一个同类型目标。

### 6.3 `mobile.service.ts`

当前逻辑：

```ts
const liveTargets = [...deviceLiveTargets, ...publicLiveTargets];
```

配置目录查询需要改为显式合并：

1. 先读取目标中心 `publicLiveTargets`。
2. 再读取旧设备配置生成的 `deviceLiveTargets`。
3. 如果同一设备存在适用、启用且设备能力可识别的目标中心第二版商品卡配置，则整体禁止生成旧 `p3ExtensionsConfig.commerceCardLiveComment` 商品卡兜底。
4. 如果没有目标中心配置，才下发旧设备配置，保证老任务还能跑。
5. 目标中心记录内部按 `(featureType, targetCode)` 去重；禁止拿有 targetId 的新记录和无 targetId 的旧记录做异构键比较。
6. 合并后按适用设备绑定 priority 排序，但排序只服务后台选择，不决定本次运行目标。
7. 返回体可以继续保留 `commerceCardLiveComment` 和 `p3ExtensionsConfig`，但必须标记为第一版兼容字段，且不得与第二版同时成为可执行口径。

V2 assignment 已存在时，`GET current-task` 不得重新拼装目标中心配置，也不得让手机端从 `liveTargets` 目录自行选择目标，而是返回 assignment 中冻结的单一 `effectiveWorkflow`：

1. `assignmentId`。
2. `selectedTargetId`、`targetCode`。
3. `configRevision`、`configHash`、`configSnapshot`。
4. `expiresAt` 和当前 assignment 状态。
5. 手机能力门禁通过后的 workflowVersion。

无 V2 assignment 时，`liveTargets` 只作为配置目录、预览和后台候选选择来源，不是第二版手机 runner 的执行主口径：

```json
{
  "liveTargets": [
    {
      "featureType": "commerce_card_live_comment",
      "searchKeywords": ["夏橙"],
      "productKeywords": ["秭归", "夏橙"],
      "runtimeConfig": {
        "configVersion": 2,
        "enabledStages": ["product_nurture"],
        "productNurtureRoundMinutes": 15,
        "productNurtureMaxRounds": 3
      }
    }
  ]
}
```

创建 V2 assignment 后，下发给第二版手机 runner 的执行主口径必须是单一 `effectiveWorkflow`：

```json
{
  "assignmentId": "assignment-uuid",
  "taskType": "commerce_card_live_comment",
  "workflowVersion": 2,
  "selectedTargetId": "target-uuid",
  "targetCode": "summer_orange_target",
  "configRevision": 12,
  "configHash": "hash-of-approved-config",
  "expiresAt": "2026-07-11T18:00:00.000Z",
  "state": "RUNNING",
  "configSnapshot": {
    "targetName": "目标直播间名称",
    "aliases": ["目标直播间别名"],
    "searchKeywords": ["夏橙"],
    "productKeywords": ["秭归", "夏橙"],
    "runtimeConfig": {
      "configVersion": 2,
      "enabledStages": ["product_nurture"],
      "productNurtureRoundMinutes": 15,
      "productNurtureMaxRounds": 3
    }
  }
}
```

### 6.4 共享类型

建议在 `account-data-platform/packages/types/src/api/admin.ts` 或相邻共享类型文件中补充：

1. `CommerceCardWorkflowStage`
2. `CommerceCardWorkflowRuntimeConfig`
3. `MobileLiveTargetConfig`
4. `LiveTargetFeatureType`
5. `CommerceCardAgentCapabilities`
6. `EffectiveCommerceCardWorkflow`
7. `TaskAssignmentRuntimeStatus`
8. `TaskAssignmentEventPayload`

前端、API、测试都引用共享类型，避免 `api-client.ts`、页面表单和服务端 schema 各写一套字段。

心跳继续使用现有 `sceneType=live` 作为兼容统计维度；具体任务和业务阶段分别通过 `currentTaskType=commerce_card_live_comment` 与 `stage` 表达，不新增近义 `sceneType`。

### 6.5 assignment 与移动端运行接口

阶段 B 前必须新增或扩展：

| 方法 | 路径 | 作用 |
|---|---|---|
| GET | `/api/v1/mobile/tasks/current` | 返回当前 assignment 的单一 effectiveWorkflow 快照 |
| POST | `/api/v1/mobile/task-assignments/:id/events` | 按事件序号和幂等键上报阶段事件 |
| PATCH | `/api/v1/mobile/task-assignments/:id/progress` | 上报进度和检查点摘要 |
| POST | `/api/v1/mobile/task-assignments/:id/complete` | 上报 SUCCEEDED、FAILED、CANCELLED、BLOCKED 或 EXPIRED |

接口约束：

1. assignment 必须属于当前 deviceToken 对应设备。
2. 已终态 assignment 拒绝新事件、进度和评论动作。
3. 重复 sequence 或 idempotencyKey 幂等返回原结果，不重复写入。
4. START 创建新 assignment；PAUSE、RESUME、STOP 必须携带原 assignmentId。
5. 命令 `DONE` 只表示当前控制命令已应用，不能统一把 assignment 写成 `ACKED`。
6. START DONE 映射 RUNNING；PAUSE DONE 只在稳定检查点成功后映射 PAUSED；RESUME DONE 映射 RUNNING；STOP DONE 映射 CANCELLED。
7. 任务业务终态只由 complete 接口或服务端超时/风控决定，并写入 completedAt 和 stopReason。

canonical assignment 状态统一为：

```text
PENDING, DISPATCHED, RUNNING, PAUSING, PAUSED, RESUMING,
SUCCEEDED, FAILED, CANCELLED, BLOCKED, EXPIRED
```

所有接口错误统一返回：

```text
code, category, stage, retryable, attempt, recoveryAction, details
```

## 7. Web 管理端改造

不新增顶级菜单，沿用现有配置、任务调度和日志入口。

主要文件：

- `account-data-platform/apps/web/src/routes/LiveTargetsPage.tsx`
- `account-data-platform/apps/web/src/lib/live-comment-config.ts`
- `account-data-platform/apps/web/src/routes/TaskSchedulerPage.tsx`
- `account-data-platform/apps/web/src/routes/LogsPage.tsx`
- `account-data-platform/apps/web/src/routes/DeviceLiveCommentConfigModal.tsx`
- 必要时补充 `account-data-platform/apps/web/tests/live-targets-page.test.js`

页面结构建议：

1. 目标基础信息：目标名称、平台、相似度、别名、启用状态。
2. 阶段选择：商品卡养号、目标直播评论、直播养号2。
3. 商品卡养号配置：商城搜索词、商品关键词、推荐区识别词、单商品停留秒数、单轮分钟、最大轮次。
4. 目标直播评论配置：候选扫描上限、主动查找分钟、每房间评论数、话术池。
5. 直播养号2配置：标题关键词、刷新阈值、单房间观看区间、总观看区间。
6. 真实执行许可：`executeEnabled`、`manualExecutionApproved`、批准配置哈希和批准有效期，并显示设备、目标、评论池、上限和有效期确认信息。
7. 设备绑定：复用现有设备绑定 tab。
8. `liveSignals` 只在第一版兼容诊断区只读展示，不放入第二版主表单。
9. 页面展示配置 revision、最近批准 revision 和预计主动执行时长区间。

前端保存规则：

1. 表单值先组装为结构化 payload，再交给 API。
2. 高级 JSON 只能作为只读诊断入口，不作为主要配置入口。
3. 保存前做轻量校验，最终以 API 校验为准。
4. 默认搜索词可以是 `夏橙`，但页面文案不能把它写死成业务唯一关键词。
5. `commentPool`、关键词类字段统一按换行、中文逗号、英文逗号切分，并去空去重。
6. 敏感配置变化时页面必须清空人工批准状态并提示重新批准。
7. 设备配置弹窗只维护设备绑定和设备级覆盖，不得一次保存同时启用搜索直播评论和商品卡组合任务。

任务调度页：

1. START 前选择设备、目标和配置 revision，后台创建单一 effectiveWorkflow 快照。
2. 不支持 V2 的设备禁用启动按钮，并显示缺少的 capability。
3. 展示当前阶段、轮次、候选数、刷新数、累计观看、评论状态和 assignment 终态。
4. PAUSE 发出后先显示“暂停中”，稳定检查点回执后再显示“已暂停”。
5. RESUME 继续同一 assignment；过期、取消或配置快照无效时禁止恢复。

日志页：

1. 保留“手机 -> 日期 -> 明细”路径。
2. 同时识别第一版事件和第二版阶段事件，使用中文阶段、决定、原因和恢复动作。
3. 运行详情按 assignment 时间线展示，评论动作作为关联记录，不把系统 ID 作为主要文案。
4. 配置准备阶段也要显示配置来源、configVersion、revision、能力门禁和拒绝原因。

## 8. 数据库与迁移

阶段 A 不需要新增运行表，因为现有表已经能存配置：

- `live_targets`
- `live_target_aliases`
- `live_target_feature_configs`
- `live_target_device_bindings`
- `live_comment_actions`

阶段 A 需要给 `live_target_feature_configs` 增加 `revision` 和 `config_hash`，也可以在完成迁移元数据审计后与阶段 B migration 合并发布。

当前 SQL 文件已到 `0019`，但 `packages/db/src/migrations/meta/_journal.json` 只登记到 `0004`。任何新 migration 前必须对账并修复 `0005-0019`，禁止直接执行生产 `db:migrate`。

阶段 B 执行安全基线必须扩展 `device_task_assignments`：

| 字段 | 说明 |
|---|---|
| `selected_target_id` | 本次运行明确选择的目标 |
| `target_code` | 目标业务编码快照 |
| `config_revision` | 启动时配置修订号 |
| `config_hash` | 启动时敏感配置哈希 |
| `config_snapshot` | 不可变完整配置快照 |
| `current_stage` | 当前业务阶段 |
| `progress_json` | 轮次、扫描、刷新、观看和副作用摘要 |
| `started_at` | 手机真正开始时间 |
| `last_event_seq` | 已确认事件序号 |
| `stop_reason` | 暂停、失败或终态原因 |

新增 `device_task_assignment_events`，记录状态转换、暂停、恢复、刷新、命中、跳过和失败。

事件表建议字段：

| 字段 | 说明 |
|---|---|
| `id` | 主键 |
| `assignment_id` | 任务下发记录 |
| `sequence` | assignment 内单调递增事件序号 |
| `device_id` | 设备 |
| `target_id` | 目标直播间，可空 |
| `feature_type` | `commerce_card_live_comment` |
| `stage` | 当前阶段 |
| `event_type` | 事件类型 |
| `from_state`、`to_state` | assignment 状态转换 |
| `status` | `started`、`succeeded`、`failed`、`skipped` 等 |
| `reason_code` | 失败、跳过或恢复原因 |
| `idempotency_key` | 事件幂等键 |
| `evidence_json` | 截断证据和恢复动作，不保存敏感大文本 |
| `occurred_at` | 发生时间 |
| `actor` | mobile、admin、scheduler 或 system |

唯一约束：

1. `(tenant_id, assignment_id, sequence)`。
2. `(tenant_id, idempotency_key)`。

阶段 C 真实评论前必须扩展 `live_comment_actions`：

1. `assignment_id`、`target_id`、`stage`、`assignment_event_id`。
2. `attempt_no`、`action_state`、`idempotency_key`。
3. `(tenant_id, idempotency_key)` 唯一约束。
4. 幂等键由 tenant、assignment、target、标准化 roomKey 和 comment slot 组成，不包含轮次或刷新次数。

丰富报表、历史事件归档和自动重试控制可以延后，但运行身份、状态、快照和评论幂等不能在对应灰度阶段后补。

## 9. 日志、审计与排查

阶段 A 配置日志：

1. 配置保存记录 target、revision、configHash、操作者和字段级校验结果。
2. 配置预览记录来源、configVersion、适用设备能力和未通过门禁原因。
3. API 保存配置失败时返回字段级错误，便于从页面直接修正。

阶段 B/C 运行日志：

1. 手机端继续上传 runtime log，但结构化 assignment event 是运行状态的事实来源。
2. 每条阶段事件至少包含 assignmentId、configRevision、stage、fromState、toState、sequence、reasonCode、retryable 和 recoveryAction。
3. 评论发送、跳过、失败继续写入 `live_comment_actions`，并关联 assignment 和幂等键。
4. 保留第一版 `commerce_card_live_start`、`commerce_card_scan_start`、`commerce_card_live_comment_send/result`、`commerce_card_live_finish` 映射，历史日志不重写。
5. PAUSE、RESUME、STOP、全局急停、审批失效和 V2 回退都必须写入 assignment 事件。

必须能从后台判断：

1. 当前下发的是目标中心配置还是旧设备 JSON 兜底。
2. 下发配置版本是 `configVersion=1` 还是 `configVersion=2`。
3. 本次 assignment 使用的 target、revision、configHash 和配置来源。
4. 商品卡任务是否因为全局急停、双开关、审批哈希不一致或审批过期而跳过评论。
4. 没有找到目标直播间时，是商品卡养号轮次耗尽，还是目标评论主动查找超时。
5. 评论失败是输入框、发送按钮、目标二次校验、风控还是接口上报问题。
6. PAUSE 是否已经到达稳定检查点，RESUME 是否继续同一 assignment。
7. 旧 APK 或 capability 不足时为什么被拒绝执行 V2。

不得记录 Token、Cookie、账号密码、完整评论流或未脱敏个人数据。

## 10. 兼容策略

### 10.1 能力与版本门禁

设备心跳或注册信息增加：

1. `supportedCommerceWorkflowVersions`。
2. `checkpointVersion`。
3. `supportsAssignmentResume`。
4. `supportsCommentIdempotency`。

后台同时检查：

1. `commerceCardWorkflowV2Enabled` 全局功能开关。
2. 租户/设备 allowlist。
3. 最低支持 appVersion。
4. assignment 所需 capability。
5. `commerceCardRealCommentEnabled` 全局真实评论急停开关。

### 10.2 单一有效执行口径

旧 APK 兼容：

1. 保留 `commerceCardLiveComment` 顶层字段。
2. 保留 `p3ExtensionsConfig.commerceCardLiveComment`。
3. 保留旧字段 `scanMinutesPerRound`、`watchMinutesPerLive`、`maxRounds`、`matchKeywords`。
4. 旧 APK 只能执行显式保留的 V1 快照，不能依赖“忽略 enabledStages”来解释 V2。
5. V2 配置无法无损投影为 V1 时拒绝启动，并记录 `agent_capability_not_supported`。

新 APK 口径：

1. 配置页可以读取 `liveTargets` 目录，但执行时只读取 assignment 的单一 `effectiveWorkflow`。
2. V2 能力齐全且设备在灰度名单时才允许创建 V2 assignment。
3. 没有有效 V2 assignment 时才按明确规则使用 V1；同一响应不得让 V1 和 V2 同时成为可执行口径。

后台兼容：

1. 保存 V2 草稿不影响当前 V1 运行。
2. 只有适用、启用、能力可识别且进入灰度的 V2 才压制 V1。
3. Web 页面保存第二版配置后，不自动删除旧设备 JSON。
4. 运行或暂停中的 assignment 始终使用启动时快照，配置 N 修改为 N+1 只影响下一次运行。
5. 兼容读取至少保留一个稳定 APK 版本周期，确认所有设备升级后再清理旧字段。

### 10.3 灰度与在途任务回退

放量顺序：

1. V2 配置保存和预览。
2. 指定设备无评论 dry-run。
3. 指定设备受控真实评论。
4. 扩大设备 allowlist。

关闭 V2 时：

1. 拒绝新的 V2 START 和 RESUME。
2. 对在途 V2 assignment 下发 STOP，确认终态并保留所有事件和评论审计。
3. 不把 V2 checkpoint 交给 V1 runner。
4. 只有所有 V2 assignment 已终态且设备回到 idle，才允许降级 APK。
5. 重新开启开关不会自动复活已取消或已过期运行。

全局真实评论急停必须在每次评论副作用前动态检查；即使配置快照不可变，急停、assignment 终态、expiresAt 和风控结果仍以实时状态为准。

## 11. 实施顺序

推荐按以下顺序开发：

1. 确认组合任务方案中的阶段组合、时长、退出抖音和评论失败策略。
2. 审计并修复 Drizzle migration journal 与 `0005-0019` SQL 对账问题。
3. 在 `packages/types` 增加共享配置、能力、effectiveWorkflow、assignment 和事件类型。
4. 在 `live-target-config.service.ts` 增加第二版 schema、revision、configHash、归一化和旧字段映射。
5. 改造 `live-target.repository.ts` 的事务保存、配置目录查询和来源优先级。
6. 改造 `LiveTargetsPage.tsx` 和 `live-comment-config.ts`，完成阶段化表单与审批失效提示。
7. 部署阶段 A；保持 V2 与真实评论全局开关关闭，验证配置保存、预览和旧 APK 不受影响。
8. 扩展 assignment、事件、运行接口、状态映射、能力门禁和任务调度/日志页面。
9. 与手机端 checkpoint v2、同 assignment PAUSE/RESUME 和状态机一起完成阶段 B 无评论 dry-run。
10. 扩展 `live_comment_actions`、服务端预占、幂等唯一约束和配置修订审批，完成崩溃恢复测试。
11. 开启单设备受控真实评论，验证全局急停、重复键、状态未知和在途回退。
12. 逐步扩大 allowlist；验收通过后再处理报表、历史 JSON 迁移和旧字段清理。

## 12. 验收口径

阶段 A 配置准备验收：

1. 后台能创建一个 `commerce_card_live_comment` 目标配置，搜索词不是 `夏橙` 时也能保存并下发。
2. 后台可以保存并预览 `configVersion=2`、`enabledStages`、商品卡浏览时长、轮次和推荐区识别词，但启动接口在 V2 开关关闭时明确拒绝执行。
3. 启用目标评论后，API 下发包含目标名、别名、相似度、评论池、双开关。
4. 目标中心 V2 与旧 `p3ExtensionsConfig` 同时存在时，预览结果能解释来源和优先级，不产生两个可执行口径。
5. 未打开 `executeEnabled` 或 `manualExecutionApproved` 时，配置仍可下发，但真实评论必须处于禁止状态。
6. 错误配置保存失败时，前端能看到明确字段错误。
7. 老配置仍能被旧 APK 拉取，不出现任务配置空白。

阶段 B 无评论灰度验收：

1. 不支持 capability 的设备无法创建 V2 assignment。
2. START、PAUSE、RESUME、STOP 使用同一 assignment，PAUSE 稳定回执前状态为 PAUSING。
3. 配置 N 启动后修改成 N+1，运行和恢复仍使用 N 的 selectedTarget 和 configSnapshot。
4. 事件 sequence、设备归属、终态写保护和 expiresAt 校验生效。
5. 关闭 V2 后在途任务停止，不再点击或输入，且不能自动恢复。
6. `commerceCardRealCommentEnabled=false` 时所有评论副作用被双端阻断。

阶段 C 真实评论验收：

1. 同一 assignment、目标、标准化房间和 comment slot 的物理发送最多一次。
2. 在发送前、发送后未确认和上报超时三个位置杀进程，恢复后均不重复发送。
3. `unknown` 状态只能人工确认，不自动重试。
4. 修改目标、设备、评论池、评论上限、阶段或有效期后，原人工批准立即失效。
5. 全局急停关闭后，在途任务下一次副作用前停止发送并写入原因。
6. 重复幂等键由数据库唯一约束拒绝，审计仍可追溯原动作。

不通过条件：

1. 仍需要主要依赖高级 JSON 才能配置第二版流程。
2. `liveTargets` 中重复下发同一商品卡任务。
3. `夏橙` 被写死在后台逻辑中，无法替换。
4. 后台允许保存负数、超大时长、空阶段、空搜索词等明显错误配置。
5. 真实评论开关未双重确认时仍可能下发为可发送状态。
6. 新配置导致旧 APK 拉取任务失败。
7. 配置准备完成后直接启动 V2 runner，却没有 assignment 快照、状态事件、checkpoint v2 或能力门禁。
8. 真实评论开放时没有 assignment 归属、幂等预占、唯一约束、审批哈希或全局急停。
9. RESUME 创建新 assignment，或 PAUSE/命令 DONE 把 assignment 写成错误终态。
10. 旧 APK 收到混合 V1/V2 payload 后静默执行第一版流程。
