# 远程脚本模块 · 进度台账

> 本文件是跨对话的唯一进度真相来源。每个节点完成后由实施者（Codex）更新，更新时间是节点验收的必需动作。

## 全局约束
- 每个 .js/.ts/.tsx 文件 ≤400 行
- 设计唯一依据：docs/方案设计/功能方案/远程脚本功能模块方案.md

## 节点状态
| 节点 | 名称 | 状态 | 完成日期 | 备注 |
|---|---|---|---|---|
| N1 | 冻结旧业务（双保险，可逆） | ✅ 已完成 | 2026-07-28 | 后端 409 守卫、Web 入口冻结、发布与设备链路放行、false 可逆验收完成 |
| N2 | 视频发布页面骨架（4 Tab 复用拼装） | ✅ 已完成 | 2026-07-28 | 单一视频发布入口、四 Tab、publish_video 配置过滤、设备绑定与旧 URL 兼容验收完成 |
| N3 | 任务操作 Tab + 手动测试发布接口 | ✅ 已完成 | 2026-07-28 | 领取、立即调度、手动测试下发、通知提示与看板刷新真实验收完成 |
| N4 | 手机端素材目录管理 + 执行器锁 + 缺料即报 | ✅ 已完成 | 2026-07-28 | 任务独立目录、安全清扫、缺料拦截、15 分钟互斥锁与 biz-scripts v1.1.0 发布完成 |
| N5 | 设备绑定开关 + 状态列 + 匹配规则核对 | ✅ 已完成 | 2026-07-28 | 绑定清空/保存、两处状态列、在线精确匹配、未绑定池与多设备优先级验收完成 |
| N5B | 业务规则对齐 | ✅ 已完成 | 2026-07-28 | 去每日上限、抖音优先、视频号守卫、三层话题断点续传与 biz-scripts v1.2.0 发布完成 |
| N6 | 真机联调紧急修复包 | ✅ 已完成 | 2026-07-28 | 下载白名单、LAN 热更新 URL、发布执行日志、10 分钟看门狗；biz-scripts v1.3.1 无 BOM 包已真机加载验证 |
| N7 | 业务脚本文件名全面 ASCII 化（根因修复） | 🟨 真机命令验收待无障碍授权 | 2026-07-28 | 18 个 features/domain 业务脚本（含热更新示例）改为 ASCII；ZIP ASCII 门禁与 displayName 清单已发布 biz-scripts v1.3.2，APK 1.0.64 / versionCode 100 已安装 |
| 1 | 数据表Schema与迁移 | ✅ 已完成 | 2026-07-27 | 新增三张表并生成 0023 迁移，数据库与类型验收通过 |
| 2 | 共享契约类型 | ✅ 已完成 | 2026-07-27 | Zod 契约与解析单测验收通过 |
| 3 | Repository层 | ✅ 已完成 | 2026-07-27 | definitions/configs 数据访问与真实唯一约束验收通过 |
| 4 | 类型注册+校验器+definitions接口 | ✅ 已完成 | 2026-07-27 | generic_form 启动同步、校验器与受保护接口验收通过 |
| 4.5 | 环境指向收敛 | ✅ 已完成 | 2026-07-27 | API/Web/手机端统一指向当前项目标准地址 |
| 5 | 配置CRUD接口 | ✅ 已完成 | 2026-07-27 | 配置CRUD与全流程API测试验收通过（事后补录） |
| 6 | Web管理页面MVP | ✅ 已完成 | 2026-07-27 | 配置筛选、分页、CRUD、停启用与 JSON 校验闭环验收通过 |
| 7 | Schema动态表单 | ✅ 已完成 | 2026-07-28 | 五类字段渲染、中文预校验、编辑回显与 JSON 兜底验收通过 |
| 08 | 前端粘贴发布与高级调试修复 | ✅ 已完成 | 2026-07-29 | 拆分 external_pull/direct_material 查询，粘贴发布按默认执行配置校验话题数，补齐 HH:mm 与设备视频号能力文案验收 |
| 09 | AutoJS 手机端发布流程对齐 | ✅ 已完成 | 2026-07-29 | 确认共享入口与本地锁防御，补视频号验证/异常时清理素材和释放锁的测试验收 |
| 10 | 最终联调验收 | ✅ 离线验收通过 | 2026-07-29 | API/Web/AutoJS 定向测试、全仓 Bun 测试、类型、lint 与构建验证完成；真机 UI 特征校准待单独验收 |
| 8 | 下发与移动端联动（阶段二） | 🟨 进行中 | | 8a 离线部分完成，8b 真机验收待进行 |
| 9 | 发布视频类型注册 | ✅ 已完成 | 2026-07-28 | `publish_video` Schema、幂等同步与 Web 配置闭环验收完成 |
| 10 | 设备账号绑定 | ✅ 已完成 | 2026-07-28 | 结构化存储、Admin 校验、Web 回显与设备拉取验收完成 |
| 11 | 外部接口接入与任务匹配 | ✅ 已完成 | 2026-07-28 | wecom 契约、外部 client、任务落库与账号匹配离线验收完成 |
| 12 | 调度下发与结果汇总 | ✅ 已完成 | 2026-07-28 | 30 秒调度、移动结果回传、外部状态 PATCH 与 Web 汇总页离线验收完成 |
| 12H | 业务脚本热更新通道 | ✅（覆盖生效待真机） | 2026-07-28 | biz-scripts 隔离打包、发布、检查、下载、SHA 与设备回滚单测完成 |
| 13 | 抖音发布执行器 | 代码✅ 真机待验 | 2026-07-28 | 中文动作编排、双闸口、素材/话题判定、结果回传与热更新分发单测完成 |
| 14 | 视频号发布流程 | 代码✅ 真机待验 | 2026-07-28 | 微信启动校正、视频号完整动作、验证弹窗待人工标记与平台分流单测完成 |

## 已完成节点档案
（每完成一个节点，在此追加一段，格式如下）
### 节点N：<名称>（YYYY-MM-DD 完成）
- 产出文件：<路径清单>
- 验收结果：<逐条粘贴验收命令与结果>
- 偏差与决策：<与方案不一致的地方及原因，无则写"无">
- 给下一节点的提醒：<注意事项>

### 节点1：数据表Schema与迁移（2026-07-27 完成）
- 产出文件：`account-data-platform/packages/db/src/schema-remote-script.ts`、`account-data-platform/packages/db/src/schema.ts`、`account-data-platform/packages/db/src/migrations/0023_remote_scripts.sql`、`account-data-platform/packages/db/src/migrations/meta/0023_snapshot.json`、`account-data-platform/packages/db/src/migrations/meta/_journal.json`、`docs/项目开发/项目记录.md`、`docs/项目开发/卡点与问题记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`
- 验收结果：`bun run db:generate -- --name remote_scripts` 退出码 0，识别 25 张表并生成 0023；`bun run db:push` 退出码 0，输出 `[✓] Changes applied`；`docker exec auto-collection-clean-postgres psql -U postgres -d auto_collection -c "\d remote_script_configs"` 退出码 0，14 个字段及 `WHERE deleted_at IS NULL` 条件唯一索引可见；`bun run typecheck` 退出码 0。
- 偏差与决策：按授权不拆分 656 行的存量 `schema.ts`，三张新表放入 81 行的新文件并 re-export。目标库混有旧 `script_*` 表导致首次 push 等待新建/重命名选择，先在事务中应用已生成的 0023 DDL再执行原始 `db:push`；push 后旧 `script_*` 表数量为 0，详见卡点记录 48。
- 给下一节点的提醒：字段名已定稿；`config_schema` 与 `config_payload` 为非空 jsonb，`config_hash` 为非空 varchar(64)，状态默认 `ENABLED`，revision 默认 1。节点2只建立共享契约，不修改本节点表结构；Web/API 指向当前项目是独立配置事项。

### 节点2：共享契约类型（2026-07-27 完成）
- 产出文件：`account-data-platform/packages/types/src/domain/remote-script.ts`、`account-data-platform/packages/types/src/domain/remote-script.test.ts`、`account-data-platform/packages/types/src/index.ts`、`docs/项目开发/项目记录.md`
- 验收结果：`bun run typecheck` 退出码 0；schema 解析单测通过（正例 + 缺 required/超长 payload 被拒）。（本档案为事后补录，验收数据以节点3会话中 typecheck/lint/test 全绿为准）
- 偏差与决策：完成后未及时更新台账，于节点3完成后补录。
- 给下一节点的提醒：契约类型已由 @pkg/types 导出，service/route 层直接引用即可。


### 节点3：Repository数据访问层（2026-07-27 完成）
- 产出文件：`account-data-platform/apps/api/src/repositories/remote-script.repository.ts`、`account-data-platform/apps/api/src/repositories/remote-script.repository.test.ts`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`
- 验收结果：`bun test apps/api/src/repositories/remote-script.repository.test.ts` 退出码 0，2 通过、0 失败、12 个断言；同名配置第二次创建捕获 PostgreSQL `23505`；`bun run typecheck` 退出码 0；`bun run lint` 退出码 0。
- 偏差与决策：首次 lint 因节点1遗留的不完整本地依赖缺少 `@typescript-eslint/eslint-plugin` 而失败，使用 hoisted 模式补齐依赖后原命令通过。用户本轮明确声明节点2已完成并验收通过，但台账节点2行仍为“未开始”；按节点3边界未代写节点2档案。
- 给下一节点的提醒：节点4可直接调用 definition 的 upsert/list/find；唯一约束错误由 Repository 原样保留 PostgreSQL `23505`，应在 service/route 层映射业务错误。配置更新无论修改 payload、状态或名称都会 revision +1 并重算 hash；未找到或已软删记录返回 null。

### 节点4：类型注册机制+Schema校验器+definitions接口（2026-07-27 完成）
- 产出文件：`account-data-platform/apps/api/src/services/remote-script-registry.ts`、`account-data-platform/apps/api/src/services/remote-script-registry.test.ts`、`account-data-platform/apps/api/src/lib/json-schema-lite.ts`、`account-data-platform/apps/api/src/lib/json-schema-lite.test.ts`、`account-data-platform/apps/api/src/routes/admin.ts`、`account-data-platform/apps/api/src/index.ts`、`docs/项目开发/项目记录.md`、`docs/项目开发/卡点与问题记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`
- 验收结果：校验器覆盖 required 缺失、类型错误、enum 越界、maxItems 超限和合法通过；校验器+注册表定向测试 6 通过、0 失败、11 个断言；API 重启前后有效 `generic_form` 均为 1；带 admin 认证的 definitions curl 返回 1 条记录并含 `configSchema`；`bun run typecheck`、`bun run lint` 均退出码 0。
- 偏差与决策：全量 `bun test` 为 54 通过、2 个既有 Web 断言失败，经授权以节点4定向测试全绿作为验收通过；失败详情已记入卡点记录 49，标记为独立修复任务。3010、3011 被旧 API 占用，当前项目使用 3012 完成验收，未修改端口配置。
- 给下一节点的提醒：节点5创建/更新配置时先按 `scriptKey` 查询已同步 definition，再将 `configSchema` 交给 `validateJsonSchemaLite`；校验失败应映射 `PAYLOAD_SCHEMA_MISMATCH`，禁用类型映射 `SCRIPT_TYPE_DISABLED`。Admin 路由已经过全局 `adminAuth`，后续 configs 路由挂在同一前缀即可。

### 节点4.5：环境指向收敛（2026-07-27 完成）
- 产出文件：`account-data-platform/apps/api/src/config.ts`、`account-data-platform/apps/web/vite.config.ts`、`account-data-platform/apps/web/package.json`、`account-data-platform/package.json`、`account-data-platform/docker-compose.development.yml`、`account-data-platform/.env.development.example`、`account-data-platform/.env.example`、`mobile-agent/autojs/config.js`、`部署说明.md`、`README.md`、`account-data-platform/README.md`、`mobile-agent/autojs/README.md`、`docs/项目开发/部署说明.md`、`docs/项目开发/验收口径.md`、`docs/项目开发/设备与联调记录/设备与联调记录.md`、`docs/方案设计/功能方案/远程脚本功能模块方案.md`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`
- 验收结果：旧 IP 与旧本地 URL 按指定排除口径 grep 输出 `NO_MATCHES`；`bun run dev` 同时启动 API `http://localhost:3012` 与 Web `http://localhost:3022`；`curl http://localhost:3012/ready` 返回 200/ready 且数据库、Redis 均正常；Web 代理登录成功，浏览器登录页正常且无控制台错误；`bun run typecheck`、`bun run lint`、`node --check mobile-agent/autojs/config.js` 均退出码 0。
- 偏差与决策：为使根 `bun run dev` 真正启动 API/Web，将工作区 dev filter 从全部 workspace 收敛为 `@app/*`；为满足无指向性残留验收，同步更新了指定清单外但仍含旧默认地址的 README、通用 env 示例、手机 README、联调示例和方案 curl 示例。未触碰其他项目进程，未创建真实 `.env`，线上地址待部署后回填。
- 给下一节点的提醒：节点5本地接口与 curl 统一使用 3012；Web 联调统一使用 3022 和 `/api/v1` 代理；手机真机必须能访问开发机局域网地址，若开发机 IP 变化需在独立环境节点更新。

### 节点5：配置CRUD接口（2026-07-27 完成，事后补录）
- 产出文件：`account-data-platform/apps/api/src/services/remote-script.service.ts`、`account-data-platform/apps/api/src/routes/remote-scripts.ts`、`account-data-platform/apps/api/src/routes/remote-scripts.test.ts`、`account-data-platform/apps/api/src/routes/admin.ts`、`docs/项目开发/项目记录.md`
- 验收结果：CRUD 全流程 API 测试通过（create→list→detail→update→status→delete）；负向用例（schema不符/重名/未认证）通过；typecheck/lint 通过。（事后补录，以节点6会话确认的实现落地为准）
- 偏差与决策：完成后未执行台账收尾，于节点6实施时补录。
- 给下一节点的提醒：admin 已挂载 /remote-scripts/* 路由，错误映射已实现，Web 端按方案1.5节对接即可。

### 节点6：Web配置管理页面MVP（2026-07-27 完成）
- 产出文件：`account-data-platform/apps/web/src/lib/api-client.ts`、`account-data-platform/apps/web/src/lib/api-client-remote-scripts.ts`、`account-data-platform/apps/web/src/routes/RemoteScriptConfigModal.tsx`、`account-data-platform/apps/web/src/routes/RemoteScriptsPage.tsx`、`account-data-platform/apps/web/src/routes/App.tsx`、`docs/项目开发/项目记录.md`、`docs/项目开发/卡点与问题记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`
- 验收结果：当前项目 API/Web 运行于 3012/3022，`GET /ready` 返回 200 且 PostgreSQL、Redis 均为 ok；浏览器从 `/remote-scripts` 完成新建（revision 1）→编辑保存（revision 2）→停用（revision 3）→启用（revision 4）→Popconfirm 删除，删除后列表从 1 条恢复为 0 条；非法 JSON 被表单阻止提交并显示解析错误；真实页面截图为 1280×720；`bun run typecheck` 退出码 0（`$ tsc -b`），`bun run lint` 退出码 0（`$ eslint .`）。
- 偏差与决策：存量 `api-client.ts` 已有 529 行，经授权不在本节点搬运既有函数，仅在原行导出 `request/mutate/patch/remove`，行数保持 529；远程脚本请求独立放入 61 行的 `api-client-remote-scripts.ts`，详见卡点记录 50。节点6继续使用 JSON 文本域，没有提前实现节点7动态表单。
- 给下一节点的提醒：节点7用 definitions 的 `configSchema` 驱动表单，并保留 JSON 高级编辑兜底；远程脚本请求继续集中在 `api-client-remote-scripts.ts`，该文件必须保持不超过 400 行。

### 节点7：Schema驱动动态表单（2026-07-28 完成）
- 产出文件：`account-data-platform/apps/web/src/components/dynamic-form/schema.ts`、`account-data-platform/apps/web/src/components/dynamic-form/field-rules.ts`、`account-data-platform/apps/web/src/components/dynamic-form/StringField.tsx`、`account-data-platform/apps/web/src/components/dynamic-form/NumberField.tsx`、`account-data-platform/apps/web/src/components/dynamic-form/BooleanField.tsx`、`account-data-platform/apps/web/src/components/dynamic-form/EnumField.tsx`、`account-data-platform/apps/web/src/components/dynamic-form/StringArrayField.tsx`、`account-data-platform/apps/web/src/components/dynamic-form/DynamicConfigForm.tsx`、`account-data-platform/apps/web/src/routes/RemoteScriptConfigModal.tsx`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`
- 验收结果：浏览器中 `generic_form` 新建弹窗按 definitions 的 `configSchema` 渲染 enum、boolean、string array、string、integer 五类字段；缺少关键词/标题模板分别显示“关键词为必填项”“标题模板为必填项”，enum 越界显示“发布方式不在可选范围内”，21 项数组显示“关键词最多填写 20 项”；合法 JSON 高级编辑可同步回动态字段，清空 JSON 后必填校验重新生效；创建 revision 1 后进入编辑弹窗，`scheduled`、启用开关、两项关键词、标题模板和每日上限 12 均正确回显，测试配置随后删除；真实页面截图为 1280×720；`bun run typecheck` 退出码 0（`$ tsc -b`），`bun run lint` 退出码 0（`$ eslint .`）。
- 偏差与决策：`apps/web/src/components/` 原先不存在，本节点按方案首次创建 `dynamic-form/`；节点6的 JSON 文本域实际位于 `RemoteScriptConfigModal.tsx`，因此只修改该弹窗，`RemoteScriptsPage.tsx` 无需产生机械改动。高级 JSON 对合法对象按字段替换同步，非法 JSON 保留最后一次有效动态值并由原 JSON 规则阻止提交。
- 给下一节点的提醒：节点8继续把远程脚本绑定请求放入 `api-client-remote-scripts.ts` 并保持不超过 400 行；下发和设备绑定不得绕过现有 config revision/hash 与服务端 Schema 校验。

### 节点8a：配置下发与移动端生效（2026-07-28 离线部分完成）
- 产出文件：`account-data-platform/packages/types/src/domain/remote-script.ts`、`account-data-platform/apps/api/src/repositories/command.repository.ts`、`account-data-platform/apps/api/src/repositories/remote-script.repository.ts`、`account-data-platform/apps/api/src/services/command.service.ts`、`account-data-platform/apps/api/src/services/remote-script.service.ts`、`account-data-platform/apps/api/src/routes/remote-scripts.ts`、`account-data-platform/apps/api/src/routes/mobile-remote-scripts.ts`、`account-data-platform/apps/api/src/routes/remote-script-delivery.test.ts`、`account-data-platform/apps/api/src/app.ts`、`account-data-platform/apps/web/src/lib/api-client-remote-scripts.ts`、`account-data-platform/apps/web/src/routes/RemoteScriptsPage.tsx`、`account-data-platform/apps/web/src/routes/RemoteScriptBindingsModal.tsx`、`mobile-agent/autojs/app/remote-script-config-handler.js`、`mobile-agent/autojs/app/control-loop.js`、`mobile-agent/autojs/tests/remote-script-config-handler.test.js`、`docs/项目开发/项目记录.md`、`docs/项目开发/卡点与问题记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`
- 验收结果：节点8 API 与既有 CRUD 定向测试 4 通过、0 失败、44 个断言，其中离线下发测试覆盖注册、绑定计数、PENDING、保存通知、拉取、ACK、重复 ACK 和解绑；新 handler Node 测试、既有 control-loop Node 测试、两文件 `node --check` 均通过；`bun run typecheck` 与 `bun run lint` 退出码 0。全量 `bun test` 为 58 通过、2 个卡点49既有 Web 断言失败，按本节点开始前授权口径验收。3012 真实 curl 使用 64 位临时 token 完成：注册 `test-device-001`→绑定 priority 20→保存到 revision 2→轮询获得两条 `SCRIPT_CONFIG_UPDATED`→拉取 revision 2/hash/payload→首次 ACK `idempotent:false`→重复 ACK `idempotent:true` 且时间不变→后台命令为 DONE 并可见 `appliedRevision:2`；再次保存 revision 3 且不轮询时命令为 PENDING、`fetchedAt/acknowledgedAt` 均为 null。Web 列表显示绑定数 1，绑定弹窗显示设备与优先级；curl 测试数据最终精确清理为 0。
- 偏差与决策：今晚无真机，本档案只代表 8a 离线部分，节点8保持进行中。存量 420 行 `mobile.ts` 未修改，移动拉取以独立 `mobile-remote-scripts.ts` 从 `app.ts` 挂载；经授权，1441 行的存量 `control-loop.js` 仅增加 6 行 handler 初始化/分发接线，现为 1447 行，详见卡点51。未修改 `uploader.js`，签名 GET 封装在 196 行的新 handler；未创建或提交 `.env`。
- 给8b的提醒：真机需验证下一轮轮询收到命令、签名拉取成功、`AgriVideoCollectorRemoteScripts` storage 跨重启保留、实际业务 apply hook 生效、ACK 携带真实 revision/hash 并在后台可见；还需验证离线后上线补发、停用/解绑后缓存清除和失败重试日志。完成这些真机项后才能把节点8改为已完成，并按方案更新设备与联调记录。

### 节点9：发布视频类型注册（2026-07-28 完成）
- 产出文件：`account-data-platform/apps/api/src/services/remote-script-registry.ts`、`account-data-platform/apps/api/src/services/remote-script-registry.test.ts`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`；截图：`outputs/node9-publish-video-list.png`、`outputs/node9-publish-video-edit-echo.png`、`outputs/node9-publish-video-edit-echo-lower.png`。
- 验收结果：`publish_video` 注册名称为“发布视频”，中文说明及 11 个配置字段、8 个 required 字段按约定落地；同步连续执行两次后数据库有效 definition 仍为 1 条且 Schema 与注册表一致。定向测试先因缺少类型得到 0/1 红灯，注册后为 1 通过、0 失败、6 个断言；`bun run typecheck` 与 `bun run lint` 均退出码 0，注册表及测试分别为 151/97 行。API `/ready` 返回 200，PostgreSQL、Redis 均为 ok；Web 出现 `publish_video`，动态表单渲染全部字段，空提交显示中文必填提示，完成新建 revision 1→编辑回显并保存 revision 2→二次确认删除→列表归零。
- 偏差与决策：生产代码只修改注册表，没有改动同步、校验、CRUD 或前端逻辑；没有保存 Token 值，仅配置环境变量名。全量 `bun test` 为 58 通过、2 失败，失败仍是卡点49记录的 `live-targets-page.test.js:79` 与 `navigation-dashboard-status.test.js:41` 两项既有 Web 文本断言，与本节点无调用关系；按“只做 publish_video 类型注册”边界未越界修改，故未达到“全量 bun test 全通过”的字面验收项。
- 给下一节点的提醒：后续发布业务只读取 `externalTokenEnv` 指向的设备环境变量，禁止把真实 Token 写入配置 payload；`publishTimeSlots` 当前由 Schema 子集约束为最多 8 个字符串，具体 HH:mm 格式校验应在获批的业务节点内实现，不在本节点扩展校验器。

### 节点10：设备账号绑定（2026-07-28 完成）
- 产出文件：`account-data-platform/packages/types/src/api/admin.ts`、`account-data-platform/apps/api/src/routes/device-account-binding.test.ts`、`account-data-platform/apps/web/src/lib/api-client-devices.ts`、`account-data-platform/apps/web/src/routes/DeviceAccountBindingControl.tsx`、`account-data-platform/apps/web/src/routes/DevicesPage.tsx`、`account-data-platform/apps/web/src/routes/App.tsx`、`account-data-platform/apps/web/tests/device-account-binding.test.js`、`docs/项目开发/项目记录.md`、`docs/项目开发/卡点与问题记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`；截图：`outputs/node10-device-account-binding.png`。
- 验收结果：Admin 复用 `PATCH /devices/:deviceCode`，当 `accountProfile` 出现任一绑定键时严格校验且只能包含 `douyinAccountId/douyinAccountName/wechatChannelsName` 三个字符串，空字符串合法；API 测试先确认数值账号名被旧接口错误接受为 200，实施后非法输入返回 400，合法结构保存并由设备身份 `tasks/current` 原样拉取，定向为 1 通过、0 失败、5 个断言。Web 新增 `/devices` 与侧栏设备入口，每行“账号绑定”弹窗可编辑三字段；`test-device-001` 保存“测试号001”后整页刷新仍正确回显。真实 `curl.exe` 携带 64 位临时设备 Token 拉取返回 `accountProfile.douyinAccountName="测试号001"`；测试设备最终清理为 0。Web 定向测试 1 通过、0 失败；`bun run typecheck`、`bun run lint` 均退出码 0。
- 偏差与决策：`tasks/current` 既有实现已在 `mobile.service.ts` 返回设备 `accountProfile`，本节点不做重复生产修改，只补集成测试与 curl 证据。指定 `DevicesPage.tsx` 实施前为 678 行且未挂载；按“新功能进新文件”将 UI/API 分别放入 84/11 行新文件，页面只增加 5 行挂载至 683 行，并在 199 行的 `App.tsx` 增加必要路由，存量超限详见卡点52。全量 `bun test` 为 60 通过、2 失败，仍仅是卡点49既有 Web 文本断言，未越界修改。
- 给下一节点的提醒：绑定保存会按本节点约定把 `accountProfile` 替换为三字段结构；后续发布逻辑应只读取这三个键，不应依赖历史账号画像的其他字段。移动端继续通过已认证的 `tasks/current` 获取，禁止另开未鉴权接口。

### 节点11：外部接口接入与任务匹配（2026-07-28 完成）
- 产出文件：`account-data-platform/packages/types/src/domain/wecom-publish.ts`、`account-data-platform/packages/types/src/domain/wecom-publish.test.ts`、`account-data-platform/packages/types/src/index.ts`、`account-data-platform/packages/db/src/publish-task-schema.ts`、`account-data-platform/packages/db/src/schema.ts`、`account-data-platform/packages/db/src/migrations/0024_publish_tasks.sql`、`account-data-platform/packages/db/src/migrations/meta/0024_snapshot.json`、`account-data-platform/packages/db/src/migrations/meta/_journal.json`、`account-data-platform/apps/api/src/repositories/publish-task.repository.ts`、`account-data-platform/apps/api/src/services/wecom-publish-client.ts`、`account-data-platform/apps/api/src/services/wecom-publish-client.test.ts`、`account-data-platform/apps/api/src/services/publish-match.service.ts`、`account-data-platform/apps/api/src/services/publish-match.service.test.ts`、`account-data-platform/apps/api/src/routes/publish-tasks.ts`、`account-data-platform/apps/api/src/routes/admin.ts`、`docs/项目开发/项目记录.md`、`docs/项目开发/卡点与问题记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`。
- 验收结果：共享契约严格校验任务 8 字段、claim/patch/error payload，并映射 `抖音/视频号→DOUYIN/WECHAT_CHANNELS`、`待发布/已发布→PENDING/PUBLISHED`。外部 client 使用 Bearer、10 秒超时与脱敏日志，mock fetch 覆盖命中、`data:null`、401、500 和 PATCH；匹配集成测试覆盖启用设备命中、禁用/未绑定账号不命中、空任务和同 taskId 幂等。节点11定向测试 9 通过、0 失败、31 个断言；`bun run typecheck`、`bun run lint` 退出码 0。`bun run db:generate -- --name publish_tasks` 识别 26 张表并生成 0024，`bun run db:push` 输出 `[✓] Changes applied`；`psql \d publish_tasks` 显示 20 列、3 个索引、2 个外键及 `(tenant_id, platform, task_id)` 唯一索引。
- 偏差与决策：`claim-once` 按要求只接收 `configId`，但节点9配置没有平台字段；本节点按当前账号匹配目标默认以“抖音”领取，契约/client 仍完整支持“视频号”，后续若需按配置选择平台应另行扩展 Schema。新表放入 49 行独立文件，659 行存量 `schema.ts` 只增加一行导出，沿用卡点47决策；其余新生产文件最大 150 行。全量 `bun test` 为 69 通过、2 失败，仍仅为卡点49既有 Web 文本断言，未越界修改。
- curl 离线验收：本地 mock 4011 与隔离 API 3013 下，首次 `claim-once` 写入 `MATCHED` 且 `matchedDeviceId` 等于账号设备；第二次相同 taskId 返回同一记录且 `created:false`；第三次写入 `UNMATCHED` 与 `matchNote="未命中：无绑定该账号设备"`，数据库最终正好 2 行。mock/API 日志中临时 Token 明文命中 0，mock claim 次数为 3；3013/4011 进程已停止，验收配置、设备和任务均清理为 0。
- 给下一节点的提醒：节点12可消费 `MATCHED` 记录做调度/下发，并用本节点 `patchTaskStatus` 回写外部状态；不得在领取匹配阶段提前创建移动命令。幂等键是 `(tenant, platform, taskId)`，重复领取不会改写原匹配结果。

### 节点12：调度下发与结果汇总（2026-07-28 完成）
- 产出文件：`account-data-platform/packages/types/src/domain/publish-task.ts`、`account-data-platform/packages/db/src/publish-task-schema.ts`、`account-data-platform/packages/db/src/migrations/0025_publish_task_dispatch.sql`、`account-data-platform/packages/db/src/migrations/meta/0025_snapshot.json`、`account-data-platform/packages/db/src/migrations/meta/_journal.json`、`account-data-platform/apps/api/src/repositories/publish-dispatch.repository.ts`、`account-data-platform/apps/api/src/services/publish-config.ts`、`account-data-platform/apps/api/src/services/publish-command.service.ts`、`account-data-platform/apps/api/src/services/publish-scheduler.service.ts`、`account-data-platform/apps/api/src/services/publish-task-result.service.ts`、对应两项 service 测试、`account-data-platform/apps/api/src/routes/mobile-publish-tasks.ts`、`account-data-platform/apps/api/src/routes/publish-tasks.ts`、`account-data-platform/apps/api/src/app.ts`、`account-data-platform/apps/api/src/index.ts`、`account-data-platform/apps/web/src/lib/api-client-publish-tasks.ts`、`account-data-platform/apps/web/src/routes/PublishTasksPage.tsx`、`account-data-platform/apps/web/src/routes/PublishTopicsModal.tsx`、`account-data-platform/apps/web/src/routes/App.tsx`、`account-data-platform/apps/web/tests/publish-tasks-page.test.js`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`；截图：`outputs/node12-publish-tasks.png`。
- 验收结果：`bun run db:generate -- --name publish_task_dispatch` 生成 0025，`bun run db:push` 输出 `[✓] Changes applied`；调度与结果映射测试 4 通过、0 失败、20 个断言，Web 页面测试 1 通过、0 失败；`bun run typecheck`、`bun run lint` 均退出码 0。mock 全链路依次得到 `dispatched:1`、轮询到 `PUBLISH_VIDEO_TASK`、成功回传 `REPORTED`、mock PATCH `status="已发布"`、页面统计成功 1；真实浏览器截图同时包含统计头和任务表格。
- 偏差与决策：`publish_tasks.status` 是 varchar，因此除主生命周期外可直接保存 `TOPIC_PENDING/MATERIAL_INVALID/CHANNELS_VERIFY_PENDING` 三个人工状态。规格只要求扩展原表，未新增调度运行表；有任务时用 `scheduledSlot` 持久防重，无任务时用进程内 slot key 防止同一分钟重复领取。全量 `bun test` 为 74 通过、2 失败，仍是卡点49的既有 Web 文本断言，未越界修改。存量 420 行 `mobile.ts` 不再增加代码，移动结果路由从 28 行 `app.ts` 独立挂载。
- 给下一节点的提醒：节点13/14只负责手机执行 `PUBLISH_VIDEO_TASK`；结果上报必须携带已认证的 `deviceId/deviceToken`，`payload.taskId` 是本地 publish_tasks UUID。执行端遇到话题不足上报 `TOPIC_PENDING`，人工补全会复用同一命令并重置为 PENDING；不得在手机端自行 PATCH 外部接口。

### 节点12H：业务脚本热更新通道（2026-07-28 离线完成）
- 产出文件：`scripts/bundle-autojs.ps1`、`scripts/publish-autojs-version.ps1`、`mobile-agent/autojs/app/biz-script-updater.js`、`mobile-agent/autojs/tests/biz-script-updater.test.js`、`mobile-agent/autojs/features/热更新示例.js`、`mobile-agent/autojs/main.module.js`、`mobile-agent/autojs/app/heartbeat.js`、`mobile-agent/autojs/config.js`、`account-data-platform/packages/types/src/api/admin.ts`、`account-data-platform/packages/types/src/api/agent-version.test.ts`、`account-data-platform/apps/api/src/services/agent-version.service.ts`、`README.md`、`mobile-agent/autojs/README.md`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`。
- 验收结果：`bundle-autojs.ps1 -Version 1.0.0` 连续运行两次得到相同 SHA `2d88eb0aeb6825c6ccea812498f6cbfb8e8ce4c9a2245830c0c575ff7140c98d`；ZIP 共 20 个条目，只含 `features/domain/biz-script-manifest.json`，`config.js` 不存在，中文示例路径在 manifest 中 URI 编码。`publish-autojs-version.ps1` 首次创建 ID `2971c502-40f6-49ea-beac-dcbde4623f3e`，第二次幂等返回同一 ID。设备 curl 检查返回新版，下载 ZIP 为 48680 字节且 SHA 匹配，Admin 记录为 `channel=biz-scripts/status=PUBLISHED`。
- 自动验证：updater Node 测试 5 通过、0 失败；既有 control-loop 测试通过；`biz-script-updater.js/heartbeat.js/uploader.js/main.module.js/热更新示例.js` 语法检查通过；`bun run typecheck`、`bun run lint` 均退出码 0。全量 `bun test` 为 75 通过、2 失败，仍仅是卡点49既有 Web 文本断言。所有本节点实际修改的 JS/TS/PS1 文件均不超过 293 行，存量超限 `uploader.js` 最终零改动。
- 偏差与决策：不复活历史全量 ZIP 替换，只恢复收窄后的 `biz-scripts` 通道；后台现有版本表、channel 过滤与 `/downloads/agent/:fileName` 已满足需求，因此不新增迁移。设备覆盖层按文件回退 APK 基线，更新包绝不含 `config.js`；同版本不同内容不允许覆盖，必须提升业务版本。
- 真机待验：验证 ZIP 下载后真实覆盖 `features/domain`；主引擎退出并由 watchdog 重启后实际加载新版；破坏包级或逐文件 SHA 时恢复备份并上报 `FAILED/ROLLBACK`，且无覆盖层时 APK 内置基线可继续运行。
- 给下一节点的提醒：节点13/14只能依赖 APK 基线或已验证的 `biz-scripts/current`，不得把发布视频账号、Token、下载目录等运行配置写进业务 ZIP；业务版本与 APK `config.app.version` 是两条独立版本线。

### 节点13：抖音发布执行器（2026-07-28 代码完成，真机待验）
- 产出文件：`mobile-agent/autojs/features/publish-video/发布视频-入口.js`、`抖音发布界面.js`、`打开抖音到相机页.js`、`选择发布素材.js`、`编辑封面.js`、`填写标题描述话题.js`、`执行发布与结果判定.js`、`mobile-agent/autojs/domain/动作时间闸口.js`、`话题校验.js`、`素材判断.js`、`mobile-agent/autojs/tests/publish-video-domain.test.js`、`publish-video-flow.test.js`、`mobile-agent/autojs/app/control-loop.js`、`mobile-agent/autojs/main.module.js`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`。
- 验收结果：节点13 Node 测试 9 通过、0 失败，覆盖闸口区间与逐动作重新随机、目标状态超时、话题校验、前两项素材判断、mock UI 全步骤序列、无封面继续、素材下载失败和 `TOPIC_PENDING` 终止；节点12H updater 5 通过、0 失败，既有 control-loop 测试通过。12 个手机脚本 `node --check` 全部通过；节点13新文件最大 291 行。
- 偏差与决策：不修改 1274 行 `uploader.js` 和既有 `storage.js`；入口复用 `uploader.ackCommand`，结果接口在热更新入口内按既有 mobileAuth 规则签名，素材目录沿用 storage 的 cache/base 目录约定。`control-loop.js` 只增加 1 个惰性 handler 变量和 8 行分发，`main.module.js` 只注入 1 行 `localRequire`，业务逻辑均位于 `features/publish-video` 与 `domain`。本节点不做视频号或真机操作。
- 真机待验：真实 `videoUrl/coverUrl` 下载及系统相册刷新；抖音从相机页到发布成功的全流程；话题不足上报后后台人工补全并重新下发；观察每个相邻动作的响应/等待双闸口；成功与各失败状态回传；经节点12H发布并覆盖此执行器后重启加载新版生效。
- 给下一节点的提醒：节点14只新增视频号执行器，不要改写本节点抖音动作；真机验证若抖音版本导致选择器变化，应先保留日志与截图，再在 `抖音发布界面.js` 收紧页面识别，纯逻辑与入口状态映射保持不变。

### 节点14：视频号发布流程（2026-07-28 代码完成，真机待验）
- 产出文件：`mobile-agent/autojs/features/publish-video/视频号发布全流程.js`、`视频号发布界面.js`、`视频号验证弹窗标记.js`、`发布视频-入口.js`、`mobile-agent/autojs/tests/publish-video-channels.test.js`、`publish-video-flow.test.js`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`。
- 验收结果：节点14测试 5 通过、0 失败，覆盖启动校正三种决策、验证弹窗特征、完整动作顺序、流程中弹窗立即终止及复合标题动作不继续交互；与节点13合并为 15 通过、0 失败。control-loop 回归通过，12H updater 5 通过、0 失败；相关文件语法检查通过，新生产文件最大 336 行，共享入口 283 行。
- 偏差与决策：不修改 `抖音发布界面.js`、五个抖音动作文件或三个共享 domain 模块；只在共享 `发布视频-入口.js` 增加平台判断和惰性视频号 handler，`WECHAT_CHANNELS/CHANNELS/视频号` 走视频号，缺少平台字段继续默认抖音。验证弹窗只读取强特征，上报 `CHANNELS_VERIFY_PENDING`，特征写入 error 与命令 ACK，不尝试点击、滑块、验证码或身份验证。
- 真机待验：微信不在前台或底栏非“信息”时完成启动校正；从发现进入视频号并走完素材、标题、导出、描述话题和发表；额外验证弹窗标记与人工链路；抖音/视频号平台任务在同设备顺序执行；经12H热更新覆盖后重启加载新版。
- 给后续节点的提醒：视频号任务下发必须在 payload 的 `platform/platformName/targetPlatform` 中携带 `WECHAT_CHANNELS` 或“视频号”；若现场微信版本不暴露底栏 selected 状态，应保留节点树和截图后只调整 `视频号发布界面.js`，不得把“信息文本可见”等同于已选中。

### 节点N1：冻结旧业务（双保险，可逆）（2026-07-28 完成）
- 产出文件：`account-data-platform/apps/api/src/config.ts`、`config.test.ts`、`routes/admin.ts`、`services/legacy-freeze.ts`、`services/legacy-freeze.test.ts`、`apps/api/.env.example`、`account-data-platform/apps/web/src/routes/App.tsx`、`DashboardPage.tsx`、`DevicesPage.tsx`、`account-data-platform/apps/web/tests/device-account-binding.test.js`、`live-targets-page.test.js`、`navigation-dashboard-status.test.js`、`docs/superpowers/plans/2026-07-28-legacy-business-freeze.md`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`。
- 验收结果：定向测试 26 通过、0 失败；全量 `bun test` 为 82 通过、0 失败、219 个断言；`bun run typecheck`、`bun run lint`、`bun run build` 均退出码 0。Bun fetch 隔离验收原文：`[api] ready port=3132 LEGACY_BUSINESS_FROZEN=true`，`POST /admin/mobile-commands -> 409`，`{"error":{"code":"LEGACY_BUSINESS_FROZEN","message":"旧采集业务已冻结","details":{}}}`，`GET /admin/publish-tasks -> 200`；重启为 false 后：`POST /mobile/device-token/register -> 200`，`POST /admin/mobile-commands -> 201`，返回 `commandType:"START"`。清理输出为 `{"deviceCode":"n1-accept-b92d2fc4a0c4","commandsDeleted":1,"devicesDeleted":1}`，隔离端口已释放，`tmp/n1-accept.ts` 与日志已删除。
- 浏览器验收：侧栏无“任务调度”“配置”，工作台无任务调度跳转；设备页按钮集合为刷新、应用筛选、脚本参数、设备信息、账号绑定、刷新配置等，不含启动、暂停、关闭、启动/继续；远程脚本和发布任务页均加载真实表格。仅观察到既有 React 19/Ant Design 5 兼容与 Spin 用法警告，无路由或接口错误。
- 偏差与决策：采集命令精确按既有状态命令集合冻结 `START/PAUSE/RESUME/STOP`，`REFRESH_CONFIG/CHECK_UPDATE/UPDATE_AGENT/RESTART_APP` 等设备维护命令继续放行；`PUBLISH_VIDEO_TASK` 与 `SCRIPT_CONFIG_UPDATED` 内部下发链路不经过守卫。`DashboardPage.tsx`、`DevicesPage.tsx` 最终 505/659 行，属于存量超限文件，按 AGENTS.md 约束不在 N1 重构；新守卫 32 行、测试 65 行，`admin.ts` 353 行且仅增加 10 行接线。基线两个旧 Web 文案断言已按当前“商品卡养号/目标直播评论”文字修正。未修改或提交 `mobile-agent`。
- 给下一节点的提醒：生产冻结时设置 `LEGACY_BUSINESS_FROZEN=true` 并重启 API；删除变量或设为 false 后旧状态命令与任务分配恢复。不得把守卫提升为全局 middleware，以免影响 `/admin/publish-tasks/*`、`/admin/remote-scripts/*`、`/admin/devices/*`、`/admin/agent-versions/*` 和 `/api/v1/mobile/*`。

### 节点N2：视频发布页面骨架（4 Tab 复用拼装）（2026-07-28 完成）
- 产出文件：`account-data-platform/apps/web/src/routes/PublishVideoModulePage.tsx`、`PublishTasksPage.tsx`、`RemoteScriptsPage.tsx`、`DeviceList.tsx`、`DeviceBindingList.tsx`、`DevicesPage.tsx`、`App.tsx`、`account-data-platform/apps/web/tests/publish-tasks-page.test.js`、`publish-video-module.test.js`、`docs/superpowers/plans/2026-07-28-publish-video-module.md`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`；截图：`outputs/n2-task-dashboard.png`、`outputs/n2-business-config.png`、`outputs/n2-device-binding.png`。
- 验收结果：测试先因 N2 页面、共享主体和设备列表不存在得到 2 通过、5 失败，实施后定向 7 通过、0 失败；全量 `bun test` 为 86 通过、0 失败、219 个断言，`bun run typecheck`、`bun run lint`、`bun run build` 均退出码 0。新文件 `PublishVideoModulePage/DeviceList/DeviceBindingList` 为 43/146/33 行，`RemoteScriptsPage` 314 行，均小于 400 行；存量 `DevicesPage` 由 659 降到 564 行。
- 浏览器验收：隔离 Web `/publish-video` 的侧栏顺序严格为“工作台→视频发布→设备→采集记录→日志中心”；四个 Tab 均可切换，“任务操作”显示“开发中”。任务看板与旧 `/publish-tasks` 的统计和表格逐值比较完全一致；业务配置只返回 1 条 `publish_video`，隐藏脚本类型筛选并保留新建、编辑、绑定设备、启停和删除；设备绑定展示全部 2 台设备及绑定状态，账号绑定弹窗可打开并回显抖音号 ID、抖音号名称、微信视频号名称。旧 `/publish-tasks` 与 `/remote-scripts` 均可直达，后者仍保留全部脚本类型筛选。
- 热更新入口检查：Web 没有 biz-scripts 版本发布表单或 `/admin/agent-versions` client；`DevicesPage` 的“检查版本/更新脚本”只是设备维护命令。业务热更新仍由 `scripts/bundle-autojs.ps1` 打包、`scripts/publish-autojs-version.ps1` 发布，因此隐藏旧“远程脚本”菜单无需迁移版本发布入口。
- 偏差与决策：发布任务和远程脚本均在原文件导出可嵌入主体，旧页面变为薄壳；设备表格机械移入 `DeviceList`，原设备页通过 action renderer 保持脚本参数、设备信息、绑定和四个维护命令，新 Tab 只渲染绑定控件。未新增样式体系，复用 Ant Design Tabs 与既有 ops 样式；未修改 `apps/api` 或 `mobile-agent`。浏览器只观察到既有 React 19/Ant Design 5 与 Spin 用法警告，无新路由或接口错误。
- 给下一节点的提醒：N3 在“任务操作”Tab 内填充操作能力，不要重新建立独立侧栏入口；业务配置继续固定 `publish_video`，任务看板继续复用 `PublishTasksContent`，避免分叉查询键或状态逻辑。

### 节点N3：任务操作 Tab + 手动测试发布接口（2026-07-28 完成）
- 产出文件：`account-data-platform/packages/types/src/domain/publish-task.ts`、`account-data-platform/apps/api/src/repositories/publish-task.repository.ts`、`services/manual-publish-test.service.ts`、`routes/publish-tasks.ts`、`routes/publish-task-manual-test.test.ts`、`account-data-platform/apps/web/src/lib/api-client-publish-tasks.ts`、`routes/PublishVideoModulePage.tsx`、`PublishTaskOperations.tsx`、`ManualPublishTestModal.tsx`、`styles.css`、`tests/publish-video-module.test.js`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`。
- 自动验收：路由测试先以 4 个 404 红灯证明接口缺失，实施后成功、配置停用、命令失败事务回滚、设备不存在和参数非法为 5 通过、0 失败；Web 测试先因操作组件/弹窗/受控 Tab 缺失红灯，实施后 5 通过、0 失败。全量 `bun test` 为 92 通过、0 失败、236 个断言；`bun run typecheck`、`bun run lint`、`bun run build` 均退出码 0。新增/修改 TS/TSX 文件最大 378 行，均小于 400 行；未修改 `mobile-agent`。
- curl 与数据库证据：真实库配置 `9c893424-a502-4fd1-9168-4b6c09e4314c`、设备 `ca78c663-6ca5-494b-9dde-29690fb7b6e0` 调用 `POST /api/v1/admin/publish-tasks/manual-test` 返回 `idempotent=false`。`publish_tasks.id=91e73535-784c-4f39-8393-3ce906c15bd4`，`taskId=manual-88f94642-9b38-492c-ab3e-d7acdac3ee6c`，`platform=DOUYIN/status=DISPATCHED`；`mobile_commands.id=069e7363-a30a-4493-a764-d780acf0a603`，`commandType=PUBLISH_VIDEO_TASK/status=PENDING`，payload 含视频 URL、空封面、响应闸口 700/1300、等待闸口 900/1700、话题数 5 与 `/sdcard/Download/n3-manual`。
- 浏览器验收：隔离 Web `http://127.0.0.1:3137/publish-video` 上，“领取一条”提示“无待发布任务”；“立即调度”提示“已下发 0 条，已回传 0 条”；手动弹窗展示配置、设备、平台、视频 URL、封面 URL、标题、描述，封面明确可留空、描述提示需含 5 个 `#` 话题。留空封面提交后提示“手动测试已下发”，弹窗关闭、自动切到任务看板，新任务 `manual-7b4eb15e-ca8e-48c7-8578-05266723bebf` 以“已下发”状态可见。
- 偏差与决策：真实库原有唯一 `publish_video` 配置已停用且指向真实外部服务，未擅自启用。为安全点通领取和调度，新建 `N3验收配置-20260728`，无发布时间窗且外部地址指向本机 3135 mock；该配置保留用于本地页面复验。React 19 下 Ant Design 静态 message 不渲染，浏览器发现后以失败契约驱动改为项目既有 `message.useMessage()`，三类通知均实测可见。提交前审查将任务写入、命令创建与设备命令时间更新收进同一数据库事务，避免命令失败后遗留 `DISPATCHED` 任务；设备下拉将 `online` 与 `running` 都视为活跃并优先展示。
- 给下一节点的提醒：手动任务复用共享 `createPublishVideoTaskCommand`，命令幂等键仍为本地任务 UUID 与设备 ID；`manual-` 前缀只隔离外部任务唯一键。账号名按当前非空数据库约束保存为空字符串，配置 ID 保存请求中的真实配置。不要让手动测试调用外部领取或复制手机命令组装逻辑。

### 节点N4：手机端素材目录管理 + 执行器锁 + 缺料即报（2026-07-28 完成）
- 产出文件：`mobile-agent/autojs/domain/素材目录管理.js`、`domain/发布任务锁.js`、`features/publish-video/发布视频-入口.js`、`视频号发布全流程.js`、`tests/发布素材目录管理.test.js`、`publish-video-flow.test.js`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`。
- 素材与锁：根目录严格按 `payload.downloadDir → config.publishMaterialRoot → /sdcard/` 解析；目录序号使用 `AgriVideoPublishMaterial` 按 `yyyyMMdd` 持久化，生成 `yyyyMMdd`、`yyyyMMdd（1）` 等全角括号目录，每次只写 `video.mp4/cover.jpg/.keep`。历史目录仅在全部条目命中 `video.* / cover.* / .keep` 且无子目录时删除，混入陌生文件则告警跳过。下载前同时校验两个 URL，每个文件落盘后立即 `media.scanFile`，入口最外层 `finally` 删除本次目录。
- 互斥与触发路径：`AgriVideoPublishTaskLock` 保存 owner 与时间戳，15 分钟内拒绝第二条并上报 `PUBLISH_BUSY/上一发布任务仍在执行`，过期可接管且旧 owner 不能误释放新锁。生产链路唯一为 `control-loop.js → 发布视频-入口.js → 抖音或视频号`；视频号没有独立生产触发点，由共享入口统一持锁、下载和清理，因此未重复加锁，`control-loop.js` 无需修改。
- 自动与模拟验收：先得到两个 `MODULE_NOT_FOUND` 红灯；实施后发布相关定向测试 20/20，手机端全量 `node --test mobile-agent/autojs/tests/*.test.js` 为 42/42，API/Web `bun test` 为 92/92、236 个断言。离线内存文件系统预置纯白名单目录与混入 `family.png` 的目录后执行 begin/download/end：前者删除，后者保留并记录跳过告警，本次 `/sdcard/20260728` 下载期间存在且结束后消失；`coverUrl=null` 精确返回“缺少封面素材：接口 coverUrl 为空”；重入第二条任务返回 `PUBLISH_BUSY`。6 个相关脚本 `node --check` 通过，最大文件 385 行，未修改 `config.js`、APK 基座层或 `project.json`。
- 热更新发布：`bundle-autojs.ps1 -Version 1.1.0` 生成 35 条目 ZIP，包含两个新 domain 文件，`config.js` 不存在，越界路径 0；SHA256 为 `d0b44d577236b5220325bbfa1b7d3008f1ba504735ea80afde3887866469b461`。`publish-autojs-version.ps1` 以说明“素材目录管理+执行器锁”发布成功，后台 `agent_versions.id=2f687fdd-df7c-4bb8-bbe7-4a174456bad6`、`channel=biz-scripts/version=1.1.0/status=PUBLISHED`；从 3015 下载 71078 字节，SHA 与 manifest 一致。
- 给下一节点的提醒：素材目录 storage 序号故意不在 `endTask` 回退，跨进程/重启保持单调不重号；混入陌生文件的日期目录属于用户资产保护边界，不得强删。若未来新增视频号独立入口，必须在该入口补同一锁和 finally 生命周期，当前不可绕过共享发布入口。

### 节点N5：设备绑定开关 + 状态列 + 匹配规则核对（2026-07-28 完成）
- 产出文件：`account-data-platform/apps/web/src/routes/DeviceAccountBindingControl.tsx`、`DeviceList.tsx`、`DeviceBindingList.tsx`、`DevicesPage.tsx`、`apps/web/src/lib/api-client-devices.ts`、`apps/web/tests/device-account-binding.test.js`、`publish-video-module.test.js`、`account-data-platform/apps/api/src/services/publish-match.service.ts`、`publish-match.service.test.ts`、`routes/device-account-binding.test.ts`、`repositories/publish-task.repository.ts`、`services/publish-scheduler.service.test.ts`、`account-data-platform/packages/types/src/domain/wecom-publish.ts`、`wecom-publish.test.ts`、`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`。
- 绑定与列表：绑定弹窗新增“绑定发布账号”开关；已有有效 `douyinAccountName` 默认开启并回填，无绑定默认关闭。关闭前显示“解除绑定后该设备将不再接收指定账号的发布任务”二次确认，保存提交空 `accountProfile={}`；开启时抖音账号名称必填，抖音号 ID 与微信视频号名称选填。视频发布设备绑定 Tab 与 DevicesPage 复用同一列，已绑定直接显示 trim 后的抖音账号名称，未绑定显示灰色徽标。
- 匹配规则结论：原实现已有“启用设备 + `accountProfile.douyinAccountName` 原样精确匹配”与 MATCHED/UNMATCHED 落库；PATCH 本来就是 JSONB 替换语义，空对象可直接清空。本节点补齐 accountName trim、最近 3 分钟心跳在线门槛、指定账号未命中文案、`accountName=null` 进入未绑定设备池、未绑定池按最近心跳优先，以及同账号多设备按设备 `updatedAt` 最早者优先并以 ID 稳定兜底。外部 null 账号落库为空字符串，以兼容 `publish_tasks.account_name` 既有非空约束。
- 自动验收：TDD RED 阶段 7 个失败分别锁定 null 契约、trim/在线/文案、未绑定池和 Web 开关/状态列缺口；PATCH `{}` 清空测试直接通过，确认无需改 Admin 路由。实施后定向测试 12 通过、0 失败、37 个断言；全量 `bun test` 为 96 通过、0 失败、249 个断言，`bun run typecheck` 与 `bun run lint` 均退出码 0。N5 新增/主要修改文件最大 236 行；存量 `DevicesPage.tsx` 仅增加一行启用状态列，未在本节点重构；未修改或提交 `mobile-agent`。
- 手动验收：浏览器关闭绑定并确认后，列表立即显示灰色“未绑定”，API 与数据库均为 `accountProfile={}`；重新开启并填写三字段保存后，API/数据库回显完整字段，视频发布绑定 Tab 与 DevicesPage 均显示实际抖音名称。隔离构造三类真实任务后，指定已绑定账号为 MATCHED 且命中正确设备，指定不存在账号为 UNMATCHED 且原因精确为“无绑定该抖音号的设备：N5不存在账号”，`accountName=null` 为 MATCHED 且命中未绑定设备。随后清理 3 条任务、1 条配置和 2 台隔离设备。
- 给下一节点的提醒：未指定账号任务只允许进入无有效 `douyinAccountName` 的在线设备池；绑定展示和匹配键都只认 trim 后的 `douyinAccountName`，仅填写 ID 或视频号名称仍视为未绑定。若未来要把“绑定更新时间”与设备心跳/维护更新时间彻底解耦，应单独新增专用时间字段并迁移，不能改用心跳排序替代当前约定。

### 节点N5B：业务规则对齐（2026-07-28 完成）
- 产出文件：`account-data-platform/apps/api/src/services/publish-config.ts`、`publish-topics.ts`、`publish-match.service.ts`、`publish-scheduler.service.ts`、`publish-task-result.service.ts`、`publish-command.service.ts`、`routes/publish-tasks.ts`、`routes/mobile-publish-tasks.ts`、`repositories/publish-task.repository.ts`、`publish-dispatch.repository.ts` 及对应测试；`account-data-platform/apps/web/src/routes/RemoteScriptConfigModal.tsx`、`apps/web/tests/publish-video-module.test.js`；`mobile-agent/autojs/domain/话题校验.js`、`话题断点续传.js`、`features/publish-video/发布视频-入口.js`、`视频号发布全流程.js`、`tests/publish-video-domain.test.js`、`publish-video-flow.test.js`、`publish-topic-continuation.test.js`；`docs/项目开发/项目记录.md`、`docs/项目开发/远程脚本模块/进度台账.md`。
- 配置兼容：`dailyLimitPerAccount` 已从运行时 schema、动态业务配置 schema、类型、Repository 计数和调度分支删除。存量 JSON 不做数据库迁移；Zod 解析使用 strip 忽略历史键，旧配置打开表单时 Web 删除该键并补 `topicResolveTimeoutMinutes=30`，保存后自然净化。`expectedTopicCount` 运行时默认 5，话题补全超时允许 1~120 分钟、默认 30。
- 调度与守卫：每个 slot 固定先领空抖音队列，再领空视频号队列；同账号连续任务不再计数或截断。视频号 null 账号直接 UNMATCHED，原因“视频号任务必须指定账号并绑定视频号”；命中设备无有效 `wechatChannelsName` 时不创建命令，外部 PATCH 与本地 REPORTED 原因均为“未发布：该设备未绑定视频号”。命令 payload 新增真实平台与 `topicResolveTimeoutMinutes`。
- 三层话题断点：API 与手机分别实现同口径校验，要求 `#` 数量精确等于配置且每个 `#` 后首字符为有效文字。claim 落库后、匹配前不合格即 TOPIC_PENDING 并记录原 slot；补全接口先复验，不合格返回 400，合格后按有无 `matchedDeviceId` 选择幂等重发或首次匹配下发。手机共享入口持锁后、下载前防御校验；流程中待补先回报 TOPIC_PENDING 且不 ACK，通过新增 `GET /api/v1/mobile/publish-tasks/:id/topics?deviceId=...`（mobileAuth/HMAC）每 20 秒查询，拿到新 description 后在当前页面覆写继续，超时固定上报“话题补全超时”，外层 finally 清素材并释放锁。
- 自动验收：API/Web 全量 `bun test` 为 107 通过、0 失败、286 个断言；`bun run typecheck`、`bun run lint` 均退出码 0。手机端全量 Node 测试 47 通过、0 失败；4 个改动业务脚本 `node --check` 通过。所有 N5B 修改/新增代码文件均不超过 400 行，最大为 351 行的存量拆分后测试文件；未修改 `config.js` 或 APK 基座层，未纳入既有 `project.json` 工作区改动。
- 离线数据库验收：同一 slot、同一设备的命令实际顺序为 `DOUYIN 11:04:16.942Z`、`WECHAT_CHANNELS 11:04:16.981Z`；无视频号绑定任务为 REPORTED/“未发布：该设备未绑定视频号”且无命令；4 个 `#` 的任务为 TOPIC_PENDING、未匹配、无命令，错误“应有5个#，实际4个”，无效补全返回同一错误，有效补全后变为 DISPATCHED、匹配设备并生成命令。隔离配置、设备、任务和命令已清理，残留配置数为 0。
- 热更新发布：`biz-scripts v1.2.0` 共 36 个 ZIP 条目，包含 `domain/话题断点续传.js`，`config.js` 不存在且越界路径为 0；ZIP 72806 字节，SHA256 `7cc73342cded1ea3faa14731523ac3123f03c98b6e217b1e916e408e55ab6abc`。版本 ID `4cc9b861-802d-4691-92a7-523bfcdc2976`，`channel=biz-scripts/status=PUBLISHED`，releaseNote 为“话题断点续传”；从记录中的 packageUrl 下载后 SHA 复核一致。
- 给 N6 的提醒：真机需验证等待期间页面不跳转且持续亮屏、后台补全后当前输入框被覆写并继续发布、30 分钟超时路径，以及同设备收到双平台命令后的实际串行顺序。不要把轮询改为重新领取或重新打开 App；补全断点必须保留当前素材、页面和执行器锁。

### 节点N6：真机联调紧急修复包（2026-07-28 完成）
- 下载托管：`apps/api/src/routes/downloads.ts` 白名单新增 `.mp4`、`.jpg`，并返回 `video/mp4`、`image/jpeg`；新增 API 路由测试覆盖两种素材。3012 进程实际工作目录为 `apps/api`，打包默认输出已固定到 `account-data-platform/apps/api/dist/agent`。`http://192.168.7.170:3012/downloads/agent/AgriVideoCollector-biz-scripts-1.3.1.zip` 实测可被 3012、3136 与真机下载，SHA 一致。
- 热更新根因与发布：`biz-script-updater.js` 下载地址来自 `agent_versions.packageUrl`。`scripts/bundle-autojs.ps1` 的内嵌与外部 JSON manifest 均改用无 BOM UTF-8 写入，并在发布前扫描 ZIP 中全部 `.js/.json`，发现 `EF BB BF` 即中止。`biz-scripts 1.3.1` 共 36 个条目、不含 `config.js`，SHA256 `4a4cc97fa18f6ef5938b5554226fcdd972fb62392d43446acc30f08da1e90e85`；后台版本 ID `80b5275d-8658-42c2-97c1-258cea030d36`，`PUBLISHED`，packageUrl 为 LAN 地址。
- 执行日志：`control-loop.js` 的 `PUBLISH_VIDEO_TASK` 分支入口增加“发布执行器启动”（该文件属于 APK 基线，需下一次 APK 生效）；热更新层的发布入口与视频号流程已覆盖 handle、下载 URL/超时、下载字节、打开 App、每个 gate 前后、finish 的 INFO 日志。下载超时默认 20 秒并写入日志。
- 看门狗：热更新域新增 `发布执行器看门狗.js`，共享发布入口启动 10 分钟看门狗；超时强制 `commandControl.polling=false`，ACK 当前命令为 `FAILED/执行器看门狗超时`，完成路径不会重复 ACK。回归测试覆盖计时器单次触发、入口超时复位与单 ACK。
- 自动验收：手机端 Node 全量 `52 pass / 0 fail`；API/Web Bun 全量 `109 pass / 0 fail / 292 assertions`；`bun run typecheck`、`bun run lint`、`bun run build` 均退出码 0；相关手机脚本 `node --check` 通过。
- 真机复测：设备 `device_03be8de3a35ae0ce` 下载 SHA 与发布记录一致；日志记录 `业务脚本热更新完成 {"fromVersion":"0.0.0","toVersion":"1.3.1"}`，重启执行器后的连续心跳上报 `bizScriptsVersion:"1.3.1"`，未再出现 `SyntaxError`。热更新中断旧 `main.js` 属预期行为，需重新启动一次 Agent 加载新脚本。防御纵深（APK 基座在 `JSON.parse` 前剥 BOM）未在本次热更新修改，随下次 APK 基线处理。

### 2026-07-28 视频发布大模块一期重构

- 业务入口拆分为任务看板、粘贴发布、接口定时、发布设置、设备账号和高级调试；原领取、立即调度、手动测试仅保留在高级调试。
- 配置以 sourceMode 区分直接素材与外部拉取：粘贴发布与发布设置不展示外部地址或 Token；接口定时页集中管理外部地址、Token 环境变量名、时间窗、平台、连接测试和立即下发。
- 发布任务补齐来源、执行方式、回写状态与错误投影；粘贴发布默认仅本地看板回显，外部任务才进入回写 outbox。
- 新增外部接口 OPTIONS 连接检测，不记录或返回真实 Token；待在目标环境配置 externalTokenEnv 后进行接口联调。数据库迁移 0026 需先正常应用，当前本地历史迁移冲突不通过手工建表绕过。
- 本节点未改动 AutoJS 手机端主流程，保留既有发布任务协议和现有未提交手机端改动；完整 typecheck、lint 与业务定向测试待执行者按工作区环境运行。

### 2026-07-29 发布设置保存卡顿一期修复

- 保存成功后立即关闭弹窗，当前列表以精确 queryKey 异步刷新；默认直接素材配置额外刷新粘贴发布查询，不再等待宽泛 `remoteScriptConfigs` 前缀刷新。
- Web API 客户端统一 20 秒超时，保留既有 401 处理；超时提示明确说明保存结果可刷新列表确认。发布设置默认补齐四个闸口、话题、封面、目录等参数，隐藏锁定的 `sourceMode`，直接素材保存前清除外部接口字段。
- 后端列表支持 `sourceMode` 过滤。PATCH 比较规范化后的配置；无变化不递增 revision、不通知设备，有变化后异步创建设备刷新命令，失败仅记录 warning，不阻塞 HTTP 保存响应。
- 直接素材列表展示默认标签，多个默认配置给出前端警告；未改动 AutoJS 手机端主流程或写入真实 Token。

### 2026-07-29 发布页 Ant Design 消息上下文修复

- 根节点已挂载 Ant Design `App` 上下文；发布配置、接口定时、发布看板和配置绑定改用 `App.useApp().message`，不再调用无法读取动态主题的静态 `message`。

### 2026-07-29 当前项目 API 与数据库对齐

- 盘点容器后确认 `auto-collection-postgres-dev:15432` 不含远程脚本和发布表；当前项目实际数据位于 `auto-collection-clean-postgres` 映射的 `127.0.0.1:5432/auto_collection`。
- 对该库完成开始前的 custom-format 备份；原 Drizzle 账本为空但 `0000–0025` 结构已存在，因此在单个事务内基线化历史迁移并应用 `0026_publish_task_lifecycle`。已验证新增发布字段、两张新表和 27 条账本记录。
- 新增被忽略的 `.env.development`，API 开发脚本显式加载它；Web 通过相对 `/api/v1` 请求经 Vite 代理转发到当前 API，统一使用 3012/3024/5432/6379 本地开发端口。

### 2026-07-29 抖音发布开直播页相机恢复热修复

- 真机日志确认 biz-scripts 1.3.3 的 openCamera() 在点击发布入口后直接返回，导致误进“开直播”页时，后续“选择发布素材”闸口一直等不到相册入口。
- douyin-publish-ui.js 现按动作链执行：点击 + → 轮询相册确认相机页 → 非相机页时通过精确文本、空白容错文本、包含式无障碍描述和包含文本查找“拍摄/相机”页签 → 点击后再次确认相册。每一步输出入口点击、页签发现/点击和相机就绪日志。
- 新增无障碍描述为包含文本的回归测试；douyin-publish-ui.test.js 三项通过，待通过 biz-scripts 1.3.4 热更新在真机验证。

### 视频发布节点 01：设备账号匹配与视频号路由（2026-07-29 完成）
- 外部抖音和视频号任务统一按设备绑定的抖音账号名称或 ID 路由；视频号名称仅用于命中后能力校验。
- 缺抖音账号、仅视频号名称相同、或设备未绑定视频号时不生成发布命令，并保留稳定未发布原因。
- 定向验证：publish-match 9 通过；publish-scheduler 覆盖无视频号能力不创建命令。

### 视频发布节点 04：素材校验与封面必填（2026-07-29 完成）
- 后端新增统一素材校验：视频、封面均要求存在且为 HTTP/HTTPS URL；缺视频、缺封面、视频 URL 非法、封面 URL 非法分别使用稳定失败码 VIDEO_REQUIRED、COVER_REQUIRED、VIDEO_URL_INVALID、COVER_URL_INVALID。
- 外部 claim 响应先按宽松契约保存，再由服务层校验；素材无效时本地标记为 REPORTED、不匹配设备、不创建 PUBLISH_VIDEO_TASK，并 PATCH 外部为“未发布”。同一素材的抖音/视频号任务均会被独立回写失败。
- 手动测试、粘贴发布与命令构造统一要求非空封面 URL；历史任务在命令层再次校验，避免空封面进入 AutoJS 下载阶段。默认 requireCover 已改为 true，手机端流程未改动。
- 验证：定向 API/types/web 回归 55 通过、0 失败；wecom-publish-client 4 通过、0 失败；bun run lint、bun run typecheck 均退出码 0。调度测试已隔离活跃任务，继续验证同槽抖音先下发、视频号进入 PUBLISH_BUSY 重试。
- 生命周期补修：即时 PATCH 成功后统一写入 status=REPORTED、reportedAt 与 reportStatus=REPORTED；PATCH 失败则写入 status=FAILED、reportStatus=REPORT_FAILED、reportLastError 并继续抛出错误，不再留下 REPORTED/REPORT_PENDING 矛盾状态。新增素材失败、未命中、未绑定视频号与 PATCH 失败覆盖；定向 14 通过、0 失败，typecheck 与 diff 检查通过。

### 视频发布节点 05：话题校验与断点补全（2026-07-29 完成）
- 后端主闸门保持在任务匹配前：话题数量或内容不合格时保存 TOPIC_PENDING，不匹配设备、不创建命令、不提前回写外部最终失败；补全后复用原任务及幂等键，已匹配任务沿用原设备，未匹配任务再走匹配。
- 前端补全弹窗与快速粘贴复用 validateTopicDescription，按任务 expectedTopicCount 实时显示“实际数/要求数”并在提交前阻断空话题或数量不符的描述；看板接口返回动态话题数量，缺失或异常时回退为 5。
- 手机端共享入口在命令前校验话题，视频号任务同样在话题不足时不上下载、不启动 App；视频号流程内界面选题校验失败后先上报 TOPIC_PENDING，轮询补全文案并在当前页面继续发表。补全超时由手机端执行中等待逻辑失败收尾，清理素材并释放发布锁，未额外新增后端超时 worker。
- 验证：Web/API 定向测试 30 通过、0 失败；手机端话题、发布域、抖音流程、视频号流程定向测试 23 通过、0 失败；bun run typecheck 与 git diff --check 均通过。

### 视频发布节点 06：发布设置时间闸口秒转毫秒（2026-07-29 完成）
- 发布设置动态表单的四个时间闸口字段现在以秒显示与编辑：响应延迟、动作等待的最小值和最大值均允许小数秒；旧配置打开时从 payload 毫秒除以 1000，保存时统一 Math.round(seconds * 1000) 还原为毫秒。
- 转换仅适用于 publish_video，表单内部保持秒、后端 API 和 AutoJS 命令 payload 继续保持既有 Ms 字段及毫秒语义；重复打开和保存不会二次乘除 1000。
- JSON 高级编辑标题明确提示时间字段保持毫秒；JSON 读入时转换为表单秒值，动态表单更新时反向同步毫秒 JSON。负数和最大值小于最小值会阻断保存。
- 验证：publish-execution-settings 与 dynamic-form-schema 定向测试 6 通过、0 失败；bun run typecheck 与 git diff --check 通过；未修改 API 发布主链路或 AutoJS。
- 后续节点 08：quick-paste-publish.ts 的 parseQuickPasteText 当前仍以默认 5 个话题校验，需改为读取默认执行配置的 expectedTopicCount 后再校验。

### 视频发布节点 07：任务看板状态与统计汇总（2026-07-29 完成）
- Dashboard 列表新增 failureCode、dispatchRetryCount、nextDispatchAt、lastDispatchAttemptAt；stats 新增 busy、topicPending、materialInvalid、channelsVerifyPending、reportFailed。
- 统计口径拆分：今日成功、今日未发、今日未命中只统计当日已回传终态；设备忙仅统计当前 PUBLISH_BUSY 等待重试任务，不计入今日未发。重试耗尽后任务回传 REPORTED 且带 PUBLISH_BUSY 失败码，才按既有口径进入今日未发。
- 前端补齐设备忙等待重试、话题待补、素材异常、视频号人工验证、外部回写失败的统计卡；状态颜色区分等待、异常和人工处理，错误列显示缺封面、缺视频与 URL 无效等结构化失败码。PUBLISH_BUSY 行展示第 n/3 次和预计下次重试时间。
- 验证：publish-tasks-page、publish-dispatch.repository、publish-busy-retry、publish-material-dispatch、publish-task-result 定向测试 17 通过、0 失败；bun run typecheck 与 git diff --check 通过。


### 节点08：前端粘贴发布与高级调试修复（2026-07-29 完成）
- 产出：高级调试查询拆分、按执行配置校验粘贴话题数、HH:mm 时间窗校验与设备视频号能力文案。
- 验收：5 个 Web 定向测试文件共32 项通过；类型检查、差异空白检查通过。
- 偏差与决策：不回改 01-07 后端主流程、看板统计口径或 AutoJS；多个 direct_material 默认配置不再静默选第一个，改为明确提示并阻断提交。
- 给下一节点的提醒：快速粘贴的话题数已从执行配置读取，后续修改 expectedTopicCount 时保持此单一口径。


### 节点09：AutoJS 手机端发布流程对齐（2026-07-29 完成）
- 产出：确认抖音与视频号均通过 publish-video-entry 共享入口获取本地锁、素材下载、结果上报、ACK 与 finally 清理；补强视频号验证待处理和启动异常时的共享入口清理测试。
- 验收：publish-video-flow 6 项通过，publish-video-channels 6 项通过，publish-topic-continuation 3 项通过，publish-video-preloader 1 项通过，control-loop 测试通过；发布入口、视频号流程、预加载器语法检查通过；差异空白检查通过。
- 偏差与决策：不为手机端新增 3 次/5 分钟 busy retry，保留 15 分钟本地发布锁作为防御；不回改 01-08 的后端、Web 主流程。
- 给下一节点的提醒：新增发布相关 AutoJS 模块时，必须同步更新 publish-video-preloader 清单与 control-loop 预加载断言。


### 节点10：最终联调验收（2026-07-29 离线通过）
- 验收：API/types 定向测试 45 项通过，Web 定向测试 39 项通过，AutoJS 发布相关定向测试全部通过；全仓 Bun 测试 181 项通过，类型检查、lint、差异空白检查通过。
- 修复：设备绑定测试从过时私有函数名断言改为抖音账号与视频号能力的行为断言；修复发布配置更新时不必要校验已存储 payload 导致的兼容性崩溃；补齐 publish-task 领域契约测试。
- 真机状态：本节点未下发真实发布任务或调用生产外部接口；抖音/视频号成功 UI 特征仍需在有效授权的真机环境单独校准。

### 2026-08-02 抖音发布成功后后台清理保护加固（真机通过）
- 发布成功备用判定必须先观察到“发布进度/正在发布/上传中”，随后连续三次稳定回到首页且发布表单已消失；普通首页不再直接判定成功，明确成功文案收敛为“发布成功/作品发布成功/作品已发布”。
- 确认成功后仍固定等待 30 秒；冷却结束再次检查发布进度，仍在发布则以 `PUBLISH_STILL_IN_PROGRESS` 跳过最近任务清理，不打开系统后台。
- 最近任务先识别抖音节点，再沿父级查找可靠卡片边界并按该边界左滑；找不到可靠卡片或清理失败仅记录 warning，不改变已经确认的发布结果。
- 验证：5 个 AutoJS 定向测试文件共 33 项通过，4 个相关脚本 `node --check` 通过；改动源文件最大 400 行。未执行真机点击、热更新发布或真实视频发布。
- 真机验收：设备 `device_03be8de3a35ae0ce` 加载 `biz-scripts 1.3.7.20260802014308` 后，任务 `manual-76e122c0-2bd3-46f7-85d4-74eda72b089d` 成功完成素材下载、抖音发布、成功确认、固定 30 秒冷却及最近任务左滑清理；任务最终为 `REPORTED` 且无错误，命令回执为 `DONE/SUCCEEDED`。

### 2026-08-03 抖音后台清理后返回燎原星火（离线完成）
- ADB 确认 MIUI 最近任务页中“燎原星火”完整卡片通过 `content-desc="燎原星火,未加锁"` 暴露且卡片主体可点击；左上角应用图标会打开应用信息，因此不使用固定图标坐标。
- 抖音卡片左滑移除后等待 1 秒，优先按描述、其次按标题识别“燎原星火”，沿父节点点击完整可点击卡片；找不到或点击失败时调用系统 `home()` 返回桌面。
- 变更仅位于独立收尾模块及其测试，未修改发布入口、素材、封面、标题、成功判定和结果上报逻辑。5 个相关测试文件共 35 项通过，相关脚本语法与差异空白检查通过；收尾模块 136 行，发布入口 385 行。真机热更新待执行。

### 2026-08-03 外部接口发布契约与调度来源收口（离线完成）

- 外部 claim 统一为 `platform + accountName`，账号缺失、空字符串或纯空白均在 HTTP 请求前拒绝；旧 `claim-once`、`dispatch-now` 后台入口及 Web 操作已移除。
- 新设备发布计划的 `timeWindows` 成为唯一自动调度时间来源；`publishTimeSlots` 仅保留为默认空数组的存量兼容字段，远程脚本注册表不再要求填写，并在字段说明中标明不参与新调度。
- 接口定时页只维护连接信息并展示固定设备发布计划；设备、平台和时间窗口由计划统一配置。保留已入库接口任务单条直发，不会额外 claim。
- 验收：交接定向测试 `36/36`、扩展回归 `27/27`、全仓 Bun 测试 `205/205` 通过；完整 typecheck、lint 通过。修改的远程脚本源文件仍低于 400 行。
- 安全边界：未执行真实外部 claim、外部任务状态 PATCH、数据库迁移、真机操作或 AutoJS 修改，节点 09 staged 文件边界保持不变。

### 2026-08-03 发布任务终态与持久化锁自恢复（离线完成）

- 新增独立 `publish-task-finalizer.js`，成功发布在返回“燎原星火”前完成结果上报、命令 DONE、素材清理、发布锁释放和看门狗结束；重复终态调用不会重复上报或 ACK。
- `publish-task-lock.js` 保存 taskId、commandId、acquiredAt、expiresAt，并兼容旧 timestamp。过期、异常未来时间戳和无效记录允许新任务接管，PUBLISH_BUSY 日志可直接显示持锁任务信息。
- 并发保护：每次发布 handle 记录自身是否真正持锁，未持锁的忙任务不得清理第一条任务的素材或释放其锁。预加载清单、控制循环夹具与业务脚本打包检查已同步新模块。
- 验收：发布相关定向测试 `40/40`、Bun 全仓测试 `205/205` 通过；相关脚本语法、行数和差异检查通过。AutoJS 全量的 5 项既有话题续传/基线偏差另行处理，不属于本次终态修复。
- 未执行业务脚本版本发布、真机安装、锁清理或真实发布；下一步需发布新 biz-scripts 后用两条专用任务验证第一条返回 Agent 后第二条能够正常获取锁。

### 2026-08-04 养号：目标直播间互动第一节点（离线完成）

- 范围：先完成“搜索并进入目标直播间”的底座，不执行点赞、评论、关注、观看计时或计划任务。左侧主菜单新增独立“养号”，位置紧跟“视频发布”；“目标直播间互动养号”作为养号主模块当前唯一子功能，支持一个目标关键词、1～30 个相关名词、多设备并行、全部/单设备停止和逐设备结果。后续观看、点赞、评论继续在该子功能内部扩展，不新增同级子页签。
- 命令：新增通用 `ACCOUNT_WARMUP_RUN` 与 `ACCOUNT_WARMUP_STOP`，复用 `mobile_commands`，没有 schema/migration 变更。运行 payload 使用 `featureKey + batchId + config`，关键词和标签在后台规范化后下发，批次与设备结果保存在 payload/result JSON。
- 手机：`target-live-entry.js` 复用抖音包名启动和搜索，切换顶部直播分类并纵向检查 3×4 个候选；稳定区域无障碍文本优先、OCR 兜底，评论区和商品卡不参与匹配。失败候选通过最近任务左滑移除抖音、回主页并按包名重启。
- 并发与停止：APK 内独立命令桥接器只拦截通用养号命令，在后台线程运行脚本、抑制重复 FETCHED、继续轮询 STOP 并在动作边界终止。终态 ACK 明确失败时保留终态结果，下次轮询只重试 ACK，不重复执行手机动作。桥接器固定加载热更新 `registry.js`，注册表只允许 `target_live_interaction` 映射到目标直播脚本，拒绝任意路径。存量 `control-loop.js` 1493 行，本节点零修改并已登记卡点 57。
- 前端恢复：多设备下发改为汇总逐设备结果；部分成功时保留 batchId、展示成功/失败设备数，页面刷新后从本地存储恢复最近批次查询和停止入口。全部失败仍返回错误且不伪报成功。
- 验收：全仓 Bun `216/216`、typecheck、lint、生产构建、相关 AutoJS 测试、语法、行数和业务脚本打包通过；业务 ZIP 清单包含注册表和目标直播脚本，相关源文件最大 252 行。前端导航定向测试 `22/22` 确认“养号”位于“视频发布”后的独立主菜单，视频发布内部继续保留粘贴发布且不再挂载养号。AutoJS 全量 `110 pass / 5 fail`，仍是既有两项发布基线和三项话题续传偏差。
- 服务验证：开发 API `3012`、Web `3024`、PostgreSQL `5432`、Redis `6379` 均在线；`GET /health` 与 Web `/publish-video` 返回 200。未知养号 `featureKey` 返回 400，验证前后命令总数保持 `1 → 1`，未向手机创建任务。
- APK 验证：已手动生成 `燎原星火-1.0.67-inrt.apk`（versionCode 103，SHA-256 `741271b7b4be272fdd7cd73a93ea8184a5ec5586c24bb163e8789d1d05ee3383`）。`apksigner` 验证通过且证书 SHA-256 与现有 `1.0.66` 一致；包内确认包含 `main.module.js`、通用养号桥接器、`registry.js` 和 `target-live-entry.js`。设备 `8185c828` 已通过 `adb install -r` 从 `1.0.66 / 102` 覆盖升级至 `1.0.67 / 103` 并保留应用数据。当前未启动新版本或下发真实养号命令；安装此基线后，新增养号 feature 只需发布 biz-scripts 注册表和业务脚本。文档未记录签名口令，密钥文件未复制进仓库。

### 2026-08-04 粘贴发布预览封面 COS 映射修复

- `/public/downloads/{草稿ID}.mp4` 继续转换为腾讯 COS `/videos/{ID}.mp4`，自动封面改为同一 COS 桶的 `/covers/{ID}.jpg`，不再使用已返回 404 的旧阿里 OSS 地址。
- Web 预览和 API 外部素材规范化使用相同规则；用户显式提供的封面 URL 不被覆盖。
- 实例 COS 封面实测 HTTP 200、旧 OSS 地址 404；Web/API 定向测试 `25/25`、全仓 Bun `218/218`、typecheck、lint、生产构建和差异检查通过。相关文件最大 276 行。

### 2026-08-04 养号快速搜索与停止任务（部署完成，真机待手动验收）

- 新增 `features/account-warmup/fast-target-search.js`，由养号注册表在 Agent 启动阶段预加载并注入目标直播任务。搜索使用固定截止时间、节点优先和坐标/回车兜底，记录入口、输入、提交、结果确认四段耗时；不修改通用搜索、OCR 或直播列表滑动策略。
- 前端批次操作改为“停止任务”，批次和单设备停止均即时显示“停止中”；STOP 部分下发失败时，已成功设备继续等待，失败设备恢复可操作状态。
- APK 桥接器固定执行 `运行命令 ACK 成功 → 返回燎原星火`。ACK 失败时保留终态，下次轮询只重试 ACK，避免丢回执或重复运行。
- 产物：`biz-scripts 1.3.11.20260804144730`，ZIP SHA-256 `1083c2a4ff5cc64f96d8b04cb08e0d82fe7ddc59d530cc77a104f5dbe8803113`；APK `1.0.69 / 105`，SHA-256 `1737028864d4e817232cb0b108a99842f88e776af3f0738707306d11bd49fccf`。两者已部署，手机已加载新脚本并进入待命。
- 自动化验收：4 个养号 AutoJS 定向测试通过，Web 养号页面 `6/6` 通过；相关脚本语法、差异空白和 400 行门禁通过。自动化时钟覆盖的最坏受控等待约 9 秒。
- 人工验收：用户自行下发真机动作。待核对真实搜索是否达到 8～15 秒、四段耗时日志是否齐全、点击停止后前端是否立即显示“停止中”，以及运行命令 `STOPPED` ACK 是否先于返回燎原星火；未取得人工结果前不标记真机通过。

### 2026-08-04 养号启动搜索固定时序（仅代码，未发布）

- `target-live-entry.js` 在 `openDouyin()` 成功后固定等待 7000 毫秒，再调用养号快速搜索；`fast-target-search.js` 点击搜索入口后固定等待 3000 毫秒，再输入前端下发的目标关键词。
- 两条测试精确锁定 `open_douyin → wait:7000-7000 → search:关键词` 与 `click:entry → sleep:3000 → input:关键词` 顺序；养号相关 4 组定向测试和脚本语法检查通过。
- 本节点未修改后续直播栏、进房、OCR、商业直播或观看逻辑，未打包、未发布、未更新手机脚本。

### 2026-08-04 养号搜索提交与首个直播间动作（仅代码，未发布）

- 输入目标直播关键词后固定等待 3000 毫秒再提交；结果确认后立即点击顶部直播栏，固定等待 3000 毫秒后点击第一结果卡片。
- 首个直播间点击使用设备比例坐标 `36% × 35%`，绕开真机已证明返回 0 候选的旧直播入口识别。测试锁定完整事件顺序和 1080×2248 下的点击点 `(388, 786)`。
- 养号快速搜索、目标直播任务、注册表和命令桥接器定向测试通过；源文件低于 400 行。未打包、未发布、未更新手机脚本。

### 2026-08-04 养号直播间 OCR 与小黄车优先判断（仅代码，未发布）

- OCR 区域覆盖顶部直播间名称与主要画面，只以命令中的 `relatedTerms` 为匹配词；搜索关键词和无障碍文本不参与本步命中。
- `captureRegions()` 返回的同一截图用于底部评论框旁小黄车检测：扫描屏幕 `x=52%～62% / y=87%～94%` 的橙色像素簇。小黄车命中优先于相关名词，直接下滑。
- 同一搜索轮次内连续判断最多 4 个直播间，商业直播或相关词未命中时使用既有 `nextVideo()` 下滑；其 void 返回值按动作成功处理，不再误报 `NEXT_LIVE_FAILED`。
- 目标直播测试覆盖 OCR-only、搜索词不替代相关词、小黄车覆盖关键词、橙色图标区域和 void 下滑接口；养号相关 4 组定向测试通过，源文件 335 行。未打包、未发布、未更新手机脚本。

### 2026-08-04 取消养号点赞节点（已清理，未发布）

- 删除 `live-like-actions.js` 及测试，移除注册表预加载和目标直播间结果中的点赞计划；同步清除中断实施留下的后台确认命令契约与测试。
- 目标直播间命中后只返回 `TARGET_LIVE_ENTERED`，没有点赞计划、真实触控、批量执行或人工确认命令入口。
- 仓库搜索确认无养号点赞引用；养号 4 组 AutoJS 定向测试、API/types 6 项测试和相关脚本语法检查通过。目标入口 335 行、注册表 27 行。未打包、未发布、未更新手机脚本。

### 2026-08-04 养号点赞间隔策略（仅计算，未接线）

- 新增 34 行 `like-timing.js`，`nextDelay(completedDoubleTaps)` 在普通动作后返回 1200～5400 毫秒；完成次数为 3 的倍数时返回 5255～7565 毫秒长停顿，普通间隔硬下限为 1000 毫秒。
- 测试覆盖两类区间的最小值、最大值、0 次边界和每三次规则；既有养号 4 组定向回归保持通过。
- 当前没有点赞次数、坐标、触控或循环，没有接入注册表和目标直播任务。未打包、未发布、未更新手机脚本。

### 2026-08-04 养号点赞 dry-run 数量与坐标计划（未接线）

- 新增 68 行 `like-plan.js`，随机生成 7～17 个红框内唯一 XY 坐标，动作类型固定为 `double_tap`，不包含随机滑动或单击。
- 红框采用设备比例 `x=9%～96% / y=21%～66.9%`；每个动作附带 `like-timing.js` 生成的间隔类型和毫秒值。
- 测试覆盖 7/17 数量边界、17 个坐标唯一性、红框边界和每三次长停顿。模块无触控、线程或定时 API，未接入注册表与目标直播任务，未打包、未发布。

### 2026-08-04 养号前端评论词库配置（未实现发送）

- 养号表单新增 `commentLibrary` 标签输入，支持逗号、中文逗号和换行拆分，可自由增删；提交时 trim、过滤空项并去重。
- 共享契约限制最多 100 条、单条最多 100 字符，缺省为空数组；Web 命令构造将词库随目标直播配置下发。
- Web 7 项、API/types 6 项测试和完整 typecheck 通过，3026 页面返回 200；用户实页截图确认新增字段及页面布局正常。当前无评论条数、时序和发送动作，未打包、未发布。

## 2026-08-04 养号点赞后评论循环（离线完成）

- 目标直播间命中后，已把既有 7～17 次 `double_tap` 计划接入真实执行；仅在最后一次点赞完成后进入评论阶段。
- 管理后台新增 `commentCount`（0～20）；评论次数大于 0 时必须配置评论词库。每条评论独立随机选词，允许重复。
- 首条评论依次执行：点赞结束后等待 2345～4876ms、点击评论输入框后再等待 2345～4876ms、输入并发送；后续评论按 3634～5187ms 间隔循环输入并发送。
- 已通过养号点赞计划、点赞时序、目标直播间、注册器、评论循环单测，以及 API/Web typecheck；未打包、未发布、未进行真机操作。
## 2026-08-04 养号直播时长与下播收尾（离线完成）

- 管理后台新增两个必填分钟字段：单直播时长和直播养号总时长，范围均为 1～1440 分钟；命令契约和手机 payload 同步携带两个字段。
- 总时长从首次进入已验证直播间后开始计算。单直播到期时，若点赞或评论仍在执行则先完成当前互动，再下滑并重置下一直播间的单场计时；总时长到期时不再开启新直播间，但会完成当前互动后结束。
- 检测到当前直播间下播时，不再等待当前互动，随机等待 2345～5432ms 后直接下滑至下一直播间并重置单场计时。
- 新增会话时序离线测试；养号相关 Node 测试、共享契约测试、API 命令测试及 API/Web typecheck 均通过。未打包、未发布、未真机执行。
## 2026-08-04 养号任务完成后返回燎原星火（离线完成）

- 仅当直播养号会话整体完成后，调用既有最近任务清理器：打开最近任务、识别抖音卡片并左滑退出后台、识别并点击燎原星火卡片返回 Agent。
- 养号收尾不使用发布后的 30 秒冷却，也不依赖抖音发布进度判断；如果抖音卡片、燎原星火卡片或点击动作失败，任务明确返回 `WARMUP_FINAL_CLEANUP_FAILED`，避免错误报告为成功。
- 增加“会话完成后才收尾”和“返回 Agent 失败即失败”回归；既有抖音发布收尾 7 项测试与养号定向测试均通过。未打包、未发布、未真机执行。

## 2026-08-05 养号共享历史词库与离线 OCR 适配（代码完成，待 APK）

- 管理端将直播间相关名词和评论词库接入后端共享历史，保留 tags 手动输入；共享选项支持搜索点选、长文本提示和确认删除，任务至少一台设备发布成功后只保存一次。
- 新增租户隔离的 `account_warmup_vocabulary` 表与 GET/POST/DELETE 管理接口；数据永久保留，删除为软删除，同一规范文本再次使用恢复原 ID，数据库限制合法词库类型。
- AutoJs6 OCR 改为优先调用内置中文 ML Kit 的 `ocr.recognizeText()`；保留旧引擎兼容，APK 版本升至 `1.0.70/106`。该基础层修复必须重新打 APK，不能只发 biz-scripts。
- Web 13 项、API 8 项/34 assertions、四工作区 typecheck、OCR 与养号定向回归通过；本地真实浏览器/API/PostgreSQL完成搜索、点选、手输、删除及缓存更新验收。
- 本地数据库迁移 0029 已应用；未执行 APK 打包、覆盖安装或真机 OCR 识别，后续由用户完成打包并验收。

## 2026-08-20 抓取评论词 MVP N0-N9（代码完成，待用户真机验收）

- N0 范围冻结：仅处理“养号 > 抓取评论词”，不改视频养号、目标直播互动养号、远程唤醒、心跳、底座连接或数据库表结构。
- N1 进入直播间：独立 `live_comment_entry` 任务按打开抖音、搜索、输入关键词、直播页签、首个结果顺序执行；无结果最多重试一次；多设备各自执行，共用 `batchId` 与配置。
- N2 红框 OCR：进入候选房间后只读取用户标注的评论红框；人数 OCR 只读取右上角 `viewerBadge` 红框；直播已结束 OCR 与人数读取使用同一限定截图，结束状态优先。
- N3 评论采集：首屏加 5 次上滑共 6 页，每页 OCR 最多重试 3 次；上滑失败、OCR 能力缺失或六页全空均明确失败，不把空结果报成成功。
- N4 候选整理：手机端按评论正文规范化去重，保留用户名、页码、来源、设备和批次作用域；重复正文保留全部来源。
- N5 进度展示：前端逐设备展示阶段、阶段历史（最多 20 条）、结果、错误、更新时间和平台验证；单设备失败不影响其他设备。
- N6 任务收尾：采集后通过最近任务退出抖音并返回燎原星火；清理失败不会误报成功，已抓候选仍保留。
- N7 人工选择：候选默认不选，支持逐条选择、全选和取消；不自动写入词库。
- N8 用户确认入库：用户确认后复用现有 COMMENT 词库 API，按最多 100 条分批提交；部分批次失败可单独重试，按 API 唯一 ID 统计成功数。
- N9 多设备批次：原批次配置和设备列表可恢复；下发失败逐台保留并可用同一 `batchId` 重试，设备结果互不覆盖。
- 验收：Web 抓取评论词测试 `27/27`，API/共享契约定向测试 `29/29`，AutoJS 入口/采集/人数/桥接回归通过，Web typecheck 与生产构建通过，AutoJS 语法和 `git diff --check` 通过。
- 业务包：未发布的 `AgriVideoCollector-biz-scripts-1.3.21.20260820160500.zip` 已生成，SHA-256 `48847c934b6fa26558d973c28cc7cbf23433d731ecbf194fcf3da6a15ac71f0c`；55 个 ZIP 条目（54 个业务文件+manifest），无 BOM、无非 ASCII 路径、逐文件哈希一致，包含评论采集新增文件和限定 OCR 截图释放修复。
- 交付边界：业务包只覆盖 `features/` 与 `domain/`，不含 `app/account-warmup-command-bridge.js`。手动 STOP 部分候选保留逻辑已随 APK `1.0.92/128` 编入本地交付包，并已覆盖安装到本次连接的 MI 8。
- APK：`dist/apk/燎原星火-1.0.92-inrt.apk`，SHA-256 `3aeff1dfb2f6147c8e1c87c2c7e95994238b01e491310035bcb450a1bef6d557`。SHA 侧车、zipalign、v2/v3 签名、与 `1.0.91` 证书一致性、包名/版本、11 个关键源码逐文件哈希、OCR/终端 native library 和测试排除均校验通过；已使用保留数据方式从 `1.0.91/127` 升级设备 `ce3eb0bf`，首次安装时间保持不变，安装后 App 处于停止状态。未发布业务包、未启动 App、未做真机业务测试。
- 视频养号停止生命周期修复：待执行 RUN 在停止请求事务中原子终结为 IGNORED/STOPPED；移动端同批次停止信号跳过 RUN；Web 区分等待取消与过期停止请求。定向 Web 14/14、AutoJS 桥接回归通过。
- 视频养号页面终态恢复修复：TIMED_OUT 等终态 RUN 优先于过期 STOP，避免错误显示停止中并阻塞重新开始；新增 Web 回归测试。

## 2026-08-28 Agent 断联安全收口

- 状态：代码完成，待 APK 与真机验收。
- 范围：生命周期字段、停止心跳、旧命令事务收口、BASE 外部清理、Web 断联锁定。
- 验证：API 退出回执 `8/8`、断联契约 `5/5`、Web Agent 状态 `7/7`、AutoJS 生命周期 `3/3`、Android-base 收口与运行时 `15/15`；迁移 0041 已应用。
- 待办：连接设备后重新打包 APK，验证停止按钮、外部 Agent 清理、重启后无任务状态和前端按钮恢复。

## 2026-08-28 停止前取消活动 Agent 命令

- 状态：代码完成，待 APK 与真机验收。
- 变更：移动端新增活动命令取消接口；launcher 在停止脚本前调用，服务端事务化收口并复用 BASE `EXIT_AGENT_APP` 清理。
- 验证：断联定向测试 `4/4`、命令认领回归 `8/8`、AutoJS 生命周期 `6/6`、API 构建通过；未修改业务动作脚本。

## 2026-08-29 APK 手动停止回执链路收紧

- 状态：代码完成，待 APK 与真机验收。
- 任务身份：启动时保存 `commandId`/`batchId`/`featureKey`/`runId`，停止时使用同一身份提交手动停止回执；命令 ID 与批次 ID 同时存在时执行 AND 校验。
- 外部收口：手动停止成功后立即创建幂等 BASE `EXIT_AGENT_APP`，不依赖后续 STOPPED 心跳仍能发现活动 RUN；重复回执返回 `ACTIVE_TASK_NOT_FOUND_OR_TERMINAL`，不会重复终结或创建新收口命令。
- 兼容性：恢复空闲心跳视频任务回收；停止兜底接口支持任务身份，旧无身份调用继续使用设备级断联兼容路径。
- 验证：API 路由集成、断联契约、Web Agent 状态、视频状态与 AutoJS 定向测试通过；未改视频养号动作脚本，未执行 APK/ADB 真机验收。
- 文件门禁：`launcher.js`、`uploader.js`、`account-warmup-command-bridge.js` 为历史存量超 400 行文件，本节点仅做必要链路修改，未进行跨职责拆分；后续另行立项处理。

## 2026-08-29 视频养号运行态心跳修复

- 状态：代码完成，待重新构建 APK 与真机验收。
- 根因：养号桥接器在独立线程执行视频任务时，主循环只读取旧 `floatyControl`，错误上报 `idle + runId`；API 据此将实际执行中的 RUN 命令提前收口，前端停止按钮随之灰色。
- 处理：主循环状态判断纳入 `accountWarmupCommandBridge.getActive()`，活动养号线程只影响状态上报为 `running`，不改变旧业务运行开关，也未修改视频动作脚本。
- 验证：AutoJS 定向回归 `7/7`，入口脚本语法检查通过；已有被错误收口的历史命令不会自动恢复，需新 APK、新批次验证。

## 2026-08-29 视频养号运行态心跳修复 APK 交付

- APK `dist/apk/燎原星火-1.0.95-inrt.apk` 已重新构建，SHA-256 `36bd0f2d2c8509f53cd9a5dfea47ad9588410627613dec5e3fbb093e6d72daf9`。
- 已保留数据覆盖安装到 ADB 设备 `41cc82eb`，版本 `1.0.95/131`，安装成功；API/前端健康检查通过。
- 待用户用新批次确认：任务执行期间数据库保持 `RUNNING`，前端停止按钮可点击；停止后再验证手机退出抖音和前端状态收口。

## 2026-08-29 守护进程养号活动状态修复

- 状态：代码完成，待重新构建 APK 与真机验收。
- 变更：守护进程读取养号桥接器活动任务；活动任务不再被独立心跳报告为 `idle`，停止请求中的活动任务报告 `stopped`。
- 验证：守护进程、养号桥接、视频停止、心跳同步和主循环定向测试通过；未修改视频养号动作脚本。

## 2026-08-29 守护进程修复 APK 交付

- 状态：APK 已构建并安装，待新批次真机业务验收。
- APK：`dist/apk/燎原星火-1.0.95-inrt.apk`，SHA-256 `F8E1ACD105CD393C66931440752D4403E313E0ABEB1F37A964DCA81890B6BCDC`，设备 `41cc82eb` 覆盖安装成功。
- 服务：API `3012`、前端 `3024` 健康检查通过，无需重启。

## 2026-08-29 控制循环即时心跳状态修复

- 状态：代码完成，待重新构建 APK 与真机验收。
- 根因补充：控制循环的即时心跳状态判断未读取养号桥接器活动状态，在配置刷新路径仍会发送 `idle`。
- 处理：控制循环纳入桥接器活动任务判断，保持视频养号运行期间的 `running` 心跳；新增回归测试，未修改业务动作脚本。

## 2026-08-29 控制循环修复 APK 交付

- 状态：APK 已重新构建、覆盖安装并重启手机端脚本，待新批次真机验收。
- APK：`dist/apk/燎原星火-1.0.95-inrt.apk`，SHA-256 `9183F2D52225F770E26F32421D6B406F602AEA410DC58CBDA387B9973B903183`，设备 `41cc82eb` 安装成功。
- 服务：API `3012`、前端 `3024` 健康检查通过，无需重启。

## 2026-08-29 业务停止与 Agent 断联状态隔离

- 状态：API 代码完成，待新批次真机验收。
- 变更：心跳 `status=stopped` 不再触发 Agent 断联收口；仅 `agentLifecycleState=STOPPED` 或 `pollingEnabled=false` 触发 `EXIT_AGENT_APP`。
- 影响范围：仅修改 `account-data-platform/apps/api/src/services/mobile.service.ts` 的断联门禁并新增回归断言；远程唤醒和 APK 链路未修改。
- 验证：`agent-disconnect-reconciliation.test.ts` 定向测试 6/6 通过；全量类型检查存在既有 `externalAccountKey` 测试错误。
## 2026-08-29 APK 停止通知诊断日志

- 状态：代码完成，待 APK 构建和真机验证。
- 处理：停止接口增加每次请求的 attempt、HTTP statusCode、响应体摘要、errorCode、后台 success/stoppedCount 与异常 message；覆盖 `manual-stop` 和失败后的 `cancel-active`。
- 验证：`agent-lifecycle-reporter.test.js` 8/8 通过，`node --check core/uploader.js` 通过。

## 2026-08-29 APK 停止按钮线程修复

- 状态：已完成构建、安装和真机验收。
- 变更：停止按钮通过 `threads.start` 执行同步后台请求，避免 UI 主线程网络异常，同时保持任务状态更新先于 Agent/脚本停止。
- 验证：生命周期回归测试 9/9 通过；真机 `manual-stop` HTTP 200，数据库任务状态 `DONE`、业务结果 `STOPPED`。

## 2026-08-29 抓取评论词任务等待停止改造

- 状态：代码与定向回归完成，待重新打包 APK 真机验收。
- 变更：隔离评论任务进入指定直播间并确认后只等待停止，移除进入后的观看人数、换房、评论 OCR、评论滑动和自动收尾编排；前端控件未改。
- 停止：继续由 `ACCOUNT_WARMUP_STOP` 精确匹配批次和运行命令，执行线程中断、退出抖音、返回 Agent 与双命令终态回执。
- 验证：工作流 6/6、停止桥接器 11/11 通过；工作流文件 199 行。

## 2026-08-29 抓取评论词退出流程统一

- 状态：代码完成，APK 已重新构建并覆盖安装到设备 `41cc82eb`，待点击停止按钮进行退出动作真机验收。
- 变更：`features/new-comment/index.js` 默认使用视频养号同一 `createDouyinPostPublishCleanup` 清理器，退出动作和兜底策略统一。
- 验证：相关定向测试 29/29 通过；旧桥哈希保护测试存在既有基线不一致。

## 2026-08-30 抓取评论词流程最小化

- 状态：APK 端代码、构建和安装完成，待点击任务进行真机验收。
- 变更：仅保留打开抖音、搜索关键词、切换直播结果、进入并确认第一个直播间；删除平台验证扫描、搜索重试及进入后的人数检测、换房、评论 OCR、评论滑动和自动抓取流程。
- 约束：前端文件和现有停止协议未修改；进入直播间后继续等待 `ACCOUNT_WARMUP_STOP`，按统一清理流程收口为 `DONE/STOPPED`。
- 验证：`new-comment-workflow.test.js` 6/6 通过；APK 构建成功并安装到 `41cc82eb`，版本 `1.0.95`、versionCode `131`。

## 2026-08-30 修复后台同步阻塞命令下发

- 状态：代码、定向回归、APK 构建与安装完成，待点击任务进行真机验证。
- 变更：后台配置同步不再依赖发布模块预加载；发布命令改为收到后按需预加载，避免预加载卡住时评论词任务长期等待下发。
- 验证：Agent 注册恢复测试、发布预加载恢复测试、控制循环测试通过。

## 2026-08-30 取消抓取评论词进入结果失败判定

- 状态：代码、定向回归、APK 构建与安装完成，待点击任务进行真机验证。
- 变更：`openFirstLive` 只执行进入直播间动作，不再根据返回值判定失败；移除 `isLiveRoom` 二次确认，动作后直接等待停止。
- 验证：`new-comment-workflow.test.js` 6/6、`new-comment-command-bridge.test.js` 11/11 通过。

## 2026-08-30 修复直播入口点击后误回退

- 状态：APK 已重新构建并安装到设备 `41cc82eb`。
- 根因：抖音适配器点击直播入口后仍执行直播间识别，识别不到时自动返回搜索结果页；工作流却继续回执已进入。
- 处理：抓取评论词使用专用点击参数，跳过直播间识别和失败回退，仅执行点击后等待停止。
- 2026-08-30 候选词待入库与确认入库：新增 `live_comment_candidates` 表，设备回执按批次和规范化正文幂等保存候选及设备/房间/轮次/用户来源；新增管理端查询与确认接口，确认只写入勾选项到 COMMENT 词库，未勾选保持 `PENDING`，重复确认幂等；Web 独立候选组件展示待入库/已入库状态。API 2/2、Web 27/27 通过，迁移 `0042_live_comment_candidates` 已应用。
