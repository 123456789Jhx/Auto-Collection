# 代码审查风险记录

更新时间：2026-07-16

本文只记录代码审查的简要结论，按日期倒序维护；同一天的记录合并到一个日期块内，并按风险等级分层。详细问题、卡点、修复过程和验证结果维护到 `卡点与问题记录.md`、`项目记录.md` 或对应提交说明中。

## 2026-07-16

| 风险等级 | 审查结果 | 处理结果 |
|---|---|---|
| P2 | APK 图标入口 `launcher.js` 如果不清理重复启动页实例，重复点击 App 或系统恢复 Activity 时可能叠出空白 AutoJs6 壳层，表现为黑屏但按返回可恢复。 | 已在 `ui.layout()` 前清理其它 `launcher.js` engine，并补源码级回归断言；不停止 `main.js` 和 `watchdog.js`，避免干扰正在执行的任务。 |
| P1 | 后台如果继续把内部任务类型 `commerce_card_live_comment` 展示成“商品卡直播评论/商品卡组合任务”，会和页面实际的 `商品卡养号`、`目标直播评论` 两个功能口径冲突，导致日志和调度误读。 | 已在 Web 展示层和 API 提示层统一映射业务名；手机端协议和历史数据字段保持不变，日志中心按 phase 区分商品卡养号与目标直播评论。 |
| P1 | 手机端正式包如果从本地过期 `.env.production` 注入注册密钥，会导致 APK 能启动但注册后台返回 403，表现为心跳中断和版本检查签名失败；该问题不能通过后台发布记录判断出来。 | `1.0.56` 改为使用服务器生产配置重新打包并做哈希一致性校验，仍不输出密钥明文；`1.0.55` 作为坏包不再用于真机测试。 |
| P1 | 热更新 ZIP 如果包含显式目录项，旧版解压器可能在目录项和后续文件写入之间出现 `ENOTDIR` 路径冲突，导致旧版本无法自动更新到新包。 | `package-autojs.ps1` 改为只写文件项，`uploader.js` 增强目录项和路径冲突兜底；已校验 `1.0.56` ZIP 目录项数量为 0，并发布 stable Agent。 |

## 2026-07-15

| 风险等级 | 审查结果 | 处理结果 |
|---|---|---|
| P1 | 商品详情页推荐区如果仍按全屏关键词节点反推商品卡，容易滑过“你可能还会喜欢/推荐”商品列表，或误点到底部购买动作栏、当前商品信息块，导致 15 分钟商品卡链路无法闭环。 | 已改为先识别推荐商品列表区域，再只在该区域内做关键词匹配和商品卡容器候选提升；推荐区识别按标题、推荐 tab、商品卡结构兜底分层执行，并排除进店/客服/购物车/领券购买等底部动作栏。 |
| P1 | 商品卡养号每轮结束后的直播门禁如果把配置缺失、入口异常、暂停停止等都当作“目标直播间未命中”，会错误消耗三轮养号次数并把异常中止显示成养号失败。 | 已新增 `isV2TargetGateBusinessMiss()` 分类，只允许 `target_live_room_not_found` 进入下一轮；其他原因按异常或控制中止失败收尾，并补充 `target_keyword_empty` 回归测试。 |
| P1 | 手机端脚本从技术分层目录调整为 `features/*` 与 `platforms/douyin/*` 后，如果入口、测试源码断言或 AutoX.js `files.join(config.runtime.scriptDir, ...)` 路径未同步，可能导致主脚本启动、直播评论任务或商品卡任务运行时加载失败。 | 已同步 `main.module.js`、`p0-live-readonly.js`、抖音适配器内部依赖和现有测试路径；移动前后的业务函数名保持不变，本次只调整目录和 require 路径，并通过 `node --check`、商品卡 runner、直播评论 runner、直播卡几何和 control-loop 回归验证。 |
| P1 | 商品卡浏览平台动作从 `douyin.js` 抽到独立模块后，如果 AutoX.js 全局 API、上下文函数或源码断言遗漏，可能导致商品卡任务运行时无法点击、无法恢复商城搜索或测试误判。 | 已通过 `createDouyinCommerceCardBrowser` 显式注入抖音基础能力，`douyin.js` 保留 `openCommerceCardSearch`、`browseCommerceCards`、`openMatchingCommerceLiveFromCards` 懒加载包装；测试同步检查新模块，语法、商品卡 runner 和 control-loop 回归均通过。 |
| P1 | 手机端脚本开始拆分 `douyin.js` 商品卡识别逻辑后，若 CommonJS 路径、纯函数迁移或测试源码断言未同步，可能导致 AutoX.js 运行时加载失败或商品卡信号规则漂移。 | 已只抽取无 AutoX.js 副作用的纯判断和签名函数到 `domain/commerce-card`，`douyin.js` 保留原函数名作为兼容包装；同步更新现有商品卡测试读取新模块规则，并通过 `node --check`、商品卡 runner 和 control-loop 回归。 |
| P1 | 商品卡养号如果首卡和详情页推荐卡仍按关键词小节点点击，真机上可能滑过“你可能想看”或点到不可跳转文本，导致 15 分钟商品卡循环无法闭环。 | 已把搜索结果首卡和详情页推荐卡都提升到商品卡容器候选；补充“你可能想看/猜你喜欢/推荐商品”等内置推荐区信号和标题型商品卡兜底，并继续保留点击后页面签名校验。 |
| P1 | 商品卡养号详情页推荐区如果只按关键词小节点点击，可能漏识别“你可能想看”后的夏橙商品卡，或点到不可跳转的标题碎片，导致详情页链路无法继续。 | 已把关键词节点提升为父级商品卡容器候选，按商品信号、推荐区信号和 bounds 尺寸打分；仍保留点击后详情页签名校验和同 bounds 跳过机制，防止误计数和重复点击。 |

## 2026-07-13

| 风险等级 | 审查结果 | 处理结果 |
|---|---|---|
| P1 | 商品卡 V2 运行门禁曾依赖 migration 插入默认行，但生产使用 `db:push` 同步结构时不会补数据，导致按钮请求到达后台后因 `FEATURE_CONTROL_MISSING` 返回 409，手机端没有命令可拉。 | 已在 API 仓储读取和更新门禁前自动补齐默认控制项；`commerce_card_workflow_v2` 对 `1.0.41+` 且具备 V2 capability 的设备默认可启动，`commerce_card_real_comment` 仍默认关闭。 |
| P1 | 历史旧协议留下的 `ACKED` assignment 不在前端活跃状态列表中，但仍被数据库唯一索引视为非终态运行，可能在点击启动时继续触发 409 或唯一约束冲突。 | 新建 START assignment 前自动把同设备遗留 `ACKED` 收口为 `SUPERSEDED`，保留 `terminalReason=legacy_acked_superseded_by_new_start` 便于后续排查。 |
| P1 | 将 `activationReady` 从阶段 A 保护状态改为代码级就绪后，如果同时自动开启真实评论，会放大真实互动风险。 | 只默认开启商品卡 V2 工作流启动；真实评论保持全局关闭，并继续受目标 `executeEnabled`、短时许可、动作审计和 capability 检查约束。 |

## 2026-07-12

| 风险等级 | 审查结果 | 处理结果 |
|---|---|---|
| P1 | 商品卡 V2 启动表单把目标、账号和真实评论审批暴露在启动动作里，且手机端缺少审批 ID 就跳过发送，导致“后台配置目标后直接启动”的业务口径无法落地。 | 已移除启动表单，V2 START 由后端按设备绑定自动选择目标；工作台配置弹窗负责维护商品卡 V2 目标和绑定；评论预约取消审批强依赖，但仍保留目标 `executeEnabled`、全局真实发送开关、动作审计和旧审批路径校验。 |
| P1 | 商品卡浏览遇到直播卡时固定只停留约 1.8 秒，直播养号2扫描到候选上限后即使未累计看满也会上报完成，和原稿“进去看直播”“累计 70-100 分钟”的验收口径不一致。 | 已改为商品卡直播卡按商品详情停留时长观看；直播养号2固定累计观看目标，未看满按 `live_nurture_watch_incomplete` 失败收口，并补充回归测试；版本提升到 `1.0.40/76`。 |
| P1 | 商品卡二阶段继续复用 `commerce_card_live_comment` 任务类型，如果只依赖目标中心默认 `enabledStages`，前端无法明确表达“商品卡养号”和“目标直播间评论”两种人工启动意图，容易造成按钮语义与手机实际阶段不一致。 | 已在任务调度页拆分两个启动按钮，并通过 `payload.commerceCardStartMode` 在后端创建 assignment 时覆盖并重新校验 `runtimeConfig.enabledStages`；旧调用未传模式时保持兼容。 |
| P1 | `1.0.38/74` APK 瘦身时移除了 `libjackpal-termexec2.so` 和 `libjackpal-androidterm5.so`，但 AutoJs6 启动阶段会初始化 `Shell`，导致手机端在业务任务执行前直接 `UnsatisfiedLinkError` 崩溃。 | 已将 `removeTerminalNative` 改回关闭，并在打包脚本中禁止该优化项排除 `libjackpal-*`；版本提升到 `1.0.39/75`，重新打包后已校验 APK 内包含两个 jackpal native library。 |
| P1 | `1.0.37/73` 的 V2 手机 runner 仍把商品卡浏览完成当作商品卡阶段成功，目标直播评论阶段继续走搜索入口，和第二版“商品卡后进入直播板块目标门禁”的业务口径不一致。 | 已改为商品卡每轮浏览后重启进入直播板块扫描目标，目标命中后复用同一房间执行评论；目标未命中按 `product_target_not_found` 或 `target_live_room_not_found` 失败收口，并补 `commerce-card-live-runner.test.js` 回归。 |
| P1 | 阶段B开始落地 assignment/command 一对多后，如果只改服务逻辑而不补数据库唯一约束、事件表和旧命令兼容，后续 `PAUSE/RESUME/STOP` 仍可能丢失运行归属或被历史 commandId 回写覆盖。 | 已新增 `0021_assignment_command_timeline.sql`、schema 字段和 `device_task_assignment_events`，控制命令追加 `assignmentId + commandSequence + idempotencyKey`；回执优先按 command 归属 assignment 回写，并保留旧 commandId fallback。V2 与真实评论开关继续关闭，手机端 checkpoint v2 和评论幂等尚未实施。 |
| P0 | 第二版要求 PAUSE、RESUME、STOP 复用同一 assignment，但当前 `device_task_assignments` 只关联一个 command，现有编排还会在控制操作前废弃旧 assignment 并新建运行，无法支撑稳定暂停恢复。 | 已在两份方案中把 assignment 定义为唯一运行实例，要求 `mobile_commands` 通过 assignmentId 和 commandSequence 形成一对多历史，并补充 stateVersion、命令幂等、每设备单一非终态运行约束和旧状态迁移；代码尚未实施。 |
| P0 | 真实评论原方案只有配置布尔值和追加式动作上报，缺少可撤销审批、服务端原子预占、动作状态更新、崩溃查询和 unknown 人工处理，无法证明物理发送最多一次。 | 已在方案中拆出运行时急停和独立执行审批，定义 reserve/update/query/resolve 接口、短时发送许可、稳定 roomKey、comment slot、账号校验、跨运行额度和 BLOCKED 人工恢复；真实评论开关在实现与验收前必须保持关闭。 |
| P1 | effectiveWorkflow 示例缺少完整目标门禁字段，配置 hash 未覆盖目标/别名/绑定聚合变化，默认 3600 秒 assignment 有效期也无法覆盖 240 分钟流程；V1/V2 压制条件存在不同表述。 | 已补全 expectedAccount、required/forbidden、相似度、snapshotHash 和聚合 revision/hash 规则，增加显式过期时间校验、capability TTL 和 V1/V2 唯一选择表；V2 在数据库、接口、手机状态机和真机验收完成前保持关闭。 |
| P1 | 阶段 A 已落地后台配置、预览和门禁，但手机端 assignment 快照、checkpoint v2、评论幂等和真机验收尚未实现；如果误开启 V2 或真实评论，会把“配置准备”误当成可执行闭环。 | `feature_rollout_controls` 默认关闭 `commerce_card_workflow_v2` 和 `commerce_card_real_comment`，服务端 `activationReady=false` 阻止开启；移动端 V2 配置下发时标记 `executionEligible=false`，预览页也显示不可执行，后续必须完成运行闭环后再调整门禁。 |

## 2026-07-10

| 风险等级 | 审查结果 | 处理结果 |
|---|---|---|
| P1 | APK 瘦身移除 OpenCV、条码扫描、MediaInfo、OpenCC 等 native/assets 后，AutoJs6 某些图片处理、OCR fallback 或脚本运行路径可能在真机运行时暴露缺库错误。 | 已将每类裁剪项做成 `project.json` 中可单独关闭的优化开关，R8/minify 默认保持关闭；临时审计包仅用于体积验证，正式发布前必须用真实密钥重新打包并完成真机回归。 |
| P2 | `project.json` 已有 `optimization.removeOpenCv=true` 但原 APK 脚本未读取，配置和实际打包结果不一致，后续新增优化项也可能被误认为已生效。 | `package-autojs-apk.ps1` 和 `package-autojs.ps1` 已统一读取 `optimization`，并在 APK 打包日志输出 native 跳过数量、assets 删除数量和体积分布，便于以后核对配置是否真正生效。 |

## 2026-07-09

| 风险等级 | 审查结果 | 处理结果 |
|---|---|---|
| P1 | 商品卡直播评论生产执行失败时，手机 runner 在 `commerce_card_live_config` 阶段停止，根因是设备级 `commerceCardLiveComment.enabled=false`；同时生产库缺少 `live_targets` 表，导致 `/mobile/tasks/current` 一度 500，手机无法刷新配置。 | 已在生产库执行只创建缺失表的 `0019_live_target_configs.sql`，当前任务接口恢复 200，手机运行日志已出现“后台配置已应用到当前脚本”；真实执行仍需运营显式开启商品卡开关和发送审批。 |
| P1 | 手机端 `refreshRuntimeConfig()` 清洗商品卡配置时丢弃 `targetRoom`，即使后台填写“鲜橙四季秭归”，后续商品卡 runner 也只能按商品关键词匹配，不能稳定执行“刷到我们直播间”为止。 | 已补 `targetRoom` 清洗保留逻辑和 `control-loop.test.js` 回归；手机端版本提升到 `1.0.36/72`，需重新打包安装后生效。 |

## 2026-07-08

| 风险等级 | 审查结果 | 处理结果 |
|---|---|---|
| P1 | 商品卡直播评论第一版曾通过 `live` 任务和 `commerceCardLiveComment.enabled` 触发，容易和普通直播采集混淆，也会让后台“直播评论”文案误导运营。 | 已新增独立任务类型 `commerce_card_live_comment`，后台任务调度单独下发；普通 `live` 永远走普通直播 phase，`live_comment` 保留搜索目标直播间评论。 |
| P1 | 商品卡 runner 的 checkpoint 和运行日志曾把任务类型写成 `live`，后续对账会把商品卡执行误判为普通直播。 | 已将商品卡 runner 的 taskType、checkpoint、日志阶段统一改为 `commerce_card_live_comment` / `commerce_card_live_*`。 |
| P1 | 生产 Web 镜像最终阶段依赖 `nginx:1.27-alpine`，服务端 Docker 镜像源返回 401 时会阻断部署，导致线上后台继续停留在旧版本。 | 已改为 Bun runtime 提供静态资源和 `/api`、`/health`、`/ready` 代理，并更新下载目录挂载；已补 `deploy/web-server.test.ts` 覆盖生产入口行为；已直连部署到生产并通过健康检查与前端资产验证。 |
| P1 | 任务调度页缺少暂停/恢复入口，运营只能停止任务，无法区分“临时暂停”和“结束任务”。 | 已在任务调度列表和详情中新增 `PAUSE`、`RESUME` 控制入口，并用当前安排任务/手机实际任务确定控制目标。 |
| P1 | 手机端如果不认识后台明确下发的任务类型，旧逻辑可能回退到 `video`，造成商品卡任务跑成旧刷视频流程。 | 已增加明确任务类型防回退保护：不支持的显式任务类型回执 `FAILED/unsupported_explicit_task_type`，不启动视频；后台增加任务不一致提示。 |

## 2026-07-07

| 风险等级 | 审查结果 | 处理结果 |
|---|---|---|
| P0 | `840959a` 将直播评论从 `phaseRunner.runPhase("live", ...)` 拆到独立 `live-comment-runner` 后，没有继承原 `phase-runner` 的持续 `writeHeartbeat()` 契约；直播评论搜索、进房、持续采样超过 3 分钟时，后台会按心跳超时临时判离线。 | 已新增 `agent-heartbeat-daemon.js`，Agent 启动后通过独立后台线程持续上报心跳；`main.module.js` 创建守护实例，`collector-app` 在权限检查前启动；已补 `agent-heartbeat-daemon.test.js` 覆盖业务线程不让出时仍持续心跳。 |
| P1 | 任务完成或失败收尾后，`collector-app.finishTask()` 写入 `paused=true`，会让恢复在线后的设备继续显示暂停或不可用，干扰离线问题判断。 | 已新增任务收尾状态补丁，完成或失败后写入 `running=false`、`paused=false` 并回到 `idle`；已补 `collector-app-finish-state.test.js` 覆盖成功和失败收尾；版本提升到 `1.0.28/64`。 |
| P1 | 当前改动尚未打包安装到三台手机，生产仍运行旧包时不会体现本次修复。 | 本轮按要求只改代码；后续通过安全来源注入注册密钥后重新打包、签名校验并做三机直播评论 10 分钟以上回归。 |
| P1 | `7d6d268` 新增商品卡直播评论后，`commerceCardLiveComment.enabled` 在手机本地默认配置中为 `true`，但后台 API 和手机 `refreshRuntimeConfig()` 尚未下发或刷新该配置；普通 `live` 任务会被商品卡 runner 抢占，且线上无法通过后台关闭或调整关键词。 | 已修复：商品卡配置纳入 `p3ExtensionsConfig.commerceCardLiveComment`，后台移动端任务接口单独返回该配置，手机刷新配置时应用；手机本地默认关闭。2026-07-08 进一步拆成独立 `commerce_card_live_comment` 任务，普通 `live` 不再触发商品卡 runner。 |
| P1 | 商品卡直播评论直接调用 `douyin.sendLiveComment()` 并用 `commentMode: "agri_chatbot"` 绕过话术池校验；当前缺少独立的真实发送总开关、人工确认字段和动作记录上传。 | 已修复：新增 `executeEnabled` 和 `manualExecutionApproved` 双开关，未审批时只写 skipped 审计记录不发送；商品卡发送使用 `commentMode: "commerce_card_live"`，发送、跳过、失败统一写入直播评论动作日志并上传。 |
| P2 | 商品卡入口匹配先在整屏文本上判断商品关键词和直播信号，再点击任意直播入口，可能出现关键词和直播入口不属于同一卡片的误点；`maxCommentsPerRoom` 配置也未真正按直播间去重。 | 已部分收口：runner 按任务内直播间文本指纹记录已评论直播间，并按 `maxCommentsPerRoom` 控制发送次数；卡片容器级标题和直播标识聚合仍需真机 UI 树回归后继续加强。 |

## 2026-07-03

| 风险等级 | 审查结果 | 处理结果 |
|---|---|---|
| P0 | 1. STOP 指令可能被后续 START 覆盖。<br>2. 进入目标直播间后 0 采样退出。<br>3. 独立 runner 只采样一轮，导致评论 1 次后立即退出直播间。<br>4. 直播评论状态分散，没有单一状态机。 | 1. STOP 优先级已修复，待真机回归。<br>2. 目标直播间进房后清理刷新标记已修复，待真机回归。<br>3. 持续采样与话术池模式已修复，待真机回归。<br>4. 状态机统一待处理。 |
| P1 | 1. stopReason 处理不够严格。<br>2. 任务完成条件不够严谨。<br>3. 真机三机回归仍未闭环。 | 后续结合直播评论状态机统一继续处理；真机回归安装新包后执行。 |

## 2026-07-16 追加审查

| 风险等级 | 审查结果 | 处理结果 |
|---|---|---|
| P1 | 商品卡浏览器从 `adapter.js` 拆出后，`createDouyinCommerceCardBrowser` 依赖的上下文方法没有统一契约校验；`isSearchResultPageText` 漏接会在任务启动后直接触发 `ReferenceError`，导致主循环进入 `paused/error`。 | 已将 `isSearchResultPageText` 绑定到现有 `isSearchResultPage`，并在既有商品卡 runner 测试中补充上下文装配断言；后续继续拆分平台适配器时，需要同步补齐“消费者 context 方法必须由 provider 装配”的测试。 |

## 2026-07-16 追加审查

| 风险等级 | 审查结果 | 处理结果 |
|---|---|---|
| P0 | STOP 控制命令依赖手机端 ACK 才把 assignment 置为终态；手机端卸载、离线或注册失败时，后台会一直保持 `RUNNING + STOP/PENDING`，阻塞新任务。 | 已改为管理员 STOP 立即将 assignment 置为 `CANCELLED`，同时保留 STOP 命令供在线手机退出；需部署后复测当前卡住任务。 |
| P1 | 打包脚本只读取环境变量，导致本机打包经常因缺少 `MOBILE_REGISTRATION_SECRET` 被阻断，或误用占位值。 | 已增加本机已打包配置 fallback，不把密钥写进代码和提交。 |
## 2026-07-16 部署后复核

| 风险等级 | 审查结果 | 处理结果 |
|---|---|---|
| P0 | 离线或已卸载手机无法 ACK STOP 时，后台旧 assignment 可能长期保持 `RUNNING + STOP/PENDING`，阻塞新任务。 | 已部署提交 `857ff0f` 到服务器并重启 `api`；公网 `/health`、`/ready` 正常；旧 assignment `e29134db-655e-4edf-8307-b8d34906e9af` 重新发 STOP 后已从 `RUNNING` 变为 `CANCELLED`，该风险在当前生产后台已闭环。 |
