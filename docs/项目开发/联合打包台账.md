# 联合打包台账

## 批次 2026-09-13-xiaomi14-recents-horizontal（已构建并安装）

- 本批次仅纳入当前对话已完成的小米14最近任务横向卡片退出修复；按用户明确授权直接执行，未纳入其他对话未确认改动。
- 构建执行者：当前对话。Build ID：`AGRI-PRODUCTION-20260913203642`；APK：`dist/apk/xiaomi14-recents-horizontal-20260913/燎原星火-1.0.96-inrt.apk`；SHA256：`e04fbcc71aa227dcde168bc26247ba45e255b8aec967cd23427b505f3e3c39b9`。
- 校验：v2/v3 签名通过；APK 内含 `core/accessibility.js`、`device-profiles.js`、发布清理和直播恢复脚本；业务基线 `apkBuildId` 与 Build ID 对齐。
- 安装：通过 ADB 5037 覆盖安装到 `d950a769`，返回 `Success`；设备版本 `1.0.96/132`，安装更新时间 `2026-09-13 20:42:47`。未启动业务任务，待人工验收退出动作。

## 批次 2026-09-13-xiaomi14-accelerated-exit（已构建，越过三方确认）

- 发起原因：用户要求将小米14最近任务退出的连续加速双贝塞尔修复构建进 APK。
- 候选纳入文件：`mobile-agent/autojs/core/accessibility.js`、`mobile-agent/autojs/features/publish-video/douyin-post-publish-cleanup.js`、`mobile-agent/autojs/features/account-warmup/target-live-entry.js` 及对应定向测试和项目记录；保留既有小米14起点/终点矩形随机取点配置。
- 预期排除：工作区中其他未完成或未确认的更新中心、评论采集、业务脚本加载和服务端改动；临时截图、日志及运行时产物不作为候选输入。
- 用户明确授权本次越过联合打包台账直接构建；构建执行者为当前对话，未安装设备、未启动任务、未发布业务包。
- 构建结果：`dist/apk/xiaomi14-accelerated-exit-20260913/燎原星火-1.0.96-inrt.apk`，Build ID `AGRI-PRODUCTION-20260913163104`，版本 `1.0.96/132`，SHA256 `38e637e5776cd00f9b3056edea50915a4f4db5a58f4a453bc590cf445bfc8ee0`。
- 输入说明：本次按用户要求越过台账，直接从当前工作区同步构建，未建立物理隔离暂存区；因此 APK 可能包含工作区中其他已存在的手机端改动，不能将“预期排除”理解为已完成的文件级剔除。
- 包内校验：存在 `assets/project/core/accessibility.js`、`features/publish-video/douyin-post-publish-cleanup.js`、`features/account-warmup/target-live-entry.js`；v2/v3 签名验证通过；业务基线 `燎原星火-1.0.96-inrt.apk.biz-script-baseline.json` 与 Build ID 对齐。
- 验证：无障碍 29/29、发布清理 14/14、视频养号相关 14/14、目标直播回归及三份脚本语法检查通过；全量测试中与本批次无关的历史评论/业务脚本失败保持原样记录。

## 批次 2026-09-13-xiaomi14-accelerated-exit-3p5（已构建，越过三方确认）

- 用户明确授权本次直接构建；纳入 `1500ms` 视频入口轮询上限和小米14 `3.5x` 连续加速双贝塞尔参数。
- 产物：`dist/apk/xiaomi14-accelerated-exit-3p5-20260913/燎原星火-1.0.96-inrt.apk`；Build ID `AGRI-PRODUCTION-20260913165848`；版本 `1.0.96/132`；SHA256 `3aca82cb268f90bbe06f893532becbea1fc7859cb287d7d7368616bfa5fe5936`。
- 包内脚本与业务基线校验通过，v2/v3 签名通过；未安装设备、未启动任务、未发布业务包。打包脚本同步当前工作区，未做物理隔离快照。

## 批次 2026-09-13-xiaomi14-bezier（当前对话单独执行）

- 用户本次明确要求不等待另外两个对话，仅纳入当前对话已完成的小米14退出动作改动，并授权安装到 `d950a769`；未纳入其他未确认工作区改动的单独筛选快照。
- 构建执行者：当前对话；Build ID：`AGRI-PRODUCTION-20260913145248`；版本：`1.0.96/132`；环境：production；API：`https://qk-api.dafengchan.top/api/v1`。
- 产物：`dist/apk/xiaomi14-bezier-20260913/燎原星火-1.0.96-inrt.apk`；SHA256：`e38f9c18c3f313c8c91c01ee412986e82355464fc28afa964be990bad8af491a`。
- 包内核验：v2/v3 签名通过；包内包含 `assets/project/device-profiles.js`、`features/account-warmup/target-live-entry.js` 和 `features/publish-video/douyin-post-publish-cleanup.js`；设备端拉回 APK 哈希与本地产物一致。
- 安装：通过 ADB 5037 覆盖安装到 `d950a769`，返回 `Success`；设备读取版本 `1.0.96/132`。未自动启动业务任务，等待人工真机验收双贝塞尔退出动作。

## 协作约定

2026-09-10 用户确认：三个对话分别开发，打包前统一汇总和确认，再指定一个对话集中打包。具体规则见仓库根目录 `AGENTS.md` 的“多对话联合打包规则”。

| 对话 | 对话 ID | 规则同步状态 |
|---|---|---|
| 群控02 | `01a06f57-9775-7fc2-b68c-51eff0f2930c` | 已发送规则通知，待核实明确回执 |
| 群控03-直播互动养号 | `01a06f59-872b-7860-b075-404cfc1a36fb` | 已发送规则通知，待核实明确回执 |
| 群控04-刷视频 | `01a0857a-f7f3-7470-9eb2-69db1d9b0171` | 已知悉，本次规则同步协调者 |

规则同步不代表三方已批准打包；每一批次必须重新协商。对话更名或接手时同步更新对应关系。

## 当前状态

- 尚未发起联合打包批次，未指定打包执行者，没有安装或发布授权。
- 当前工作区存在多个模块的未提交修改。它们尚未完成三方盘点，不能直接视为已批准的打包输入。
- 已确认的风险：设备新版 APK 启动后下载旧 `9.8.56232` 业务包，视频养号的新动作被覆盖；评论词加载也存在覆盖优先和基座相对依赖问题。2026-09-10 统一加载修复已完成源码与定向测试，尚未部署，不得标记设备已修复。
- 当前任务仅完成源码/测试/文档，不构建 APK、不改写打包暂存区、不操作手机任务、不发布业务包。
- 用户追加指定群控04接手群控02只读排查的更新中心修复。当前基线源码作为共同基础，更新中心发布流程和状态链待补齐；本批输入仍未冻结，不得以基础修复完成视为已可打包。

## 群控04 待纳入修改（不是已批准批次）

- 基座：`main.module.js`、`app/biz-script-updater.js`，新增 `app/biz-script-policy.js`、`app/biz-script-runtime.js`；`core/accessibility.js` 中曲线能力缺失明确失败，与既有连续保持改动共同纳入，不能相互覆盖。
- 业务：`features/account-warmup/video-warmup-foundation.js` 保留矩形随机点，取消直线兜底并增加动作标识/参数日志；不负责删除群控03维护的旧入口，也不改变群控02评论采集规则。
- 工具/服务：新增 `scripts/biz-script-manifest.ps1`，更新 `package-autojs-apk.ps1`、`bundle-autojs.ps1`、API `biz-script-release.service.ts` 及对应测试；最终 APK 核验后导出配套业务基线，后续 bundle 必须使用匹配基线。
- 验证：手机端 106/106、helper fixture 7/7、API mock 11/11；两个独立审查已通过。扩展发布链 2 项既有失败、全局类型错误、旧真实构建测试不安全且契约过时均见问题记录。
- 未验收：真实 APK 构建与签名/内置基线校验、AutoJS Java SHA/require、watchdog 恢复、两台业务设备运行来源与视频动作。先确定目标设备 APK 已升级兼容基座，才能考虑新业务包发布，禁止向新旧混合基座直接全量下发。
- 三方批次确认：仍待发起；本清单没有固定输入快照、唯一执行者或安装授权，不允许直接据此开打包。

### 群控04 更新中心本轮待纳入修改（仍未批准打包）

- Web：`UpdateCenterPage.tsx`、`components/update-center/*`、目录读取与业务更新 API 客户端；本机目录完整包预览、设备试运行/推广/撤回、APK 页签保留。
- API/数据库：业务预览/归档/审计服务、设备实际状态与范围下发、`0044` 迁移 artifact、`fflate`/`acorn` 依赖；旧业务全局发布入口隔离，APK 通道未改变。
- 手机：`heartbeat.js` 增加 `bizScriptRuntime` 和 `pendingBizScriptsVersion`；`biz-script-runtime.js` 早期坏覆盖层记录拒绝指纹。上述手机改动必须进入下一次三方联合 APK，不能只部署前端宣称设备可用。
- 本轮验证与未验收：后端 38 项、Web 19 项、手机心跳/运行时定向通过，API/Web 类型检查通过；真实 DB 迁移、服务部署、业务下载、APK 构建安装和设备试运行均未执行。输入快照、三方回执和唯一执行者仍待后续批次确认。

## 批次 2026-09-10-01（收集中）

- 批次编号与发起时间：`2026-09-10-01`，2026-09-10 13:45（Asia/Shanghai）。
- 协调对话、唯一打包执行者：群控04-刷视频负责协调；执行者尚未指定。
- 状态：收集中；尚未构建、安装或发布。
- 候选输入：加载与运行时基座（`main.module.js`、`app/biz-script-updater.js`、`app/biz-script-policy.js`、`app/biz-script-runtime.js`、`app/heartbeat.js`）；曲线/权限兼容（`core/accessibility.js`、`core/permission.js`）；视频养号与当前评论入口；群控03旧 `live_comment_entry` 删除；业务包与 APK 构建脚本；更新中心 API、数据库契约和 Web 改动作为伴随源码部署项，是否纳入仍待三方核对。
- 明确排除：临时截图、日志、`.ua`/`.claude` 产物及未获授权的真实数据库迁移、设备安装、业务下发和远程发布。
- 固定输入快照：尚未建立，等待三方逐项报告并处理共享文件冲突。
- 三方沟通：已向群控02和群控03发送同一份候选清单，要求报告实际修改、依赖、验证、冲突、纳入/排除及验收设备范围，并给出本批次“同意/不同意”回执。两方此前回执均为不同意，当前等待按本清单重新确认。
- 安装/发布授权：未授权，暂不执行。

## 每批次流程

1. 发起对话新建批次记录，联系另外两个对话，说明准备打包的范围。
2. 三方分别报告修改清单、完成情况、依赖、测试、风险和纳入意见；由本批次协调者集中写入台账，避免同时改写同一记录。
3. 处理共享文件冲突和未完成内容，确定固定输入快照。清单同时覆盖基座、业务脚本及所需服务端/契约依赖，不能只看 APK 版本号。
4. 三方针对该快照逐一明确确认，登记唯一执行者。未回复、存在未解决冲突或输入再次变化时，保持待确认，不开始打包。
5. 执行者独占打包现场，运行必要回归、构建、签名和包内校验，登记产物并向另外两个对话同步结果。
6. 如另获安装或发布授权，记录目标设备/范围和实际生效证据。正在执行的任务未经授权不停止，下载完成不等于运行版本已切换。

## 批次记录模板

- 批次编号与发起时间：
- 协调对话、唯一打包执行者：
- 状态：收集中 / 待确认 / 已确认 / 构建中 / 已构建 / 已验收 / 取消。
- 本批次纳入功能、文件清单、依赖及明确排除项：
- 固定输入快照位置、提交标识或文件校验清单：
- 三方回执：分别记录确认时间、输入标识、是否同意；不能以规则回执代替批次回执。
- 测试结果、共享文件冲突处理、已知失败及未验收项：
- APK 版本、Build ID、内置业务基线、远端业务版本和兼容检查：
- 产物路径、SHA256、签名及包内脚本校验结果：
- 安装/发布授权及实际目标范围（未授权时写明未执行）：
- 设备实际加载来源、业务版本、关键动作验证和回滚信息：

## 历史批次

暂无。此前各对话独立产生的 APK 不追认为已通过本规则的联合批次。
# 批次 2026-09-13-xiaomi14-vertical-exit（已构建，未安装）

- 本批次仅纳入当前对话已完成的小米14最近任务退出修复：停止命令进入最近任务后，在抖音卡片内执行随机起终点双贝塞尔垂直上滑，成功后直接点击燎原星火卡片；目标直播异常恢复同步调整。未纳入其他对话未确认改动。
- 构建执行者：当前对话；Build ID：`AGRI-PRODUCTION-20260913212701`；APK：`dist/apk/xiaomi14-vertical-exit-20260913/燎原星火-1.0.96-inrt.apk`。
- SHA256：`c41897d2e1bc1f54484632283f7924940391e116699f8e6d74f15560e4bc2340`；APK 版本：`1.0.96/132`；生产 API：`https://qk-api.dafengchan.top/api/v1`。
- 校验：Gradle 构建成功；APK v2/v3 签名验证通过；包内包含 `core/accessibility.js`、`device-profiles.js`、`features/publish-video/douyin-post-publish-cleanup.js`、`features/account-warmup/target-live-entry.js`；包内脚本已确认使用垂直上滑和加速双贝塞尔轨迹；定向测试清理器14/14、无障碍29/29、视频养号12/12、目标直播及注册表通过。
- 安装与真机任务：本批次未执行，等待单独授权；当前设备仍是旧 APK，安装后需重新启动停止任务进行真机验收。
# 批次 2026-09-13-xiaomi14-two-swipe-stop（已构建，未安装）

- 本批次纳入当前对话的小米14停止流程两段手势：底部中央上滑至屏幕中心进入最近任务；在右侧抖音卡片内使用更新后矩形范围随机点和双贝塞尔上滑；成功后直接点击燎原星火卡片。
- 构建执行者：当前对话；Build ID：`AGRI-PRODUCTION-20260913220151`；APK：`dist/apk/xiaomi14-two-swipe-stop-20260913/燎原星火-1.0.96-inrt.apk`。
- SHA256：`4b0ca336929a4e1c07f9688b3dbc619fee634a5c0e5b07f6ea1ce9c2bbf1b381`；业务基线版本：`20260913.140151224`；APK 版本：`1.0.96/132`。
- 校验：Gradle 构建成功；APK v2/v3 签名通过；包内包含停止清理器、目标直播恢复、设备画像和无障碍驱动；包内确认包含底部中央→屏幕中心手势及起点下移70/终点上移50的卡片轨迹；定向测试清理器14/14、无障碍29/29、视频养号12/12通过。
- 安装/任务：已按用户授权通过 ADB 5037 安装到 `d950a769`，返回 `Success`；设备版本 `1.0.96/132`，安装更新时间 `2026-09-13 22:49:35`。设备端拉回 APK SHA256 与产物一致；尚未启动任务，等待停止动作真机验收。
# 批次 2026-09-13-xiaomi14-two-swipe-only（已构建并安装，已被后续批次替代）

- 本批次仅纳入当前对话的停止动作收敛：小米14只执行底部中央到屏幕中心的进入最近任务上滑，以及抖音半展示卡片内的垂直上滑；移除 recents 兜底、重试、回桌面、包名启动和燎原星火卡片点击。
- 构建执行者：当前对话；Build ID：`AGRI-PRODUCTION-20260913223848`；APK：`dist/apk/xiaomi14-two-swipe-only-20260913/燎原星火-1.0.96-inrt.apk`。
- SHA256：`97b89567e71997d04078671b5d7b3d976f33cec675bbd3823f0a51c80b03b4be`；APK 版本：`1.0.96/132`；业务基线 `apkBuildId` 已对齐。
- 校验：Gradle 构建成功；APK v2/v3 签名通过；包内确认仅保留小米14两段上滑逻辑，失败不再执行后续退出动作；停止清理器14/14、无障碍29/29、视频养号12/12通过。
- 安装/任务：已于 2026-09-13 22:49:35 安装到 `d950a769`，设备端 APK SHA256 与本地产物一致；该版本真机发生误划燎原星火，后续由 `2026-09-14-xiaomi14-latest` 替代。

## 批次 2026-09-14-xiaomi14-latest（已构建并安装，越过三方确认）

- 用户明确要求立即重新构建并安装；本批次按该一次性授权越过三方回执，由当前对话独占构建现场。未发布远程业务包，未启动业务任务。
- 输入范围：从当前 `mobile-agent/autojs` 工作区整体同步，包含小米14严格最近任务清理器、评论词共用退出入口、业务脚本加载/基线机制及当时工作区其他手机端修改；未建立可排除其他未提交改动的物理暂存区。
- 构建执行者：当前对话；Build ID：`AGRI-PRODUCTION-20260914140213`；版本：`1.0.96/132`；业务基线版本：`20260914.060213829`；`baseCompatibilityId=98cd760c7e5ed2bcb53dd75fea28ee6a41fb861f7054e0c2e596a075550bd152`。
- 产物：`dist/apk/xiaomi14-latest-20260914/燎原星火-1.0.96-inrt.apk`；SHA256：`2427a4fab98a6e6a427fa4363706652bfedbdb22da9d876b5eedc3ad1e81c3db`；生产 API：`https://qk-api.dafengchan.top/api/v1`。
- 构建：首次在 Kotlin 编译阶段因 Gradle `-Xmx1g` 堆不足失败；打包脚本调整为 Gradle `3g`、Kotlin `1g` 后，同一源码输入构建成功。APK v2/v3 签名通过。
- 包内核验：`xiaomi14-recents-cleanup.js`、`douyin-post-publish-cleanup.js`、`new-comment/index.js`、`new-comment/workflow.js`、`biz-script-runtime.js` 与源码 SHA256 逐文件一致，内置业务基线 Build ID 对齐。
- 回归：小米14防误划 23/23、清理器 12/12、目标直播恢复 3/3、评论入口接线 5/5、评论动作 17/17、评论采集 10/10、评论 runner 18/18、业务运行时 16/16、更新器 14/14、基线更新 10/10、启动隔离 4/4 通过，关键脚本语法检查通过。`new-comment-workflow.test.js` 仍有 9/18 失败，属于评论文本保留作者前缀、已移除房间租约与旧 OCR/日志断言的既有契约不同步；未据此宣称评论全量回归通过。
- 安装：ADB 5037 仅发现小米14 `d950a769`；使用 `adb install -r -g` 覆盖安装返回 `Success`，保留应用数据，安装时间 `2026-09-14 14:12:09`。设备端拉回 APK SHA256 与本地产物完全一致，应用已启动且进程存在。
- 未验收：启动 Agent 后系统停在截图授权页，默认是“共享一个应用”；需用户在手机上选择“整个屏幕”并确认后，以下一次心跳/任务日志核验 `bizScriptRuntime.source=baseline`、版本和 Build ID，再真机验收评论任务与停止动作。安装成功不能替代运行时和动作验收。
