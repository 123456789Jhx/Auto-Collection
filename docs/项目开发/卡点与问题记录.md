# 卡点与问题记录

更新时间：2026-06-23

本文只记录当前卡点、问题、风险和处理建议。

## 1. 当前未闭环问题

| 优先级 | 问题 | 影响 | 当前状态 | 下一步 |
|---|---|---|---|---|
| P0 | 直播评论 STOP 后可能仍有残余流程 | 手机可能停留在搜索或直播流程 | 待回归 | 验证 STOP 后 Agent 回到常驻待命 |
| P0 | 指定直播间仍需防误入验证 | 真实发送可能发到错误直播间 | 待回归 | 继续加强目标直播间校验 |
| P0 | 手机 Agent 未启动时后台命令不会执行 | 后台下发看似成功，手机无动作 | 设计如此 | 页面明确展示在线和轮询状态 |
| P1 | Agent ZIP 自动更新替换不稳定 | 更新失败可能导致脚本不可启动 | 待回归 | 先完善校验、备份和回滚 |
| P1 | 屏幕识别依赖平台页面结构 | UI 变化会导致搜索或发送失败 | 持续风险 | 保留文本、OCR、坐标兜底和日志 |
| P1 | 多设备节流和审计回归不足 | 多台设备可能互相影响 | 待验证 | 增加多设备回归 |
| P2 | 证据归档不完整 | 成果复查不足 | 待补 | 按证据目录补正式材料 |

## 2. 已处理问题

| 问题 | 处理结果 |
|---|---|
| 多设备共用旧设备身份 | 已改为唯一设备 ID 和 token 绑定 |
| 普通移动端接口自动创建设备 | 已改为未注册返回 401 |
| 方案分散在多个目录 | 已收敛到 `docs/方案设计/` |

## 3. 回归清单

1. 设备注册和命令轮询正常。
2. 直播评论配置可保存、下发和回传。
3. 直播评论停止后 Agent 保持常驻。
4. 生产环境 `/ready` 正常。

## 4. 证据要求

1. 关键修复要有日志、命令 ID 或测试结论。
2. 真机截图和录屏放入正式证据目录。
