# 卡点与问题记录

更新时间：2026-07-16

本文合并原 `卡点与问题记录.md`、后台 UI 重构卡点、后台 UI 页面收敛卡点和后台 UI 视觉优化卡点。`docs/项目开发/error` 是临时日志材料，不纳入本文合并，也不删除。

维护规则：所有卡点、Bug、异常现象和阻断问题必须先记录到本文；修复成功后在原条目回填根因、解决方式、验证结果和剩余风险，不能只在对话、提交说明或临时日志里说明。

## 1. 当前未闭环问题

| 优先级 | 问题 | 影响 | 当前状态 | 下一步 |
|---|---|---|---|---|
| P0 | 后台直播评论暂停/停止与手机实际任务状态需要继续对账 | 页面显示已暂停，但手机可能仍在搜索、滑动或直播流程中 | 待回归 | 用命令回执、心跳 `currentTaskType`、运行日志和 `live_comment_actions` 对账 |
| P0 | 保存指定直播间配置后远程指令触发链路需要验证 | 修改目标账号后，手机可能未立即进入新的直播评论任务，或进房后采样/评论收尾不符合预期 | 0 采样退出、单轮采样后立即退出、保存目标直播间强制切 `agri_chatbot` 均已修，待真机回归 | 保存后触发 `REFRESH_CONFIG` 或调度命令，并确认进房后持续采样、多次评论、话术来自 `replyPools` |
| P0 | 任务切换时手机端页面恢复能力不足 | video 切 live_comment 时，脚本可能停在搜索页、猜你想看、错误直播间或弹窗 | 代码已修，待真机回归 | 用 `run-request-resolver`、当前命令对账和三机切换日志确认旧任务已中断 |
| P0 | 直播评论 STOP 后可能仍有残余动作 | 停止后仍可能搜索或下滑 | 手机端 STOP 优先级已修，待真机回归 | STOP 只停止直播评论子任务，Agent 回到常驻待命并上报停止原因；重点验证 STOP 不被后续 START 覆盖 |
| P0 | 直播评论执行期间后台短暂离线 | 手机脚本仍在运行，但直播评论长链路期间心跳空窗超过后台 3 分钟阈值，页面先离线后恢复在线 | 代码已修，待打包安装回归 | 安装 `1.0.28/64` 后直播评论连续运行 10 分钟以上，确认独立心跳守护持续上报且心跳年龄不超过 3 分钟 |
| P0 | 指定直播间防误入仍需真机验证 | 真实评论可能进入错误直播间 | 代码已补用户行入口，待真机回归 | 目标直播间校验必须结合主播名、标题关键词、画面关键词和点击入口；本轮重点验证 `target_user_live_entry` |
| P0 | 商品卡直播评论链路待真机回归 | 新增商品卡路径会操作商城、商品卡、直播板块和目标直播间，UI 形态不稳定时可能找不到商品卡或目标房间 | `1.0.37/73` 商品卡 V2 未按方案执行，已修复为商品卡浏览后进入直播板块目标门禁；`1.0.41/77` 已收敛后台配置入口、启动链路和无审批评论预约，APK 与 Agent 更新包已上传到生产下载目录，待安装回归 | 后台下发 `commerce_card_live_comment` 前，确认工作台配置已保存商品卡养号和目标直播间评论配置；安装 `1.0.41/77` 后用“商城 -> 搜索关键词 -> 浏览商品卡 15 分钟 -> 直播板块找目标 -> 评论/跳过审计 -> 直播养号可选阶段”完整录屏和运行日志验证 |
| P0 | 直播评论搜索关键词输入失败 | 后台直播评论任务能启动，但手机停在搜索/设置流程，不能进入目标直播间 | 代码已修，待打包安装回归 | 安装 `1.0.31/67` 或更高版本后，用三台手机验证目标账号搜索不再报 `search_keyword_set_failed`，并核对失败日志中的 `inputText/activity/screen` |
| P0 | 任务过程中误跳无障碍设置页 | 无障碍已开启但两台手机仍进入 MIUI 无障碍设置页，阻断直播评论链路 | 代码已修，待打包安装回归 | 安装 `1.0.31/67` 或更高版本后，冷启动和任务失败收尾均不得自动误跳 `android.settings.ACCESSIBILITY_SETTINGS`；如权限状态未知，只记录并待命 |
| P0 | 生产后台仍未部署到最新提交 | 线上后台仍显示旧版本，无法验证搜索直播评论和商品卡直播评论拆分后的 UI 与日志看板 | 已强制重建 Web 并验证新前端包内容 | 继续安装新 APK 做真机回归，重点确认搜索直播评论、商品卡直播评论、日志实时同步和移动端签名请求 |
| P0 | AutoJS6 运行时 OOM 导致 APP 崩溃 | 手机出现 AutoJS6 应用崩溃页，主脚本、守护脚本和无障碍状态可能随之混乱 | 代码已修，初步回归中 | 安装新 APK 后继续待命观察 logcat，确认 `ScriptThread...main.js notifyThreadExit` 不再持续刷屏 |
| P1 | 后台功能状态看板与日志自动同步待联调 | 工作台新看板依赖 overview 聚合、心跳和日志文件自动同步；未部署或旧 APK 下数据可能不更新 | 后台已部署，待安装新 APK 真机回归 | 安装新 APK 后，确认工作台四类状态、日志中心完整日志、手机本地日志时间一致 |
| P1 | 搜索页网络空白或异常处理不足 | 空白页可能不会点击重试，也可能无限等待 | 待处理 | 增加 `network_error`、`search_empty` 状态和重试上限 |
| P1 | 抖音页面和弹窗不可控 | 新弹窗、键盘、权限框会阻塞任务 | 持续风险 | 记录页面状态、截图或可见文本，按保守顺序关闭或暂停 |
| P1 | 多设备调度和审计仍需增强 | 多设备切换任务时可能互相影响或无法解释原因 | 方案中 | 后续引入 Task Orchestrator 和 assignment 对账 |
| P2 | 正式证据归档不完整 | 成果复查不足 | 待补 | 真机截图、录屏、日志样本放入正式证据目录 |

## 1.0.1 2026-07-07 商品卡直播评论真机回归卡点

现象：

- 产品新增商品卡路径：进入商城搜索“夏橙”，刷商品卡，命中 `秭归/夏橙` 和直播信号后进入直播间评论并观看。
- 该路径依赖抖音商城、商品卡、商品详情和直播入口控件形态，真机 UI 可能与搜索直播间链路不同。

当前实现：

- 新增 `commerce-card-live-runner.js`，作为 `live` 任务下的独立商品卡直播评论 runner。
- 商品卡配置收口到后台高级配置 `p3ExtensionsConfig.commerceCardLiveComment`，手机本地默认关闭；只有后台明确 `enabled=true` 时才会替代普通直播任务。
- 真实评论发送必须同时开启 `executeEnabled` 和 `manualExecutionApproved`，否则只记录 skipped 审计结果，不点击评论输入框。
- 新增商品卡配置：搜索词 `夏橙`，匹配词 `秭归/夏橙`，每轮扫描 15 分钟，命中直播观看 15 分钟，最多 3 轮。
- 新增抖音适配方法：进入商城、商城商品卡搜索、商品卡直播入口识别、进入直播间后二次关键词校验。
- 评论仅从审批后的 `commentPool` 中随机选择，不购买、不加购、不关注、不私信；发送、跳过和失败结果进入统一直播评论审计记录。

待验证：

1. 真机是否能稳定进入商城并搜索“夏橙”。
2. 商品卡列表或详情页是否能稳定识别 `秭归/夏橙` 和直播入口。
3. 进入直播间后二次关键词校验是否足够，是否会因直播间可见文本不足导致误判。
4. 后台未开启或未审批时是否不会触发商品卡任务、不会真实发送。
5. 随机评论是否能在商品直播间成功发送，且执行记录能看到 sent/skipped/failed。
6. 观看 15 分钟期间 STOP/PAUSE 是否能中断。
7. 任意一轮 15 分钟内没刷到匹配直播时，任务是否正常结束而不是无限滑动。
8. 同一任务内重复进入同一直播间时是否按直播间指纹跳过重复评论。

## 1.0.2 2026-07-08 后台功能状态与日志同步联调卡点

现象：

- 独立“直播评论”页面容易让运营误以为新商品卡直播评论仍从旧页面启动。
- 日志中心默认事件过多，用户更关心设备在线、配置同步、采集链路和日志同步是否正常。
- 完整日志如果依赖用户手动拉取，会导致排查时看到旧日志。

当前实现：

- 后台导航移除独立直播评论入口，`live_comment` 任务控制合并到任务调度页。
- 旧直播评论 runner、配置、动作记录 API 和页面文件未删除，只是不再作为主导航入口。
- 工作台新增功能状态看板，按设备在线、配置同步、采集链路、日志同步四类状态聚合。
- overview 接口返回每台设备当天结构化日志和完整日志上传状态。
- 手机端心跳后按节流策略自动上传当前日志文件，手动“重新同步”只作为兜底。

待验证：

1. 部署后台后，导航不再显示独立“直播评论”菜单。
2. 任务调度页仍能下发直播评论任务，并能从心跳看到手机实际任务。
3. 工作台四类功能状态能随心跳、运行日志和完整日志上传自动变化。
4. 日志中心默认显示功能状态，完整日志标签展示后台收到的完整内容。
5. 安装新 APK 后，不手动点击重新同步也能看到当前日志文件周期性更新。

## 1.0.3 2026-07-08 搜索直播评论与商品卡直播评论拆分卡点

现象：

- 用户确认旧“直播评论”功能是从搜索框搜索目标直播间后评论。
- 新商品卡直播评论必须从抖音商城商品卡进入直播，不能挂在普通直播或旧直播评论入口下。
- 第一版商品卡 runner 曾由普通 `live` 任务配置触发，容易抢占普通直播采集并造成后台文案混淆。

当前实现：

- 新增独立任务类型 `commerce_card_live_comment`，后台 schema、任务调度页、命令 payload、手机 resolver 和 task scheduler 均已识别。
- `live_comment` 继续表示搜索直播间评论，执行 `live-comment-runner.js`。
- `commerce_card_live_comment` 执行 `commerce-card-live-runner.js`，日志和 checkpoint 均按商品卡任务类型记录。
- 普通 `live` 只执行普通直播 phase，不再检查 `commerceCardLiveComment.enabled` 来切换 runner。

待验证：

1. 任务调度页两个按钮分别下发 `live_comment` 和 `commerce_card_live_comment`。
2. 下发 `live_comment` 时手机仍走搜索框搜索目标直播间并评论。
3. 下发 `commerce_card_live_comment` 时手机从商城商品卡路径进入匹配直播间。
4. 下发普通 `live` 时，即使商品卡配置开启，也只执行普通直播采集。
5. 工作台和日志中心能把“搜索直播间评论”和“商品卡直播评论”分开显示。

## 1.0.4 2026-07-08 生产 Web 镜像构建卡点

现象：

- 用户打开生产后台仍看到老版本，当前提交没有在线上生效。
- 部署日志显示自托管 runner 已完成 SSH 连接、源码同步和 compose 构建启动，但 Web 镜像构建失败。
- 失败点为 `nginx:1.27-alpine` 基础镜像元数据请求，服务端 Docker 镜像源返回 `401 Unauthorized`。

根因：

- 生产 `Dockerfile.web` 最终阶段依赖 `nginx:1.27-alpine`。
- 服务端 Docker daemon 使用的镜像源对该 nginx 镜像请求未授权，导致 Web 镜像无法构建，后续 compose 启动和健康检查不会执行。
- API/Web 构建阶段使用的 `oven/bun:1.3.14` 在当前构建链路可用，可以用同一 runtime 承接静态服务和 API 代理，减少生产基础镜像依赖。

处理方案：

1. 新增 Bun Web server，保留原 Nginx 行为：静态资源、SPA 回退、`/downloads` no-store 下载、`/api`、`/health`、`/ready` 代理到 API 容器。
2. `Dockerfile.web` 最终阶段改为 `oven/bun:1.3.14`，不再拉取 nginx 镜像。
3. 生产 compose 下载目录挂载从 `/usr/share/nginx/html/downloads` 调整为 `/app/public/downloads`。
4. 增加专项测试覆盖代理、静态资源、下载目录、SPA 回退和路径穿越防护。

验证结果：

- 已先执行新增测试并确认失败：缺少 `deploy/web-server` 模块。
- 修复后已执行 `bun test deploy/web-server.test.ts`，通过。

剩余风险：

- 用户提供生产 SSH 登录方式后，已直连服务器 `/opt/auto-collection/account-data-platform` 部署当前提交。
- 远端已备份 `.env.production`，补齐强管理员密码和 `MOBILE_REQUEST_SIGNING_REQUIRED=true`，未记录密钥明文。
- 已检查 `http://106.54.41.106:18080/health` 返回 `ok`，`/ready` 返回 `ready`，前端页面静态资源更新为 `/assets/index-BPs2pwE9.js`。

## 1.0.5 2026-07-12 生产 Web 镜像 TypeScript 引用构建卡点

现象：

- 商品卡组合任务全量部署到 `/opt/auto-collection/account-data-platform` 时，远端 Web 镜像构建失败。
- 失败点为 `src/lib/api-client.ts` 和 `LiveTargetsPage.tsx` 引用 `@pkg/types` 时，TypeScript 报 `TS6305`，提示 `packages/types/dist/src/index.d.ts` 未从源文件构建。

根因：

- `apps/web/tsconfig.json` 使用 project references 引用 `packages/types`。
- `Dockerfile.web` 在干净容器里直接执行 `bun --filter @app/web build`，没有先构建 `packages/types` 的声明产物；本地工作区因为已有构建产物和 `tsc -b` 验证，不会暴露该问题。

处理方案：

- `Dockerfile.web` 在 Web build 前新增 `bunx tsc -b packages/types`，保证生产容器内先生成 project reference 需要的声明文件。

验证结果：

- 已重新执行远端 API/Web/db-push 构建、数据库同步和 API/Web 重启。
- 远端 `docker compose` 显示 postgres、redis healthy，API/Web started。
- 公网 `http://106.54.41.106:18080/health` 返回 200，`/ready` 返回 200，首页资产更新为 `/assets/index-BX1GbJtC.js`。

## 1.0.6 2026-07-12 商品卡组合任务调度入口缺失

现象：
- 任务调度页只显示一个“商品卡评论/商品卡组合任务”启动入口，运营无法明确选择“刷商品卡养号”还是“目标直播间评论”。
- 后台创建 V2 assignment 时始终使用目标配置里保存的 `enabledStages`，没有把一次手动启动的阶段意图冻结到 `effectiveWorkflow`，容易出现点了商品卡任务但实际按默认组合阶段运行的问题。

根因：
- 二阶段方案推荐继续使用同一个 `commerce_card_live_comment` 任务类型，通过 `enabledStages` 区分 `product_nurture`、`target_comment` 等阶段；前端只落了单个任务类型按钮，没有补充“启动模式”。
- 后端 orchestration 只基于目标中心配置生成快照，未读取启动请求中的阶段模式。

处理方案：
- 任务调度列表和详情工具栏新增两个按钮：`商品卡养号` 下发 `commerceCardStartMode=product_nurture`，`目标直播间评论` 下发 `commerceCardStartMode=target_comment`。
- 后端 V2 assignment 创建时读取 `payload.commerceCardStartMode`，用该模式覆盖并重新校验 `runtimeConfig.enabledStages`；未传模式的旧调用保持按目标配置执行。
- 纯商品卡养号模式隐藏真实评论审批，并在提交时强制 `executionApprovalId=null`；目标直播间评论模式保留审批选择。
- 手机端不需要升级：现有 V2 runner 已按后台冻结的 `effectiveWorkflow.configSnapshot.runtimeConfig.enabledStages` 执行。

验证结果：
- `bun test apps/api/src/services/task-assignment-types.test.ts` 通过。
- `bun test apps/web/tests/navigation-dashboard-status.test.js` 通过。
- `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 通过。
- `bun run typecheck`、`bun run lint`、`bun test`、`bun run build` 均通过；生产构建仅保留既有 Vite chunk 体积警告。

剩余风险：
- `target_comment` 模式仍依赖目标配置里的评论池和单房评论上限校验；如果目标中心配置不完整，后台会拒绝启动并返回配置错误。
- 需要部署到生产后在页面上确认两个按钮可见，并用 1 台手机分别回归两种模式的实际日志。

## 1.1 2026-07-01 直播评论执行层未彻底隔离

现象：

- 后台下发直播评论任务后，手机可能停留在上一轮搜索词页面。
- 直播评论任务可能复用普通直播流程，导致进入非目标直播间。
- 搜索阶段出现风控页时，日志可能继续记录为后续点击失败，而不是明确的 `risk_control`。

根因：

- `live_comment` 只有调度状态分离，执行流程仍可能复用普通 `live` runner。
- 搜索关键词设置后缺少二次校验，无法发现旧关键词残留。
- 风控检测没有覆盖搜索、点击、进房、评论前的全部阶段。

处理方案：

- 以 `docs/方案设计/功能方案/直播评论独立任务闭环修复方案.md` 为唯一修复口径。
- 先确认包含任务切换修复的代码基线。
- 新增或整理直播评论独立 runner。
- 禁止直播评论任务调用普通直播 runner。
- 搜索、目标直播入口、进房、评论、退出都做强校验。
- 三台手机用 ADB 观察完整链路，后台日志与手机画面必须一致。

已落地代码：

- `f073eed fix: stabilize backend task switching`：新增 `run-request-resolver.js`，将后台当前命令解析和旧任务中断前置，避免手机继续执行上一轮任务。
- `bb1e041 fix: improve target live entry fallback`：补充目标直播入口几何兜底识别，减少搜索页错点和非目标直播入口误入。
- `840959a fix: isolate live comment execution flow`：新增 `live-comment-runner.js`，直播评论独立执行搜索、目标入口、进房校验、评论发送和退出收尾，不再复用普通直播 runner。
- `mobile-agent/autojs/project.json` 已提升为 `1.0.21/57`，本地产物为 `dist/apk/燎原星火-1.0.21-inrt.apk`。

验证状态：

- 已补充 `run-request-resolver.test.js`、`live-card-geometry.test.js`、`live-comment-runner.test.js`。
- 2026-07-02 使用 `1.0.22/58` 三机从后台直播评论页下发 `START`：
- `幸福安康` 成功进入目标直播间 `爱番茄的蛋`，产生 `planned -> sent` 评论动作，但发送 1 条后立即记录任务完成并退出抖音；2026-07-03 复盘确认这不是历史期望行为，而是独立 runner 只采样一轮导致的回归。
  - `勤能致富` 和 `大道至简2266` 均失败，停止原因为 `search_keyword_set_failed`，未进入目标直播间。
  - 两台失败设备随后出现无障碍设置页；系统日志显示 `android.settings.ACCESSIBILITY_SETTINGS` 的启动来源 UID 等于 `com.agri.video.collector`，不是系统或抖音主动跳转。
- 未安装 `1.0.21/57` 的手机不能按已修复能力判断。

### 1.1.1 2026-07-02 三机直播评论回归异常

现象：

- 后台下发直播评论 `START` 后，三个命令均被手机拉取并回执 `DONE`，回执内容为 `live comment command accepted; target room execution continues asynchronously`。
- `幸福安康` 成功进入 `爱番茄的蛋` 直播间，发送 1 条评论后记录 `直播评论任务完成`、`农业视频手机采集脚本结束`、`任务结束后退出抖音`。
- `勤能致富` 和 `大道至简2266` 没有进入目标直播间，后台运行日志记录 `直播评论任务失败，已停止`，结束上下文中的 `lastStopReason` 为 `search_keyword_set_failed`。
- 两台失败设备进入 MIUI 无障碍设置页；logcat 记录 `START u0 {act=android.settings.ACCESSIBILITY_SETTINGS ...} from uid ...`，该 UID 与 `com.agri.video.collector` 包 UID 一致。

当前判断：

- 幸福安康评论后立即退出不是正确行为；历史链路是按直播任务时长持续采样并多次评论。根因是独立 `live-comment-runner` 只调用一次 `sampleCurrentRoom()` 后返回，外层随即 `finishTask()` 并退出抖音。
- 两台失败的主因不是“没有直播间”或“进入了错误直播间”，而是搜索关键词输入阶段没有通过校验，直接以 `search_keyword_set_failed` 停止。
- 无障碍设置页是本 APK 拉起的，但本次日志没有打印到 `无障碍服务未开启，跳转设置页` 这条业务日志，说明还需要继续补调用阶段日志或检查 launcher/权限检查重入路径。

待修复方向：

1. `openSearch` 和 `enterSearchKeyword` 必须把每种输入策略的失败原因、当前 activity、焦点输入框、可见文本样本上报到运行日志。
2. 对直播评论任务，搜索关键词输入失败后应停在可解释状态并退出抖音或回 APP 待命，不允许再误跳系统设置页。
3. `permission.ensureAll()` 自动跳设置页只能发生在启动前权限检查阶段；任务执行中、任务失败收尾和后台待命循环不应自动拉起设置页。
4. 如果系统设置值已经包含 `com.agri.video.collector/org.autojs.autojs.core.accessibility.AccessibilityServiceUsher`，即使 `auto.service` 短暂不可用，也应先记录错误并待命，不应直接打开设置页。

2026-07-03 补充修复：

1. `live-comment-runner` 改为按 `durationMinutes` 多轮调用 `sampleCurrentRoom()`，汇总 `sampleCount`、`sentActionCount`、`plannedActionCount` 等结果，避免进房后只评论一轮就收尾。
2. 后台直播评论页保存指定直播间时不再强制写入 `liveCommentMode: "agri_chatbot"`，改为明确写入 `target_follow`，以便继续走 `liveComment.replyPools` 话术数组。
3. 新增回归测试 `testLiveCommentSamplesUntilDurationExpires()` 和 `live-comments-page.test.cjs`，分别覆盖持续采样和目标直播间保存不强制切模式。

### 1.1.2 2026-07-02 直播评论暂停、进房耗时与更新后无障碍弹窗排查

现象：
- 后台直播评论页点击 `PAUSE` 后，手机端可能仍继续搜索、进直播间或完成当前评论链路。
- 后台直播评论从打开抖音、搜索、搜索结果、直播间列表到目标直播间耗时偏长。
- 综合搜索页已出现目标账号且头像带“直播/直播中”标识时，页面可能没有下方大直播卡片；旧逻辑仍可能优先走通用直播卡片扫描或坐标兜底。
- 更新 APK 后立刻启动脚本并从后台下发任务，部分手机会跳到无障碍设置页；等待一段时间后再测，直播评论链路可正常执行且不再弹出设置页。
- 幸福安康曾出现 12 小时限制发言，需要确认是否已闭环。

排查证据：
- 后台命令记录显示，2026-07-02 15:18 左右三台设备的 `PAUSE` 命令被后续 `STOP` 覆盖，状态为 `IGNORED`，原因是 `superseded_by_new_state_command`；这类情况下手机根本不会收到暂停命令。
- 幸福安康 2026-07-02 15:20 的 `PAUSE` 命令被手机拉取并回执 `DONE`，回执结果为 `{ applied: true, taskType: "live_comment_control", commandType: "PAUSE" }`，说明直播评论页按钮本身能下发有效命令。
- 手机端 `live_comment_control` 的 `PAUSE` 只设置 `paused=true`、`liveCommentExecutionEnabled=false`，不设置 `stopRequested=true`；而 `live-comment-runner` 的搜索进房链路主要检查 `shouldStop()`，`shouldStop()` 只看 `exitRequested/stopRequested`，不看 `paused/liveCommentExecutionEnabled`。因此正在搜索或进房时，暂停不会立即中断。
- 成功链路耗时集中在两段：`task_start -> launch_route` 约 18-34 秒；`live_comment_start -> target_room_verified` 约 69-88 秒。三台成功进房记录的 `searchResult.source` 都是 `text_coordinate_fallback`，说明直接结构化识别没有命中，走了坐标/文本兜底路径。
- 用户截图显示，“爱番茄的蛋”在综合搜索页以用户行展示，头像圆环上有“直播”标识，用户行下方是普通视频卡片，不一定出现大直播卡片。这种形态应优先点击目标用户行/头像直播入口。
- `permission.ensureAll()` 只在初始化权限检查阶段主动打开 `android.settings.ACCESSIBILITY_SETTINGS`。更新后立即启动时，Android 设置里可能已显示无障碍已开启，但 AutoJS 服务对象或辅助服务绑定还没稳定可用，权限检查短时间误判后会拉起设置页；等待后再测不弹窗，符合启动竞态特征。
- 代码中已有对“访问被拒绝、操作过于频繁、预计 N 小时后正常访问”等风控文本的识别测试；但 2026-07-01 至 2026-07-02 的运行日志、评论执行记录和完整日志文件没有命中 `12小时/禁言/限制/访问被拒绝/操作过于频繁/risk_control` 等真实线上记录。

当前判断：
- 直播评论暂停问题不是单一前端按钮问题，而是两个问题叠加：后台会用新状态命令覆盖未拉取的旧 `PAUSE`；手机端收到 `PAUSE` 后，对直播评论搜索进房这种长链路没有即时中断能力。
- 慢链路主要卡在目标直播间搜索和进房识别，不是后台命令下发慢。截图场景的直接根因是“头像带直播标识的目标用户行”没有被当成最高优先级直播入口，导致脚本继续尝试通用直播卡片或 `text_coordinate_fallback`。
- 工作台和任务调度页的暂停存在同类风险：只要共用状态命令覆盖规则和手机端协作式暂停模型，就可能出现“页面已暂停、手机仍跑到下一个检查点才停”的现象；任务调度页还需要单独按 assignment 链路回归。
- 更新后误跳无障碍页当前更像启动竞态和权限检测误判，不应通过改业务搜索逻辑处理；应在权限检查层补充多次确认、冷启动等待和“已在系统配置中开启但服务暂不可用时不主动跳设置页”的保护。
- 12 小时限制发言目前不能判定已彻底解决，只能说代码有识别能力、今天没有线上复现。后续如再次出现，需要把风控文本、当前账号、直播间、动作状态和停止原因明确上报为 `risk_control`。

后续处理方向：
1. 后台命令层区分“状态切换”和“紧急控制”，避免 `PAUSE` 在手机拉取前被后续状态命令静默覆盖，或至少在页面明确显示“已被覆盖/未送达”。
2. 手机端直播评论 runner 在搜索、输入、点击直播卡片、进房校验、采样前后统一检查暂停状态；`PAUSE` 要能让长链路快速停在可解释状态。
3. 直播入口识别优先使用目标主播名、直播中标识、卡片区域和进房后主播/标题校验，减少 `text_coordinate_fallback` 的使用次数和固定等待。
4. 权限检查只允许在冷启动授权阶段拉起无障碍设置页；更新后首次启动增加服务绑定稳定等待和诊断日志，不在任务执行失败收尾阶段误跳设置页。
5. 12 小时限制按风控事件处理，不尝试绕过；命中后停止当前账号任务、上报 `risk_control`，并进入账号冷却/人工确认流程。

已落地代码：
- `mobile-agent/autojs/app/control-loop.js`：直播评论 `PAUSE` 回执前设置 `stopRequested=true` 和 `manual_pause`，让正在执行的搜索/进房链路可被停止检查捕获。
- `mobile-agent/autojs/app/live-comment-runner.js`：`shouldStop()` 将 `paused` 和 `liveCommentExecutionEnabled=false` 纳入停止条件；目标直播搜索失败后如已暂停，按 `manual_pause` 收尾，不再误报为普通搜索失败。
- `mobile-agent/autojs/platforms/douyin.js`：`openTargetLiveRoomFromSearch()` 接收 `shouldStop` guard，在打开搜索、每轮尝试、直播卡片点击、OCR/文本兜底和滚动后检查中断；搜索结果补充 `elapsedMs`，便于后续判断耗时。
- `mobile-agent/autojs/core/permission.js`：当系统设置里已存在本 APK 无障碍服务但 AutoJS 绑定暂不可用时，先短暂等待并记录诊断日志，不再直接打开无障碍设置页。
- `account-data-platform/apps/api/src/services/command-policy.ts`：抽出后台状态命令覆盖策略，`STOP` 保持最高优先级；`START/RESUME/PAUSE` 不再覆盖未送达的 `STOP`。
- `mobile-agent/autojs/platforms/live-card-geometry.js`：新增 `user_live_row` 几何候选，识别“目标用户行左侧头像带直播标识”的入口，并优先点击头像直播标识、头像中心和用户名行。
- `mobile-agent/autojs/platforms/douyin.js`：`openTargetLiveRoomFromSearch()` 调整为先尝试 `clickTargetUserLiveEntryFromSearch()`，再尝试通用直播卡片；通用直播卡片扫描跳过 `user_live_row`，避免把目标用户行误当双列视频卡片。
- `mobile-agent/autojs/tests/live-card-geometry.test.js`：新增截图对应形态的几何测试，确认头像直播标识会被识别为 `user_live_row`。
- `mobile-agent/autojs/tests/live-comment-runner.test.js`：新增静态回归测试，确认目标用户直播入口在通用直播卡片扫描之前执行。
- `mobile-agent/autojs/project.json`：版本提升到 `1.0.24/60`，用于区分本次目标用户直播入口优化包。
- `dist/apk/燎原星火-1.0.24-inrt.apk`：本地已打包，包名 `com.agri.video.collector`，版本 `1.0.24`，构建号 `60`，SHA256 为 `7c7ca6008d029c943439d1e2ec13637d20fc2f305633ad5689de85c6fc2f8eb2`。

验证结果：
- 已执行 `node mobile-agent/autojs/tests/permission.test.js`。
- 已执行 `node mobile-agent/autojs/tests/live-comment-runner.test.js`。
- 已执行 `node mobile-agent/autojs/tests/control-loop.test.js`。
- 已执行 `node mobile-agent/autojs/tests/run-request-resolver.test.js`。
- 已执行 `node mobile-agent/autojs/tests/live-card-geometry.test.js`。
- 已执行 `node mobile-agent/autojs/tests/live-comment-runner.test.js`，覆盖目标用户直播入口优先级。
- 已执行 `bun test account-data-platform/apps/api/src/services/command-policy.test.ts`。
- 已执行 `scripts/package-autojs-apk.ps1`，生成 `dist/apk/燎原星火-1.0.24-inrt.apk`。
- 已执行 `aapt dump badging` 校验包名、版本号和应用名；中文文件名对 `aapt` 有路径编码问题，已复制到临时 ASCII 文件名后完成校验。
- 已执行 `apksigner verify --verbose --print-certs`、`zipalign -c -p 4` 和 SHA256 对账。

剩余风险：
- 以上为代码层、单元测试和本地 APK 校验，尚未安装到三台手机做真机闭环。
- 直播入口慢的问题已补“目标用户行头像直播标识优先入口”策略，但仍需用真机日志确认 `searchResult.source` 从 `text_coordinate_fallback` 改善为 `target_user_live_entry`，并确认进房后目标账号校验通过。
- 12 小时限制仍按“有识别能力、未复现、未闭环”处理；再次出现时必须确认是否上报 `risk_control`。

### 1.1.3 2026-07-03 直播评论进房后 0 采样退出

现象：

- 后台直播评论页下发目标直播间任务后，手机能搜索并进入目标直播间。
- 进入直播间后没有产生 `直播间只读采样` 明细和 `live_comment_actions` 评论动作，随后直接记录 `直播评论任务完成`、`农业视频手机采集脚本结束`、`任务结束后退出抖音`。
- 生产运行日志显示 `勤能致富`、`大道至简2266` 最近一次任务的 `searchResult.source=target_user_live_entry`、`reason=room_verified`，但完成日志为 `sampleCount=0`、`sentActionCount=0`、`failedActionCount=0`、`skippedActionCount=0`。

根因：

- 后台保存目标并触发 `REFRESH_CONFIG` 时携带 `reenterTargetRoom=true`。
- 手机端 `control-loop.js` 会按设计设置 `context.liveCommentTargetRoomRefreshRequested=true`，表示需要重新进入目标直播间。
- 独立 `live-comment-runner.js` 自己完成目标进房后，没有清理这个刷新标记，也没有写入 `context.targetLiveRoomEntry`。
- `live-room-sampler.js` 采样循环启动后先检查 `isTargetRoomRefreshRequested()`；由于标记仍为 true，采样器在第 1 次采样前就以 `target_room_refresh_requested` 结束，所以外层看到的是 `sampleCount=0`。
- `live-comment-runner.js` 之前没有把 `target_room_refresh_requested` 当失败处理，继续调用 `finishSuccess()`，最终由 `collector-app.js` 正常收尾退出抖音。

处理结果：

1. `mobile-agent/autojs/app/live-comment-runner.js` 新增成功进房后的状态落点：清理 `liveCommentTargetRoomRefreshRequested`。
2. 同步写入 `context.targetLiveRoomEntry`，记录 `enteredAt`、`keyword`、`anchorName`、`titleKeywords`、`roomKeywords`、`source`、`reason`，供后续 `matchTargetRoom()` 在直播间文本不完整时判断“确实由目标搜索进入”。
3. `mobile-agent/autojs/tests/live-comment-runner.test.js` 新增 `testLiveCommentClearsTargetRefreshBeforeSampling()`，先复现采样阶段刷新标记仍为 true 的失败，再验证修复后采样前标记为 false 且 `sampleCount=1`。
4. 审查发现 `control-loop.js` 的手机端命令压缩策略仍会让同批次 `STOP -> START` 中的 STOP 被 `START` 覆盖；已改为 STOP 优先，指定任务 STOP 覆盖同任务后续状态命令，全局 STOP 覆盖同批次所有状态命令。
5. `mobile-agent/autojs/tests/control-loop.test.js` 新增 `testStopIsNotSupersededByLaterStartInSamePoll()`，先复现当前代码最终仍 `running=true`，再验证修复后 STOP 回执 `DONE`、后续 START 回执 `IGNORED`。
6. `mobile-agent/autojs/project.json` 提升到 `1.0.26/62`；`1.0.25/61` 已被本次 STOP 优先级修复包取代。
7. 已打包 `dist/apk/燎原星火-1.0.26-inrt.apk`，包名 `com.agri.video.collector`，版本 `1.0.26`，构建号 `62`，SHA256 为 `8f6462105786760505eef3482226d9c0475ae1c20ac9b1490082d0e0bbe23d4e`。

验证结果：

- 已先执行新增测试并确认失败：`target room refresh flag must be cleared before sampling`。
- 修复后已执行 `node mobile-agent/autojs/tests/live-comment-runner.test.js`，通过。
- 已执行 `node mobile-agent/autojs/tests/control-loop.test.js`，通过。
- 已执行 `node mobile-agent/autojs/tests/live-card-geometry.test.js`，通过。
- 已执行 `node mobile-agent/autojs/tests/run-request-resolver.test.js`，通过。
- 已执行 `scripts/package-autojs-apk.ps1`，生成 `dist/apk/燎原星火-1.0.26-inrt.apk`。
- 已执行 `aapt dump badging`，确认包名 `com.agri.video.collector`、版本 `1.0.26`、构建号 `62`。
- 已执行 `apksigner verify --verbose --print-certs`，确认 v2/v3 签名通过。
- 已执行 `zipalign -c -p 4` 和 SHA256 对账。

剩余风险：

- 尚未安装 `1.0.26/62` 到三台手机做真机闭环。
- 真机回归必须确认进房后出现 `直播间只读采样启动/直播间只读采样` 日志，完成日志 `sampleCount>0`。
- 真机回归必须确认后台 `STOP` 不再被后续 `START/RESUME/PAUSE` 在手机端覆盖，停止后不继续搜索、进房或评论。
- 如果进房后能采样但不发评论，再按 `live_comment_actions` 的 `planned/sent/skipped/failureReason` 继续拆下一个问题，不再把“进房后退出”与“评论未发送”混为一个问题。

## 1.2 2026-07-02 AutoJS6 OOM 与线程高频创建

现象：

- 手机出现 `AutoJS6 应用崩溃` 页面。
- 崩溃信息为 `java.lang.OutOfMemoryError: OutOfMemoryError thrown while trying to throw an exception; no stack trace available`。
- 部分手机 APP 页面显示主脚本运行但守护脚本未运行，后台仍可收到心跳。
- 崩溃后再次启动时，可能出现无障碍设置页跳转或无障碍状态误判。

证据：

- ADB 可见手机曾持续出现 `/data/user/0/com.agri.video.collector/files/project/main.js` 的 `ScriptThread ... notifyThreadExit`。
- 频率与主脚本待命循环 `sleep(Math.min(config.runtime.agentIdleLoopMs || 1000, 300))` 对应，接近每 300ms 一次。
- 后台心跳由 `main.js` 的常驻循环上报，不依赖 `watchdog.js`，因此守护脚本未运行时仍可能在线。

当前判断：

- `collector-app.js` 的待命循环高频调用 `controlLoop.pollControlCommandsAsync(false)`。
- `pollControlCommandsAsync(false)` 在创建线程后才进入 `pollControlCommands` 判断是否达到 `commandPollIntervalSeconds`。
- 当未到轮询间隔时，新线程很快退出，但线程已经创建，长期待机会导致 AutoJS6 线程/脚本资源累计，最终触发 OOM。

处理方案：

1. 将后台命令轮询的间隔判断提前到创建线程之前。
2. 保留 `force=true` 的即时轮询能力。
3. 限制后台异步线程启动频率，避免待命循环每 300ms 创建线程。
4. 补充日志或诊断字段，区分主脚本运行、守护脚本运行、无障碍检测来源和后台心跳来源。

解决结果：

- 已在 `mobile-agent/autojs/app/control-loop.js` 增加 `shouldPollControlCommands`，在 `pollControlCommandsAsync(false)` 创建线程前先判断命令轮询间隔，未到间隔直接返回。
- 设备未注册时也会更新 `commandControl.lastPollAt`，避免未注册状态下每 300ms 创建一次后台线程。
- 已新增 `mobile-agent/autojs/tests/control-loop.test.js`，覆盖“未到轮询间隔不创建线程”和“未注册轮询受节流”两个场景。
- 已在 `mobile-agent/autojs/core/permission.js` 增加无障碍检测诊断日志，记录 `enabled`、`source`、`packageName`、`enabledServices`；不改变原有自动跳无障碍设置页行为。
- `mobile-agent/autojs/project.json` 已提升到 `1.0.22/58`，用于区分本次 OOM 修复和无障碍诊断日志版本。
- 1.0.22 已被 1.0.23 打包产物取代；当前应安装 `dist/apk/燎原星火-1.0.23-inrt.apk` 后真机回归，确认 AutoJS6 OOM 不再出现。

已执行验证：

- `node mobile-agent/autojs/tests/control-loop.test.js`
- `node mobile-agent/autojs/tests/live-comment-runner.test.js`
- `node mobile-agent/autojs/tests/run-request-resolver.test.js`
- `node mobile-agent/autojs/tests/live-card-geometry.test.js`

验证口径：

1. 真机待命至少 10 分钟，logcat 不再持续刷 `ScriptThread...main.js notifyThreadExit`。
2. 后台仍能按配置间隔收到心跳和控制命令。
3. APP 页面不再出现 AutoJS6 OOM 崩溃页。
4. 守护脚本未运行时，后台能明确展示或日志能解释心跳来源。

## 2. 已处理问题

| 日期 | 问题 | 处理结果 |
|---|---|---|
| 2026-06-15 | 多设备共用旧设备身份 | 改为唯一设备 ID 和 token 绑定 |
| 2026-06-15 | 普通移动端接口自动创建设备 | 改为未注册返回 401 |
| 2026-06-22 | 直播评论需求分散 | 合并为 `docs/方案设计/功能方案/直播评论自动化方案.md` |
| 2026-06-26 | H5 UI 重构压平采集记录和日志中心路径 | 恢复“手机卡片 -> 日期卡片 -> 明细”三层路径 |
| 2026-06-28 | 工作台和设备运行重复 | 设备运行能力收敛到工作台，移除重复入口 |
| 2026-06-28 | 任务调度页面英文状态和技术字段过多 | 增加中文映射，移除主界面 JSON 技术块 |
| 2026-06-28 | 直播评论页自动刷新下拉和固定右栏压缩主表 | 去掉刷新下拉，设备详情改右侧抽屉 |
| 2026-06-28 | 后台设备卡片视觉松散、按钮拥挤 | 改为紧凑监控行、心跳状态色块和统一图标按钮 |
| 2026-06-29 | 方案文档分散、后续方案和废弃方案混在一起 | 方案设计收敛为功能方案和架构方案，架构只保留一份系统架构 |
| 2026-07-02 | 文档闭环规则缺少统一规范入口 | 已并入 `docs/规范/开发变更与编码规范总则.md`，明确有效改动、卡点、Bug、验证和交付记录的处理流程 |
| 2026-07-02 | 直播评论执行层复用普通直播流程 | 代码层已新增独立 `live-comment-runner` 并合入任务切换解析、目标直播入口兜底；真机验证未闭环，仍保留 P0 回归项 |
| 2026-07-02 | 采集记录和日志中心重复内容过多 | 已在列表展示层增加分页、每页条数选择和同页重复合并；原始记录不删除，页面路径保持不变 |
| 2026-07-03 | 直播评论进房后 0 采样并退出抖音 | 已修复独立 runner 成功进房后未清目标直播间刷新标记的问题；审查发现并修复手机端同批次 STOP 被后续 START 覆盖的问题；版本提升到 `1.0.26/62`，APK 已打包校验，待真机三机回归 |
| 2026-07-07 | 直播评论执行期间后台短暂离线 | 已新增 Agent 独立心跳守护线程，心跳不再依赖业务 runner；同时修复任务收尾后保留 `paused=true` 的状态语义问题；版本提升到 `1.0.28/64`，待打包安装回归 |
| 2026-07-08 | CI 未覆盖手机端且生产配置有弱默认值 | 已将手机端项目 JSON 校验、可解析脚本语法检查和全部手机端 Node 测试加入 GitHub Actions；生产 API 配置缺少关键密钥或未开启移动端签名时直接启动失败，生产 compose 不再给关键密钥弱默认值 |
| 2026-07-08 | 生产 Web 镜像构建依赖 nginx 基础镜像导致部署失败 | 已改用 Bun runtime 提供静态资源和 API 代理，避免生产构建再拉取 `nginx:1.27-alpine`；已直连部署到生产并通过 `/health`、`/ready`、前端资产和新功能文案验证 |
| 2026-07-09 | 商品卡配置保存目标直播间会被 schema 拦截 | 已在 `p3ExtensionsConfigSchema` 放开 `commerceCardLiveComment.targetRoom`，并补 `updateDeviceTaskConfigSchema` 回归测试；配置中心保存后可把目标直播间名称和匹配词写入设备级商品卡配置 |

### 2.1 2026-07-02 文档闭环规则补充

现象：项目功能更新、卡点处理和规范维护容易只留在对话或提交说明里，后续复盘时难以确认哪些变更已经进入项目记录，哪些问题仍未闭环。

根因：原规范没有把“所有有效改动必须维护到项目记录”和“所有卡点、Bug 必须维护到卡点记录”作为统一交付门槛。

处理结果：

1. 已在 `docs/规范/开发变更与编码规范总则.md` 明确功能更新、修复、交互、接口、脚本、配置、部署和文档规范调整都要同步维护项目记录。
2. 已在 `docs/项目开发/项目记录.md` 补充文档闭环规范强化记录，写明变更模块、影响范围、验证结果和后续注意事项。
3. 已在本文维护规则和已处理问题中保留卡点记录要求，后续修复类事项必须在原条目回填根因、解决方式、验证结果和剩余风险。

剩余风险：后续执行仍依赖交付前检查；如果某次代码或文档变更没有同步维护记录，最终说明必须明确原因并补为待办。

### 2.2 2026-07-02 采集记录与日志中心重复内容过多

现象：手机端运行一段时间后，采集记录和结构化日志会出现大量相似内容。运营查看时容易被重复行淹没，排查人员也需要频繁翻页才能找到真正的异常或关键动作。

根因：原页面一次拉取固定数量记录并直接展示原始列表，没有分页信息、每页条数控制，也没有按同类事件进行展示层合并。

处理结果：

1. `RecordsPage` 增加分页参数、每页条数选择和同页重复采集记录合并，重复项展示为“重复 N 条”。
2. `LogsPage` 增加分页参数、每页条数选择和同页重复日志合并，异常汇总和最近关注列表也显示重复次数。
3. `styles.css` 增加统一分页条和紧凑选择框样式。
4. 合并只发生在前端展示层，不删除、不改写后台原始采集记录和日志。
5. 原有“手机 -> 日期 -> 明细”和“手机 -> 日期 -> 运行动态/异常汇总/完整日志”路径保持不变。

验证口径：

1. 采集记录和日志中心在选择设备、日期、筛选条件后页码应回到第一页。
2. 上一页、下一页和每页条数选择可正常工作。
3. 重复合并不影响点击详情、异常汇总和完整日志查看。
4. 后端原始记录总数与页面“原始 N 条”的展示能对上。

### 2.3 2026-07-07 直播评论执行期间后台短暂离线

现象：直播评论执行一段时间后，后台设备先显示离线，过一段时间又恢复在线；手机侧守护脚本和采集脚本仍在运行。

排查证据：

1. 后台 `mapDeviceStatus()` 使用 `lastHeartbeatAt <= 3 分钟` 判在线；超过 3 分钟会强制显示 `offline`。
2. 最近 100 条心跳中，`device_b6f808fa52420e43` 在 `21:10:44 -> 21:18:14` 有约 `7.5` 分钟空窗，`device_9b185a40148fe414` 在 `21:10:47 -> 21:18:02` 有约 `7.24` 分钟空窗。
3. 空窗后的下一条心跳状态为 `idle` 或 `paused`，说明脚本没有退出，后台恢复在线是因为后续心跳重新到达。
4. 当前设备恢复在线后仍可能上报 `paused`，这是任务收尾状态语义问题，不是离线的主因。

根因：直播评论独立执行层拆分时，`live_comment` 从原来带 `writeHeartbeat()` 的 `phaseRunner.runPhase("live", ...)` 路径切到 `liveCommentRunner.runTargetLiveCommentTask()`。新 runner 搜索目标直播间、进房和持续采样时没有独立心跳；持续采样修复后任务链路变长，心跳空窗超过后台 3 分钟阈值。

处理结果：

1. 新增 `mobile-agent/autojs/app/agent-heartbeat-daemon.js`，Agent 启动后创建独立后台线程，按固定间隔上报心跳。
2. 独立心跳只读取 `floatyControl.state` 状态快照，不操作抖音 UI，不依赖视频、直播、直播评论任一业务 runner。
3. `main.module.js` 创建 `agentHeartbeatDaemon`，`collector-app.initializeAgent()` 在悬浮状态就绪后、权限检查前启动守护线程。
4. 新增 `createFinishedTaskStatePatch()`，任务完成或失败后写入 `running=false`、`paused=false`，让状态回到 `idle`，避免在线设备被误显示成暂停或不可用。
5. `mobile-agent/autojs/project.json` 提升到 `1.0.28/64`，用于区分本次心跳高可用修复。

已执行验证：

1. `node mobile-agent/autojs/tests/agent-heartbeat-daemon.test.js`
2. `node mobile-agent/autojs/tests/collector-app-finish-state.test.js`
3. `Get-ChildItem -LiteralPath 'mobile-agent\autojs\tests' -Filter '*.test.js' | ForEach-Object { node $_.FullName }`
4. `node --check mobile-agent/autojs/app/agent-heartbeat-daemon.js`
5. `node --check mobile-agent/autojs/app/collector-app.js`
6. `node --check mobile-agent/autojs/main.module.js`
7. `node --check mobile-agent/autojs/app/live-comment-runner.js`

打包状态：本轮按要求只改代码，未重新打包；后续打包仍必须通过受控环境变量或既有安全来源注入注册密钥。

剩余风险：代码修复尚未安装到三台手机；真机回归时需要直播评论连续运行 10 分钟以上，并用后台心跳历史确认没有超过 3 分钟的心跳空窗。

### 2.4 2026-07-08 直播评论搜索输入与无障碍误跳修复

现象：

1. 直播评论目标账号搜索阶段可能报 `search_keyword_set_failed`，手机停在搜索页、建议页或后续设置流程，未进入目标直播间。
2. 部分手机在无障碍已开启或刚启动状态不稳定时，会被本 APK 拉起 `android.settings.ACCESSIBILITY_SETTINGS`，阻断直播评论链路。

根因：

1. 搜索关键词输入后的成功判断只看整屏可见文本，没有读取搜索输入框当前值；抖音搜索页在键盘或建议页状态下，输入框文本不一定进入整屏文本采样，可能把已输入误判为失败。
2. 无障碍检测拿不到 Android `context` 或读取系统设置失败时，旧逻辑无法区分“状态未知”和“确实未开启”，权限层会继续自动打开系统设置页。

处理结果：

1. `mobile-agent/autojs/platforms/douyin.js` 增加 `getSearchInputText()`，关键词校验先读取输入框真实文本，再读取整屏可见文本。
2. 搜索输入失败日志补充 `inputText`、`activity`、`screen` 和文本采样，便于后台日志判断是输入框未命中、键盘/建议页遮挡，还是页面状态错误。
3. `mobile-agent/autojs/core/accessibility.js` 区分 `context_unavailable` 和 `settings_unavailable`，不再把系统状态未知包装成普通未开启。
4. `mobile-agent/autojs/core/permission.js` 对未知状态只短暂等待并记录诊断，不自动打开无障碍设置页；确实检测到未开启的原启动授权路径仍保留。
5. `mobile-agent/autojs/project.json` 提升到 `1.0.31/67`，用于区分本次修复版本。

已执行验证：

1. `node mobile-agent/autojs/tests/permission.test.js`
2. `node mobile-agent/autojs/tests/live-comment-runner.test.js`
3. `Get-ChildItem -LiteralPath 'mobile-agent\autojs\tests' -Filter '*.test.js' | ForEach-Object { node $_.FullName }`
4. `node --check mobile-agent/autojs/platforms/douyin.js`
5. `node --check mobile-agent/autojs/core/permission.js`
6. `node --check mobile-agent/autojs/core/accessibility.js`

打包状态：本轮只改代码和记录，未重新打包；后续打包仍必须通过受控环境变量或既有安全来源注入注册密钥。

剩余风险：代码修复尚未安装到三台手机；真机回归时需要从后台直播评论页启动目标账号任务，确认搜索输入不再报 `search_keyword_set_failed`，且冷启动、任务失败收尾和待命状态不会误跳无障碍设置页。

## 3. 后台 UI 卡点归档

### 3.1 采集记录和日志中心路径被压平

原因：H5 UI 重构时把原本的卡片点击路径改成下拉筛选，用户无法按原习惯先选手机、再选日期、再看明细。

处理：

1. `RecordsPage` 改回未选手机、已选手机未选日期、已选日期三段状态。
2. `LogsPage` 恢复手机卡片、日期卡片、当天日志明细。
3. 保留筛选、右侧详情、日志同步、导出和完整日志能力。

结论：UI 可以优化，但不能覆盖已验证的业务路径。

### 3.2 工作台与设备运行重复

原因：两个页面都展示设备在线、心跳、任务进度和控制按钮。

处理：

1. 设备运行能力合并到工作台。
2. 设备详情改为右侧抽屉。
3. 左侧导航移除设备运行入口。

结论：设备状态只保留一个主入口，减少运营判断成本。

### 3.3 任务调度技术字段过多

原因：页面直接展示 assignment status、source、targetContext、commandStatus 等字段。

处理：

1. 增加中文状态映射。
2. 表格操作统一为视频、直播、评论、停止。
3. 详情中隐藏 JSON 技术块，只在排查区展示。

结论：主界面要解释“后台希望什么、手机实际做什么、最近命令怎样”，技术细节不能成为主文案。

### 3.4 直播评论页承担内容过多

原因：固定右侧详情、自动刷新下拉和执行记录挤在同一屏。

处理：

1. 主区只展示设备评论状态和执行记录。
2. 点击设备打开右侧抽屉。
3. 自动刷新下拉删除，保留手动刷新。
4. 行内按钮和工作台保持一致。

结论：直播评论页要聚焦“配置、控制、动作结果和失败原因”。

### 3.5 后台视觉不统一

原因：工作台、任务调度、直播评论分别使用不同卡片、按钮和状态色。

处理：

1. 工作台设备行改为五列监控行。
2. 心跳状态使用明确 `heartbeat-*` class。
3. 按钮改为图标加短文案。
4. 统一卡片边框、阴影、表格选中态和按钮高度。

结论：视觉优化不能改变 API、数据库、设备命令和原有点击流程。

## 4. 回归清单

1. 设备注册、token 认证和命令轮询正常。
2. 工作台显示设备在线、心跳、当前任务和最近日志。
3. 任务调度页无 assignment 时仍显示手机清单和快捷分配。
4. 直播评论配置保存后，手机能拿到最新 targetRoom。
5. 直播评论启动、暂停、恢复、停止都有命令回执和手机日志。
6. STOP 后 Agent 保持常驻，不继续直播评论动作。
7. 视频任务切直播评论时，手机能中断旧任务并进入目标直播间或给出明确失败原因。
8. 搜索页空白或网络异常时能点击重试或上报 `search_network_retry_failed`。
9. 采集记录保持“手机 -> 日期 -> 明细”路径。
10. 日志中心保持“手机 -> 日期 -> 运行动态/异常汇总/完整日志”路径。
11. 生产 `/ready` 正常。

## 5. 证据要求

1. 关键修复保留命令 ID、日志片段或后台截图。
2. 真机异常要保留当前页面截图、可见文本样本或完整日志。
3. 临时日志可继续放在 `docs/项目开发/error`，该文件不作为正式文档合并。
4. 正式截图和录屏放入 `docs/截图证据/` 或 `docs/演示录屏/`。

## 6. 2026-07-08 APK 打包与注册密钥风险

现象：本机未设置 `MOBILE_REGISTRATION_SECRET`，远端当前运行的 `auto-collection-api` 容器和生产 `.env.production` 仍处于占位注册密钥口径；直接按生产强密钥规范打包会被阻断。

处理：为保证当前后台联调可用，本次只读获取运行容器中的当前注册密钥值并注入 APK 打包进程，密钥明文未写入仓库、文档或终端输出；产物为 `dist/apk/燎原星火-1.0.31-inrt.apk`，已完成 SHA256、包名版本、签名和 zipalign 校验。

结论：当前包适合连接现有后台做真机回归；正式生产必须先把后台 `MOBILE_REGISTRATION_SECRET` 改为真实强密钥，再用同一值重新打包 APK 并重新安装，否则注册鉴权收紧后会出现设备无法注册或新旧包密钥不一致的问题。

后续处理：已将远端生产 `.env.production` 切换为新的强注册密钥并重建 `auto-collection-api`，`/ready` 返回 `200`；已同步打包 `dist/apk/燎原星火-1.0.32-inrt.apk`，版本 `1.0.32/68`，SHA256 为 `902092C4D7C6B1945154C2B2E0E64AB8193F918C96BC758741F905D9F1908B35`。

当前要求：旧 APK 不再作为正式注册包使用；三台手机必须安装 `1.0.32/68` 后再做直播评论和任务切换回归。

## 7. 2026-07-08 任务调度暂停恢复与任务类型回退卡点

现象：

1. 任务调度页只有“停止”，没有直接“暂停”和“恢复”入口。
2. 点击“商品卡直播评论”后，后台显示已下发新任务，但手机实际心跳仍是旧视频任务。

排查证据：

1. `mobile_commands` 中 `START` 命令的 `payload.taskType` 为 `commerce_card_live_comment`，说明后台任务调度和前端按钮没有把商品卡任务写成视频任务。
2. 手机端回执同一命令时返回 `taskType=video`，随后心跳 `currentTaskType=video`，最近消息为“搜索: 大棚蔬菜”“进入搜索视频”等旧视频流程。
3. 两台在线设备的 `STOP` 命令已被手机领取并回执，说明停止链路可用；另一台离线设备的 STOP 保持 `PENDING`，属于设备未在线领取。

根因：

1. 后台下发正确，问题发生在手机端任务类型解析或旧脚本能力边界。
2. 当前线上手机运行版本不一定包含最新商品卡任务分流逻辑；按当前要求，不通过自动更新解决。
3. 旧控制循环存在风险：如果手机脚本不认识后台明确下发的任务类型，可能回退到默认 `video`，导致“新任务跑成旧刷视频”。

处理结果：

1. 任务调度页新增“暂停”“恢复”“停止”三类控制按钮，控制目标按当前安排任务优先，安排缺失时才使用手机实际任务，最后兜底视频。
2. 页面新增“任务不一致”提示，并把安排任务与手机实际任务不一致计入异常，运营能直接看到“后台希望什么、手机实际做什么”。
3. 手机端控制循环新增防回退：明确下发的任务类型不受当前脚本支持时，回执 `FAILED` 和 `unsupported_explicit_task_type`，不再默认启动视频。

本轮不做：

1. 不处理自动更新链路。
2. 不发布或触发手机端自动升级。
3. 不打包 APK，不变更 agent 版本发布表。

待回归：

1. 后台已部署并确认新前端资产包含暂停、恢复和任务不一致文案；仍需在任务调度页实点暂停、恢复、停止，验证都能创建对应命令并显示回执。
2. 已打包手动安装包 `dist/apk/燎原星火-1.0.34-inrt.apk`，版本 `1.0.34/70`，SHA256 为 `3E96A11633E8B9E5D4536AA71B42EFA960D091348FE856B78E3E10FE2645F58E`；安装后验证未知任务不会回退视频，商品卡任务能按 `commerce_card_live_comment` 分支启动或给出明确失败原因。

## 8. 2026-07-09 直播目标配置与目标匹配卡点

现象：

1. 直播评论、商品卡直播评论都需要配置搜索关键词、目标直播间名称、别名和执行参数，但旧配置分散在任务模板 JSON、设备配置 JSON 和脚本默认值里。
2. 如果直接用直播间名称搜索，抖音结果不稳定；实际业务希望先按关键词搜索，再对搜索结果卡片里的直播间名称做相似匹配。
3. 商品卡直播评论除了评论，还要求按商品卡刷直播：单轮刷 15 分钟，刷到目标直播间后看 15 分钟并循环，没刷到就结束。

影响：

1. 运营无法在后台清晰配置“搜什么”和“匹配哪个直播间”。
2. 手机端容易把关键词、目标名、商品关键词混成同一组字段，导致进错直播间或找不到直播间。
3. 后续扩展多个目标直播间时，大 JSON 配置不可审计、不可绑定设备、也不方便定位问题。

证据来源：

1. 用户明确要求采用方案 B：独立配置表，直播间名字匹配度超过 90% 即可进入。
2. 历史脚本 `_3-抖音直播间互动(1).js` 表明旧逻辑存在按目标账号/直播间跟随评论的业务基础。
3. 现有代码中 `live-comment-runner` 只读 `liveCommentBotConfig.targetRoom`，`commerce-card-live-runner` 只读 `commerceCardLiveComment`，没有统一目标配置入口。

处理结果：

1. 新增直播目标配置表、别名表、功能配置表和设备绑定表，并新增后台保存接口和手机端聚合下发字段 `liveTargets`。
2. 新增后台“直播目标配置”页面，配置项拆成目标直播间、直播间别名、搜索直播评论、商品卡直播评论和设备绑定。
3. 手机端新增 `live-target-matcher.js`，按归一化文本和相似度阈值匹配目标直播间，排除词命中时拒绝。
4. 两个 runner 优先使用 `liveTargets`，旧配置保留兜底，减少切换风险。

验证结果：

1. 已通过后端 live target service 测试、后台页面入口测试、手机端目标匹配测试、直播评论 runner 测试、商品卡直播 runner 测试和控制循环测试。
2. 已通过 `bun test`、`bun run lint`、`bun run typecheck` 和关键手机脚本 `node --check`。

剩余风险：

1. 本次未部署数据库迁移到服务器，未打包新 APK。
2. 90% 相似度仍需真机验证抖音搜索卡片文本质量；如果 OCR 或无障碍文本拿不到直播间名称，需要补截图/OCR 证据后调整候选文本采集。
3. 设备绑定目前用于控制下发范围，仍需在多设备场景验证不同目标是否正确下发到对应手机。

## 9. 2026-07-09 MIUI 拒绝 USB 安装 APK 卡点

现象：

1. `1.0.35/71` APK 已打包并通过包名、版本、签名和 zipalign 校验。
2. 4 台在线设备中，`ce3eb0bf` 能正常卸载旧包并安装新版；`a538cc4a` 在 `adb install` 路径失败后，通过系统文件管理器发起安装成功。
3. `a538cc4a` 在 `adb install -r -g` 和推送到 `/data/local/tmp` 后执行 `pm install -r -g` 两种路径下，均返回 `INSTALL_FAILED_USER_RESTRICTED: Install canceled by user`。

影响：

1. 只通过 adb 静默安装时，MIUI 设备可能无法进入新版脚本测试，后台直播目标配置、商品卡直播评论和心跳相关变更无法在这些设备上闭环。
2. `a538cc4a` 曾按要求先卸载旧包，安装被系统拦截后短暂处于无 `com.agri.video.collector` 可用包状态；后续已通过文件管理器安装恢复。
3. 如果后续遇到“后台无设备注册/设备不在线/无障碍服务不存在”，需要先核对手机实际是否已装包，不能误判为 APK 构建失败。

已处理：

1. 已通过 adb 写入 `verifier_verify_adb_installs=0`、`package_verifier_enable=0`、`install_non_market_apps=1`、`miui_usb_dialog=0`、安装器安全提示关闭等设置。
2. 已对比成功设备 `ce3eb0bf` 的 MIUI 安装相关设置，并将可写设置同步到失败设备。
3. 已将 `com.android.shell`、`com.miui.packageinstaller` 和可用系统安装器的 `REQUEST_INSTALL_PACKAGES` app-op 设置为 `allow` 并重试安装。
4. 已验证 `a538cc4a` 从 adb shell 直接拉起安装器时，MIUI 日志返回 `Requesting uid 2000 needs to declare permission android.permission.REQUEST_INSTALL_PACKAGES`；说明 shell 不是合规安装发起方。
5. 已把 APK 推送到 `/sdcard/Download/agri-1.0.35.apk`，通过 `com.android.fileexplorer` 文件管理器点击 APK，由文件管理器拉起 MIUI 安装器后完成安装。

后续处理：

1. 若只需要安装 4 号和 15 号，本卡点已闭环：`ce3eb0bf` 和 `a538cc4a` 均已安装 `1.0.35/71`。
2. 若后续需要安装其他 MIUI 设备，优先使用“推送 APK 到 `/sdcard/Download` -> 打开系统文件管理器 -> 点击 APK -> MIUI 安装器确认”的路径。
3. 安装后用 `adb -s <serial> shell dumpsys package com.agri.video.collector` 核对 `versionName=1.0.35` 和 `versionCode=71`，再进入无障碍设置重新授权。

## 10. 2026-07-09 配置中心部署触发 API root 密码校验卡点

现象：

1. 配置中心 UI 收敛部署时，远端 Web 镜像构建成功。
2. 执行 `docker compose up -d web` 后，Compose 也重建了 API 容器。
3. API 容器持续重启，外网 `/health` 返回 500，日志显示 `ADMIN_PASSWORD must be configured for production`。

影响：

1. Web 页面可以启动，但 API 不健康会导致后台登录和数据接口不可用。
2. 该问题只在 API 容器重建后暴露；旧容器未重启时不会提前发现。

根因：

1. 当前服务器是开发联调环境，后台账号密码按要求保持 `root/root`。
2. API 的生产配置校验把 `root` 放在通用弱值集合里，导致显式配置的 `ADMIN_PASSWORD=root` 也被判定为未配置。
3. JWT、手机注册密钥等仍应保持强校验，不能因为开发服后台密码放行而全部放开。

处理结果：

1. 新增并通过 API 配置测试：生产环境显式配置 `ADMIN_PASSWORD=root` 时允许启动。
2. 实现上仅对 `ADMIN_PASSWORD=root` 放行，`JWT_SECRET=root` 和 `MOBILE_REGISTRATION_SECRET=root` 仍按弱值拒绝。
3. 已提交 `3291b61`，同步到服务器后重建 API/Web，外网 `/health`、`/ready` 和 `root/root` 登录均验证通过。

后续处理：

1. 开发服可以继续使用 `root/root`，但正式生产切换时必须改回强管理员密码。
2. 后续部署前，如果会重建 API，应先确认 `.env.production` 中关键配置是否满足当前代码校验，避免容器重建后才暴露问题。

## 11. 2026-07-09 配置中心单机编辑不适合批量配置卡点

现象：

1. 配置中心上一版按手机配置已经能写入单台手机的直播目标和商品卡参数，但页面仍是左侧手机列表、右侧单机编辑。
2. 多台手机需要相同目标直播间、关键词和商品卡参数时，需要逐台选择、逐台填写，公共模板与单机配置之间的关系不够直观。
3. 用户明确要求页面展示所有手机，每台手机一块表单，从上到下配置；公共模板只负责把同样配置快速应用到多台手机。

影响：

1. 运营无法一眼对比所有手机当前配置，容易漏配或把配置保存到错误手机。
2. 公共模板容易被误解为自动应用到所有手机，和当前“按手机 task-config 生效”的实际链路不一致。
3. 后续新增功能配置时，如果继续使用单机右侧编辑，会继续放大批量配置成本。

处理结果：

1. 配置中心改为公共模板工具条和全量手机纵向表单列表。
2. 公共模板支持应用到选中手机或全部可见手机，只填充表单，不直接绕过单机配置保存。
3. 每台手机保留独立保存、重新载入和未保存状态；批量保存只保存已修改手机。
4. 已更新配置中心回归测试，约束页面必须加载公共模板、渲染全量手机表单，并提供“应用到选中手机 / 应用到全部手机 / 保存已修改手机”入口。

验证结果：

1. 已确认更新后的测试在旧页面上失败，失败点为缺少公共模板加载。
2. 实现后 `bun test account-data-platform/apps/web/tests/live-targets-page.test.js`、`bun test`、`bun run typecheck`、`bun run lint`、`bun run build` 和 `git diff --check` 通过。
3. 已直连部署 Web 到 `/opt/auto-collection/account-data-platform`；外网 `/health`、`/ready` 返回 200，首页资产更新为 `/assets/index-7f0rV3tj.js`，资源包包含“保存已修改手机”“应用到选中手机”“应用到全部手机”“手机配置表单”。
4. 已验证后台 `root/root` 登录和设备列表接口，当前设备列表返回 3 台设备。

后续处理：

1. 需要用户在浏览器里用真实设备数据操作一次模板填充、单机保存和批量保存。
2. 如果手机数量继续增加，再评估是否需要折叠面板、分组筛选或批量复制某台手机配置。

## 12. 2026-07-09 配置入口分散与工作台卡片突兀卡点

现象：

1. 侧边栏“配置中心”把公共模板和单台手机配置混在一个独立入口里，用户希望公共模板恢复到原来的“配置”侧边栏。
2. 任务调度负责启动搜索直播评论和商品卡直播评论，但配置入口在另一个页面，执行任务前调整关键词、目标直播间和评论池不顺手。
3. 工作台设备监控卡片把心跳、配置同步、采集链路、日志同步、进度和操作按钮以大块状态色铺开，页面显得突兀且不利于快速扫读。

影响：

1. 运营容易误以为“配置中心”是唯一配置入口，也容易把公共模板和单台手机生效配置混淆。
2. 执行任务时需要在任务调度和配置页之间来回切换，降低现场调试效率。
3. 工作台看板在多设备场景下视觉噪声偏高，用户难以快速判断“哪台手机需要处理、下一步按哪个按钮”。

处理结果：

1. 已删除独立 `ConfigCenterPage`，侧边栏恢复“配置”，公共模板页标题、弹窗标题和提示语恢复旧口径。
2. 已在任务调度页新增当前选中手机的“配置”按钮和弹窗，保存时写入设备级 `task-config` 并沿用后端 `REFRESH_CONFIG` 下发链路。
3. 已把工作台设备卡片改为身份状态、运行信息和操作区三段式布局，功能状态改成有序列表，按钮固定在操作区。
4. 本次不改自动升级、不改手机端轮询、不打 APK；真实发送审批字段仍按原项目安全口径控制。

验证结果：

1. 已更新并通过 `live-targets-page.test.js`，约束配置中心移除、侧边栏恢复“配置”、任务调度配置弹窗字段完整。
2. 已更新并通过 `navigation-dashboard-status.test.js`，约束工作台设备卡片使用有序行式结构。
3. 已执行 `bun run --cwd account-data-platform/apps/web typecheck`、`bun run --cwd account-data-platform lint` 和 `bun run --cwd account-data-platform build`；生产构建通过，仅保留 Vite 大 chunk 提示。
4. 已部署到 `http://106.54.41.106:18080`，仅重建并重启 Web 容器；`/health`、`/ready`、后台登录和设备列表接口验证通过，首页资产更新为 `/assets/index-HmdX5TEL.js`。

后续处理：

1. 需要在真实后台中点选一台手机，打开任务调度配置弹窗保存一次，再看手机日志是否领取 `REFRESH_CONFIG` 并刷新配置。
2. 如果现场确实需要在这个弹窗里直接放开真实发送，再增加明确的“允许发送评论”开关，而不是保存配置时隐式开启。

## 13. 2026-07-09 工作台配置按钮误发控制指令与自动化脚本卡点

现象：

1. 用户在工作台设备卡片点击“配置”后，页面提示“控制指令已下发，等待手机领取”。
2. 该行为不是打开配置表单，而是直接发送 `REFRESH_CONFIG` 控制命令，和用户预期的“弹出配置表单、保存后自动更新手机配置”不一致。
3. 服务器部署、代码提交和 USB 安装 APK 仍依赖人工重复执行，容易遗漏远端、提交错仓库或安装前未卸载旧包。

影响：

1. 工作台卡片上的“配置”语义不准确，用户容易以为表单未实现或点击无效。
2. 配置和刷新混在同一个入口，会导致现场调试时误下发控制命令。
3. 手工部署和安装步骤多，复盘时难以确认执行过哪些验证、推送到哪些远端、安装到哪些手机。

根因：

1. 工作台卡片按钮仍写死调用 `sendCommand(item.deviceCode, "REFRESH_CONFIG")`，按钮文案改成“配置”后没有同步改交互。
2. 任务调度页已有单台手机配置弹窗，但实现只在该页面内部，工作台没有复用该表单。
3. 既有部署脚本偏 CI/远端 compose 场景，缺少本机一键提交、直连只重建 Web、USB 批量卸载安装的固定入口。

处理结果：

1. 新增共享 `DeviceLiveCommentConfigModal`，任务调度和工作台共用单台手机配置表单。
2. 工作台卡片“配置”改为打开表单；设备详情抽屉继续保留“刷新配置”按钮，用于明确下发 `REFRESH_CONFIG`。
3. 新增 `scripts/quick-submit.ps1`，支持按文件暂存、验证、中文提交消息、默认推送 `origin` 和 `tomato`。
4. 新增 `scripts/deploy-web-fast.ps1`，通过 Paramiko 直连服务器，从当前 `HEAD` 同步 `account-data-platform`，只执行 Web 容器构建和重启，并检查 `/health`、`/ready`。
5. 新增 `scripts/install-apk-usb.ps1`，先 `adb uninstall com.agri.video.collector`，再安装 APK；遇到 `INSTALL_FAILED_USER_RESTRICTED` 时推送到 `/sdcard/Download` 并通过 `am start` 拉起系统安装器。
6. 部署脚本实跑时发现远端 Docker build 输出中的 `✓` 会触发本机 Python GBK 编码错误，已强制临时 Python stdout/stderr 使用 UTF-8，并设置 `PYTHONIOENCODING=utf-8`。

验证结果：

1. 已新增并通过工作台配置按钮回归测试，约束卡片“配置”必须打开表单，不能直接发送 `REFRESH_CONFIG`。
2. 已新增并通过脚本结构测试，约束提交、部署、USB 安装脚本包含关键步骤。
3. 已执行三个 PowerShell 脚本解析检查，均无语法错误。
4. 已执行 `bun test account-data-platform/apps/web/tests/navigation-dashboard-status.test.js account-data-platform/apps/web/tests/live-targets-page.test.js`、`bun run --cwd account-data-platform/apps/web typecheck`、`bun run --cwd account-data-platform lint`、`bun run --cwd account-data-platform build` 和 `git diff --check`。
5. 已通过 `scripts/deploy-web-fast.ps1` 直连部署 Web 到服务器，外网 `/health`、`/ready` 返回正常；首页资产为 `/assets/index-CChhUmTz.js`，资源包包含“配置已保存”“目标直播间名称”和“刷新配置”。
6. 2026-07-13 发现仅验证健康接口不足以证明前端包已刷新，脚本已改为 `build --no-cache web` 和 `up -d --no-deps --force-recreate web`；重新部署后首页资产为 `/assets/index-DBCxlRUQ.js`，资源包包含“刷视频配置”“商品卡养号配置”“目标直播间评论配置”，且不再包含“本次执行账号”“真实评论审批”。

后续处理：

1. 自动提交脚本默认只在传 `-Paths` 或 `-All` 时暂存，避免误提交未跟踪资料；使用时仍要先看 `git status --short`。
2. 部署脚本要求本机 Python 可用且安装 `paramiko`，服务器必须已有 `.env.production`、Docker Compose 和 `rsync`。
3. USB 安装脚本不绕过 MIUI 安全守护；受限设备需要在手机上确认安装器，或提前在开发者选项中打开“USB 安装/USB 调试（安全设置）”。

## 14. 2026-07-09 1.0.36 APK USB 安装与 MIUI 安全守护卡点

现象：

1. 用户要求用 `scripts/install-apk-usb.ps1` 安装 `1.0.36` APK，并验证是否能绕过 MIUI 安全守护完成卸载和安装。
2. 当前 ADB 授权设备为 `3f79ff76`、`44a75f5b`、`8185c828`、`ce3eb0bf`、`daede198`。
3. `aapt` 不能直接读取中文 APK 路径，报 `Illegal byte sequence`。
4. 首次执行脚本时，`3f79ff76` 已卸载旧包，但 `adb install` 被 MIUI 拦截；脚本本应兜底推送并拉起安装器，却被 PowerShell native stderr 捕获提前中断。
5. MIUI 安装器可通过系统文件管理器点击 APK 进入，但仍需要在可见授权页选择“本次允许”和“安装”。

影响：

1. 被 MIUI 拦截的手机在卸载后如果未人工确认安装，会处于未安装 `com.agri.video.collector` 的状态。
2. 原脚本没有完整覆盖 ADB stderr + 非零退出码场景，导致兜底流程没有稳定执行。
3. 不能把“拉起系统安装器待确认”误记为“已安装成功”。

根因：

1. MIUI 安全守护对部分设备的 ADB 安装路径返回 `INSTALL_FAILED_USER_RESTRICTED: Install canceled by user`，即使脚本执行了开发者安装准备命令，也不能保证绕过。
2. `install-apk-usb.ps1` 设置 `$ErrorActionPreference = "Stop"`，`adb install` 写 stderr 时会被 PowerShell 当作错误记录处理，导致 `Invoke-Adb -IgnoreError` 还没返回就终止。
3. `aapt` 对中文路径兼容性差，需要使用临时 ASCII 路径校验 APK。

处理结果：

1. 已将 `dist/apk/燎原星火-1.0.36-inrt.apk` 复制到临时 ASCII 路径后校验，确认包名 `com.agri.video.collector`、版本 `1.0.36`、构建号 `72`，SHA256 为 `A33C09DAF14DC0DC4E02A53CC5F62548C0F44ED8C757990A47A9BBECA3B049B0`。
2. 已修复 `Invoke-Adb`：执行 ADB 时临时把 `$ErrorActionPreference` 降为 `Continue`，并在存在 `PSNativeCommandUseErrorActionPreference` 时临时关闭，执行后恢复原值。
3. 已补充回归测试，约束 USB 安装脚本必须显式处理 native stderr 捕获，避免再次跳过 MIUI fallback。
4. 已重新执行安装脚本：`ce3eb0bf` ADB 卸载并安装成功，当前为 `1.0.36/72`。
5. `3f79ff76`、`44a75f5b`、`8185c828`、`daede198` 均被 MIUI 拦截，改为把 APK 推送到 `/sdcard/燎原星火/liaoyuanxinghuo-1.0.36-inrt.apk`，通过 `com.android.fileexplorer/.activity.FileActivity` 打开文件管理器，点击 APK 后在可见授权页选择“本次允许”和“安装”，最终安装成功。

验证结果：

1. 已执行 `bun test account-data-platform/apps/web/tests/live-targets-page.test.js`，新增脚本回归测试通过。
2. 已执行 PowerShell 解析检查，`scripts/install-apk-usb.ps1` 语法通过。
3. 已执行 `dumpsys package com.agri.video.collector`：`3f79ff76`、`44a75f5b`、`8185c828`、`daede198`、`ce3eb0bf` 均返回 `versionName=1.0.36`、`versionCode=72`。

后续处理：

1. 后续批量安装 MIUI 受限设备时，优先使用“推送 APK 到手机存储 -> 打开系统文件管理器 -> 点击 APK -> 可见授权页确认”的路径，或把该路径固化为脚本 fallback。
2. 如要求真正 ADB 全自动安装，需要逐台手机在开发者选项中开启“USB 安装/USB 调试（安全设置）”，否则脚本只能拉起安装器或文件管理器，不能隐藏绕过安全确认。

## 15. 2026-07-10 APK 与安装占用过大卡点

现象：

1. 用户反馈手机端 App 安装占用达到 270MB 级别，与“只是一个 AutoX.js 小脚本”的预期不一致。
2. `1.0.37` APK 本体约 63.99MiB，手机侧 `dumpsys diskstats` 显示部分设备 App 占用约 180-250MB，Data 仅约 2-3MB。
3. `mobile-agent/autojs` 源码约 1.38MB，APK 内项目脚本 `assets/project` 约 856KB，脚本本身不是体积大头。

影响：

1. 多台手机批量安装时，下载、传输、安装和系统编译耗时都会变长。
2. 低存储设备可能因为 APK 本体、native 解压目录和 oat/odex 编译产物叠加导致空间压力。
3. 用户容易把安装占用误判为日志、缓存或自动更新包堆积，排查方向会偏离 APK runtime 体积。

根因：

1. APK 集成完整 AutoJs6 runtime，Dex、native libraries、assets 模型数据是主要体积来源。
2. `project.json` 虽已有 `optimization.removeOpenCv=true`，但原 APK 打包脚本未读取这些优化字段，仍把 `libopencv_java4.so` 注入 APK。
3. AutoJs6 构建默认 `release.isMinifyEnabled=false`，且 ProGuard 规则较宽；直接强制开启 R8/minify 存在反射、脚本运行和无障碍链路被误裁剪风险。

处理结果：

1. `scripts/package-autojs-apk.ps1` 新增读取 `optimization` 配置，按需跳过 OpenCV、MediaInfo、条码扫描、pngquant、terminal native、OpenCC 等 native library 注入。
2. 打包签名前删除不需要的 APK assets，包括 `assets/project/tests/`、条码模型、OpenCC 数据、Jieba/Pinyin 数据和 Android devices DB。
3. `scripts/package-autojs.ps1` 同步按 `excludeTests` 排除热更新包中的 `tests` 目录。
4. 新增 `enableMinify`/`-EnableMinify` 开关，但默认关闭；后续只有在单独完成 AutoJs6 真机回归后再考虑启用。
5. 临时审计包体积从 63.99MiB 降到 43.67MiB，减少约 20.32MiB；跳过 7 个 native library，删除 46 个 assets entries。

验证结果：

1. 已执行 `scripts/package-autojs-apk.ps1` 和 `scripts/package-autojs.ps1` 的 PowerShell 解析检查。
2. 已执行 `node --check mobile-agent/autojs/main.js` 和 `project.json` JSON 解析检查。
3. 已执行 `scripts/package-autojs-apk.ps1 -SkipBuild`，确认 staging 项目不再包含 `tests`，且 `build.gradle.kts` 的 release minify 默认保持 `false`。
4. 已用临时 dummy `MOBILE_REGISTRATION_SECRET` 完整构建审计 APK，确认裁剪输出和体积分布正常。

后续处理：

1. 需要使用真实 `MOBILE_REGISTRATION_SECRET` 重新打正式 APK，不能发布或安装本轮 dummy secret 审计包。
2. 正式包安装后必须做启动、悬浮窗、无障碍、截图授权、OCR fallback、商品卡搜索、直播进房和评论链路真机回归。
3. 如真机日志出现 OpenCV、MediaInfo、OpenCC、条码扫描或 terminal native 相关缺库错误，按具体能力恢复对应 `optimization` 裁剪项，不要一次性回退全部瘦身规则。

## 16. 2026-07-11 直播入口方案原稿被文档收敛误删

现象：

1. 用户此前放在 `docs/方案设计/功能方案/` 的直播卡片识别原稿在当前目录中不可见。
2. Git 历史显示《直播入口识别与评论链路稳定方案》于 2026-07-01 创建，并在 2026-07-03 的提交 `80ff3e0` 中被文档收敛操作直接删除。

影响：

1. 用户手写需求、截图场景和直播卡片识别实施清单失去当前工作区入口。
2. 后续方案虽然吸收了部分结论，但不能替代原稿的需求追溯价值。

根因：

1. 文档收敛时把“已有后续方案”误判为可以删除原稿，没有先确认原稿是否需要作为用户需求证据保留。
2. 删除前未建立历史原稿入口，也未在项目记录中明确原稿去向。

处理结果：

1. 从首次提交 `c5c33d4` 恢复 `docs/方案设计/功能方案/直播入口识别与评论链路稳定方案.md`。
2. 恢复文件保持原始 blob `44b84750b42b798f8dfa8b37b62735673571e3ad`，未修改原稿正文。
3. 在项目记录中补充恢复记录和原稿入口，并标注该文档用于历史追溯，不替代当前修复闭环。

验证结果：

1. 已确认恢复文件共 438 行，标题、用户截图场景、直播卡片识别说明和任务清单完整。
2. 已用 `git hash-object` 校验恢复文件与 2026-07-01 首次提交版本一致。

后续处理：

1. 后续整理用户原始需求文档时只新增现行入口或归档说明，不直接删除原稿。
2. 必须删除或合并用户原稿时，先确认保留策略，并在项目记录中留下可追溯路径。

## 17. 2026-07-12 1.0.38 APK 启动缺少 jackpal native library

现象：

1. 用户提供 `docs/info` 崩溃栈，`1.0.38/74` 在 Android 10 手机上启动后直接抛出 `Uncaught Exception`。
2. 栈信息显示 `java.lang.UnsatisfiedLinkError`，`PathClassLoader` 在 APK 的 `lib/arm64-v8a` 中找不到 `libjackpal-termexec2.so`。
3. 崩溃发生在 `jackpal.androidterm.TermExec.<clinit>`，调用链进入 `org.autojs.autojs.runtime.api.Shell` 初始化阶段，尚未进入商品卡业务任务。

影响：

1. `1.0.38` APK 不能作为真机测试包继续验证商品卡养号和目标直播评论。
2. 该问题和后台任务配置、商品卡搜索逻辑无关，属于 APK runtime native library 打包问题。

根因：

1. 7 月 10 日 APK 瘦身把 `optimization.removeTerminalNative=true` 纳入正式打包。
2. `scripts/package-autojs-apk.ps1` 根据该配置排除了 `libjackpal-termexec2.so` 和 `libjackpal-androidterm5.so`。
3. AutoJs6 启动时会初始化 `Shell`，即使前面已把 root shell 改为非 root，仍需要 jackpal native library 创建 shell session，因此该库不能裁剪。

处理结果：

1. `mobile-agent/autojs/project.json` 版本提升到 `1.0.39/75`，并将 `optimization.removeTerminalNative` 改为 `false`。
2. `scripts/package-autojs-apk.ps1` 改为忽略 `removeTerminalNative` 的排除请求，只打印提示，不再删除 `libjackpal-*`。
3. 其他瘦身项保持不变，继续保留 OpenCV、MediaInfo、条码扫描、pngquant、OpenCC、Jieba/Pinyin 数据和 Android devices DB 的裁剪。

验证结果：

1. 已重新构建 `dist/apk/燎原星火-1.0.39-inrt.apk`，SHA256 为 `398a53d12e97227eaa59d53c7c96daab23a84c7cd70b5213295958425a7f3085`。
2. 已确认包内包含 `lib/arm64-v8a/libjackpal-termexec2.so` 和 `lib/arm64-v8a/libjackpal-androidterm5.so`。
3. 已执行 `apksigner verify --verbose --print-certs`、`zipalign -c -p 4`、`aapt dump badging`，确认签名有效、zipalign 通过、包名 `com.agri.video.collector`、版本 `1.0.39/75`。
4. 仍需要重新安装 `1.0.39` 后做启动、无障碍、悬浮窗、后台注册和商品卡任务真机回归。

## 18. 2026-07-12 商品卡直播卡停留与直播养号2完成条件不严

现象：

1. 对照 `商品卡片直播间评论.md` 后发现，商品卡浏览中遇到直播卡时只固定停留约 1.8 秒，不能体现“存在直播卡片就进去看直播”的业务动作。
2. V2 `live_nurture` 在扫描 100 个候选后即使累计观看时间不足，也会上报 `stage_live_nurture_completed`，可能把没有看满 70-100 分钟的任务误判为成功。

影响：

1. 商品卡养号阶段可能表现为闪进闪出直播卡，真机录屏不符合业务预期。
2. 直播养号2可能在未命中足够关键词直播间时误闭环，后台验收日志会把失败场景显示成成功。

根因：

1. `douyin.browseCommerceCards()` 打开商品卡内直播后使用固定短等待，没有读取商品详情停留配置。
2. `runV2LiveNurture()` 只用候选扫描上限保护循环，没有在退出循环后校验 `completedWatchMs >= plannedWatchMs`。

处理结果：

1. 商品卡直播卡停留改为由 runner 传入，默认跟随 `productCardDwellSeconds`，并支持暂停/停止中断。
2. 直播养号2首次进入阶段时固定累计观看目标并写入 checkpoint；退出循环时未看满则按 `live_nurture_watch_incomplete` 失败收口。
3. 手机端版本提升到 `1.0.40/76`，本地 APK 已生成，等待安装后真机回归。

验证结果：

1. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，覆盖商品卡直播停留传参、直播养号2看满成功和未看满失败。
2. 已执行 `node --check mobile-agent/autojs/app/commerce-card-live-runner.js`、`node --check mobile-agent/autojs/platforms/douyin.js`、`node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`。
3. 已用生产服务器当前 `MOBILE_REGISTRATION_SECRET` 重新构建 `dist/apk/燎原星火-1.0.40-inrt.apk`，SHA256 为 `52FE414DA2D786A501AF843BD6D4B9C6E45F9EA00CE1050DAC2357B94BEEA806`；`apksigner` 验证 v2/v3 签名通过，`zipalign -c -p 4` 通过，`aapt dump badging` 确认包名 `com.agri.video.collector`、版本 `1.0.40/76`。

## 19. 2026-07-12 商品卡 V2 启动弹窗误暴露目标、账号和审批字段

现象：

1. 后台任务调度页点击“目标直播间评论（商品卡片养号）”后弹出启动表单。
2. 表单要求选择“直播目标”、填写“本次执行账号”、选择“真实评论审批”，并在无 V2 目标时提示“暂无可运行的 V2 目标配置”。
3. 用户确认目标直播间和关键词应通过工作台“配置”维护，启动任务时不应再进行目标、账号或审批选择。

影响：

1. 运营容易把目标配置误解为启动时临时选择，和“先配置、再启动任务”的后台口径冲突。
2. 未配置审批时手机端 V2 评论会被 `execution_approval_missing` 阻断，导致目标直播间评论任务只能识别和跳过，无法按后台配置执行评论。
3. 工作台配置弹窗仍是单页直播评论表单，不能清楚区分刷视频、刷直播、搜直播评论、商品卡养号和目标直播间评论配置。

根因：

1. 任务调度页把商品卡 V2 启动做成了二次表单，并在前端主动查询 `liveTargets` 和 `commerceCardExecutionApprovals`。
2. API 契约和任务编排服务仍要求 V2 `START` 必须携带 `targetId` 和预期账号。
3. 手机端 `sendV2Comment()` 把 `executionApprovalId` 作为发送前置条件，而不是把后台目标配置和全局真实发送开关作为控制点。

处理结果：

1. 商品卡 V2 启动改为按钮直接下发 assignment，后台根据设备绑定或默认绑定自动选择商品卡 V2 目标。
2. 工作台配置弹窗改为五个配置页签，并在填写目标直播间后同步维护当前设备的商品卡 V2 直播目标、功能配置和设备绑定。
3. 预约评论动作不再强制依赖审批；无审批时仍保留动作审计和短时许可，旧审批路径继续保留额度和冷却校验。
4. 手机端版本提升到 `1.0.41/77`，已使用既有正式 APK 中的同一注册密钥重新打包，密钥未写入仓库、文档或终端输出。

验证结果：

1. 已执行 `bun run typecheck`、`bun test`，后台和前端测试通过。
2. 已执行 `node --check mobile-agent/autojs/app/commerce-card-live-runner.js`、`node --check mobile-agent/autojs/core/uploader.js` 和 `project.json` JSON 解析。
3. 已生成 `dist/apk/燎原星火-1.0.41-inrt.apk`，SHA256 为 `444C4E8B89018431BECDFC696F2055964429239DE30E3D10CA26EFFC97A8821E`；`apksigner` 验证 v2/v3 签名通过，`zipalign -c -p 4` 通过，`aapt dump badging` 确认包名 `com.agri.video.collector`、版本 `1.0.41/77`。

## 20. 2026-07-13 商品卡养号配置默认保存报参数不合法

现象：

1. 工作台配置弹窗已经显示五个页签。
2. 在“商品卡养号配置”页签保持默认值保存，页面提示“配置保存失败：参数不合法”。
3. 默认值包括商城搜索关键词 `夏橙`、商品卡匹配词 `秭归/夏橙`、每轮浏览 15 分钟、最多 3 轮、单卡停留 120 秒。

影响：

1. 运营无法保存商品卡养号和目标直播间评论的设备级配置。
2. 即使前端 UI 已更新，任务启动前置配置仍无法落库，后续下发 `commerce_card_live_comment` 会缺少目标配置。

根因：

1. 前端保存 `p3ExtensionsConfig.commerceCardLiveComment` 时会写入 `productKeywords` 和 `productCardDwellSeconds`。
2. 后端 `p3ExtensionsConfigSchema` 对 `commerceCardLiveComment` 使用 `.strict()`，但旧白名单没有这两个字段，导致 PATCH `/devices/:deviceCode/task-config` 在校验阶段返回参数不合法。

处理结果：

1. `account-data-platform/packages/types/src/api/admin.ts` 已补齐 `productKeywords` 和 `productCardDwellSeconds`。
2. `live-target-config.test.ts` 已覆盖带商品卡默认字段的设备配置 payload。

验证结果：

1. 已执行 `bun test account-data-platform/apps/api/src/services/live-target-config.test.ts`，9 个测试通过。
2. 已执行 `bun run --cwd account-data-platform typecheck`，类型检查通过。
3. 已按 `9f441f2` 强制重建并重启生产 API/Web，`/health`、`/ready` 正常。
4. 生产 V2 启动探针不再返回 `targetId/expectedAccountName` 校验错误，商品卡配置保存探针不再返回 `productKeywords/productCardDwellSeconds` 参数非法。
5. 后续需要在真实设备 `device_0e346fca2fc348c7` 上重新点击保存，确认页面弹出“配置已保存”。

## 21. 2026-07-13 商品卡养号按钮点击后无命令下发

现象：

1. 工作台配置保存问题已解决后，点击任务调度页“商品卡养号”仍没有看到手机执行。
2. 生产 API 访问日志中能看到两次 `POST /api/v1/admin/task-assignments`，返回状态均为 `409`。
3. 手机端持续请求 `/mobile/commands?deviceId=device_0e346fca2fc348c7`，但后台日志一直显示 `commandCount:0`。

影响：

1. 前端按钮点击已到后台，但后台在创建 assignment 和 START command 前拒绝请求，手机端没有任何可执行命令。
2. 运营容易误判为按钮无效、手机离线或商品卡目标配置未保存。

根因：

1. 生产库 `feature_rollout_controls` 为空，`db:push` 只同步表结构，不会执行历史 SQL migration 里的默认数据插入。
2. 任务编排启动商品卡 V2 前调用 `assertCommerceCardWorkflowV2Allowed()`，缺少 `commerce_card_workflow_v2` 控制项时返回 `FEATURE_CONTROL_MISSING`，最终映射为 `409`。
3. 代码里 `activationReadyByFeature.commerce_card_workflow_v2=false` 仍停留在阶段 A 风险保护状态，即使补齐控制项也无法真正启用。
4. 当前设备还有 2026-07-10 遗留的 `ACKED` assignment；前端不把 `ACKED` 当活跃任务，但数据库唯一索引仍把它当非终态运行，可能继续阻止新 assignment 插入。

处理结果：

1. API 仓储层新增默认门禁补齐，读取列表、读取单项和更新控制项前都会确保 `commerce_card_workflow_v2`、`commerce_card_real_comment` 两行存在。
2. `commerce_card_workflow_v2` 默认启用，但仍要求手机版本不低于 `1.0.41` 且上报 `workflow_v2/checkpoint_v2/pause_resume/stable_room_key` capability。
3. `commerce_card_real_comment` 默认关闭，只把代码级 `activationReady` 打开，真实发送仍必须由全局开关和目标配置显式开启。
4. 新建 START assignment 前自动把同设备遗留 `ACKED` 收口为 `SUPERSEDED`，避免旧协议残留状态继续占用每设备单一运行约束。

验证结果：

1. 已执行 `bun run --cwd account-data-platform typecheck`，类型检查通过。
2. 已执行 `bun test account-data-platform/apps/api/src/services/task-assignment-types.test.ts account-data-platform/apps/api/src/services/live-target-config.test.ts`，14 个测试通过。
3. 已按本轮修复代码重建并重启生产 API，`/health`、`/ready` 正常。
4. 已调用生产门禁接口触发默认控制项补齐，`commerce_card_workflow_v2` 为启用状态，`commerce_card_real_comment` 为默认关闭状态。
5. 已把生产库 3 条历史 `ACKED` assignment 标记为 `SUPERSEDED`，当前没有非终态 assignment 阻塞新启动。

## 22. 2026-07-14 商品卡养号浏览跑偏与状态版本冲突

现象：

1. 点击“商品卡养号”后，手机进入商城并开始浏览夏橙相关商品卡。
2. 日志显示浏览到第 4 个商品后页面出现 `返回直播`、`静音/取消静音`，随后变成 `赞/评论/收藏/分享/购物/视频同款` 等视频页结构。
3. 后台 STOP 已下发后，手机仍继续商品卡浏览循环，最终上报检查点时返回 `ASSIGNMENT_STATE_VERSION_CONFLICT`。

影响：

1. 商品卡养号阶段可能离开商城商品卡路径，导致 15 分钟流程失真。
2. 后台 STOP/PAUSE 在商品卡详情长停留期间不能及时生效，容易把正常停止表现成任务失败。
3. 商品卡养号后的直播目标门禁候选数量和用户文档要求的随机 10 至 20 个直播间不一致。

根因：

1. `browseCommerceCards()` 只按关键词判断是否继续点击，没有判断当前页面是否仍为商城搜索结果或商品详情。
2. 商品详情内触发直播/视频入口后，返回路径可能落到视频页，原逻辑没有恢复到商城搜索。
3. 商品卡浏览内部的可中断等待只检查本地 `floatyControl`，没有主动拉取后台控制命令；STOP 先把服务端 assignment 版本推进，手机用旧版本上传检查点时被乐观锁拒绝。

处理结果：

1. 商品卡浏览增加商城上下文检测，识别到视频页/直播页结构时重新进入商城搜索。
2. 商品卡浏览和直播板块目标扫描的中断检查会主动触发后台控制命令轮询。
3. 商品卡养号完成商品卡流程后的直播目标门禁改为随机 10 至 20 个候选，未命中按失败终止。
4. 手机端版本提升到 `1.0.42/78`，用于重新打包并区分真机验证日志。

验证结果：

1. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，测试通过。
2. 已执行 `node mobile-agent/autojs/tests/control-loop.test.js`，测试通过。
3. 已执行 `bun run --cwd account-data-platform typecheck`，类型检查通过。
4. 已执行 `node --check mobile-agent/autojs/app/commerce-card-live-runner.js`、`node --check mobile-agent/autojs/platforms/douyin.js` 和 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，语法检查通过。
5. 已生成 `dist/apk/燎原星火-1.0.42-inrt.apk`，SHA256 为 `21F0469E056FED5254D4CB015199381182A4D0FF0DDC63581D09B465A663BAB5`；`apksigner verify`、`zipalign -c -p 4` 和 `aapt dump badging` 校验通过，确认包名 `com.agri.video.collector`、版本 `1.0.42/78`。
6. 已上传热更新包到 `http://106.54.41.106:18080/downloads/agent/AgriVideoCollector-autojs-1.0.42.zip`，SHA256 为 `6F276EBB8E92455ED1107B709095DBAD14190398CF0C577ECA1EBE42A7DF8B8A`；已上传 APK 到 `http://106.54.41.106:18080/downloads/apk/liaoyuanxinghuo-1.0.42-inrt.apk`，公网下载入口返回 `200`。
7. 已发布 stable Agent 版本 `1.0.42`，生产 `/health`、`/ready` 正常，后台 Agent 版本列表最新记录为 `1.0.42`。

## 23. 2026-07-14 商品卡结果页被误判跑偏

现象：

1. 用户复测 `1.0.42` 商品卡养号，手机已进入商城、搜索 `夏橙` 并点击“商品”页签。
2. 手机随后没有继续浏览商品卡，而是直接收尾退出，后台显示任务失败或结束。
3. 后台完整日志最后失败原因为 `commerce_context_recover_failed`。

影响：

1. 商品卡养号在商品搜索结果页第一轮就提前失败，无法进入 15 分钟商品卡浏览。
2. 用户无法从 PC/手机文件管理器直接看到 App 私有目录中的完整本地日志，只能依赖后台上传日志或 logcat。

根因：

1. `1.0.42` 新增的跑偏检测没有把 `综合/销量/直播/筛选/好评多` 这类商品搜索结果页特征作为最高优先级。
2. 商品结果页仍可能残留 `评论/收藏/分享` 等文本，`isCommerceVideoDriftText()` 先命中后把正常商品页误判为视频页跑偏。
3. `recoverCommerceCardSearch()` 在当前仍处于搜索结果页时没有优先重新点击“商品”页签，而是直接尝试重新找底部商城入口，导致恢复失败。
4. 安装版 AutoJs6 脚本目录位于 App 私有目录，`config.output.baseDir` 跟随脚本目录后，日志落到 `/data/user/0/com.agri.video.collector/files/project/datasource/运行日志/`，无 root 设备无法用文件管理器或普通 ADB 读取。

处理结果：

1. 商品卡上下文识别新增 `综合 + 销量/筛选/好评多/直播` 的合法商品结果页判断。
2. 合法商品上下文优先于视频跑偏判断，正常商品页不再因 `评论/收藏/分享` 残留文本被误判。
3. 商品卡恢复逻辑在当前仍是搜索结果页时优先重新点击“商品”页签，失败后才重新进入商城搜索。
4. 手机端运行输出目录优先选择可见内部存储，预期日志路径为 `内部存储/燎原星火/datasource/运行日志/2026-07-14.log`；如外部存储不可写，再回退到 App 私有目录。
5. 手机端版本提升到 `1.0.43/79`，用于重新打包和真机复测。
6. 已上传 APK 到 `http://106.54.41.106:18080/downloads/apk/liaoyuanxinghuo-1.0.43-inrt.apk`，上传热更新包到 `http://106.54.41.106:18080/downloads/agent/AgriVideoCollector-autojs-1.0.43.zip`，并发布 stable Agent 版本 `1.0.43`。

验证结果：

1. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，测试通过。
2. 已执行 `node mobile-agent/autojs/tests/control-loop.test.js`，测试通过。
3. 已执行 `node --check mobile-agent/autojs/platforms/douyin.js`、`node --check mobile-agent/autojs/main.module.js`、`node --check mobile-agent/autojs/watchdog.js` 和 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，语法检查通过。
4. 已执行 `apksigner verify --verbose --print-certs`、`zipalign -c -p 4` 和 `aapt dump badging`，确认 `dist/apk/燎原星火-1.0.43-inrt.apk` 包名为 `com.agri.video.collector`、版本为 `1.0.43/79`，APK SHA256 为 `E6F2C8B9E95F632E26443A72541BDC2D5C45A2F5F048D4ECA2996796428018EB`。
5. 热更新包 SHA256 为 `982CE09CF9860CDA82E194918374D7BA5084EC872CF59FF61991F2F927BFA502`；生产 `/health`、`/ready` 正常，Agent 版本列表最新记录为 `1.0.43`。

## 24. 2026-07-14 商品卡启动时恢复在历史搜索结果页

现象：

1. 用户复测 `1.0.43` 后反馈“进去抖音就退了”。
2. 通过 ADB 拉取 `/sdcard/燎原星火/datasource/运行日志/2026-07-14.log`，15:08 日志显示已收到商品卡养号 START 指令并打开抖音。
3. 抖音打开后当前 Activity 为 `com.ss.android.ugc.aweme.search.activity.SearchResultActivity`，页面文本已包含 `夏橙`、`综合/销量/直播/筛选/好评多`，但脚本记录 `未找到商城入口`，随后以 `commerce_search_failed` 收尾并退出抖音。

影响：

1. 当抖音恢复到上一次搜索结果页时，商品卡养号还没有进入浏览阶段就提前失败。
2. 用户看到的表现是“进入抖音后马上退出”，容易误判为抖音闪退或后台命令未下发。

根因：

1. `1.0.43` 只处理了浏览过程和跑偏恢复过程中的商品结果页识别。
2. 首次 `openCommerceCardSearch()` 仍然固定先找底部“商城”入口，没有先判断当前页是否已经是目标关键词的商品搜索结果。
3. 抖音从历史搜索结果页恢复时，页面本身可继续执行，但底部商城入口不可见，导致入口函数错误返回失败。

处理结果：

1. `openCommerceCardSearch()` 新增 `tryReuseCommerceCardSearchResult()`，启动前优先复用当前目标关键词搜索结果页。
2. 如果找不到商城入口，会再次检查当前页是否为可复用商品搜索结果，避免从 `SearchResultActivity` 直接失败。
3. 手机端版本提升到 `1.0.44/80`，用于重新打包、发布和真机复测。
4. 已上传 APK 到 `http://106.54.41.106:18080/downloads/apk/liaoyuanxinghuo-1.0.44-inrt.apk`，上传热更新包到 `http://106.54.41.106:18080/downloads/agent/AgriVideoCollector-autojs-1.0.44.zip`，并发布 stable Agent 版本 `1.0.44`。

验证结果：

1. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，测试通过。
2. 已执行 `node mobile-agent/autojs/tests/control-loop.test.js`，测试通过。
3. 已执行 `node --check mobile-agent/autojs/platforms/douyin.js` 和 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，语法检查通过。
4. 已执行 `apksigner verify --verbose --print-certs`、`zipalign -c -p 4` 和 `aapt dump badging`，确认 `dist/apk/燎原星火-1.0.44-inrt.apk` 包名为 `com.agri.video.collector`、版本为 `1.0.44/80`，APK SHA256 为 `27FC71E6CD0699308277EF22F3A4D70978BDF3D8175141947ABE4B911636C265`。
5. 热更新包 SHA256 为 `5433614ABFC3FEDA6B603B4DDA74518E5D2C3C3883D259A7750BAAE2EA5CEDA9`；生产 `/health`、`/ready` 正常，下载入口返回 `200`，Agent 版本列表最新记录为 `1.0.44`。
## 25. 2026-07-14 商品卡养号门禁未命中后提前失败

现象：
1. 用户导出的 `docs/info` 显示，商品卡养号任务在 17:12 左右完成第 8 个商品卡详情浏览后，进入直播板块目标门禁。
2. 17:13 左右未找到目标直播间后，任务直接上报 `product_target_not_found` 并退出抖音。
3. 该行为与 `商品卡片直播间评论.md` 中“商品卡流程后识别目标直播间；未识别到则继续养号流程，最多循环 3 次”的要求不一致。

影响：
1. 商品卡养号任务只跑一轮就失败，无法按文档完成最多 3 轮商品卡养号。
2. 真机表现为刷完商品详情后进入直播板块，未命中目标就退出，容易误判为直播板块流程本身错误。

根因：
1. `runV2ProductNurture()` 在 `openV2TargetLiveGate()` 未命中时立即返回 `product_target_not_found`。
2. 旧测试断言也把一轮失败当成正确结果，导致实现偏离文档后没有被测试拦住。
3. 商品卡浏览缺少“按单轮时间跑满”的显式参数，存在按卡片数提前结束的风险。

处理结果：
1. V2 商品养号目标门禁未命中时继续下一轮商品卡养号，达到 `productNurtureMaxRounds` 后才失败。
2. V2 商品卡浏览传入 `requireFullScan=true`，浏览循环在单轮时间内持续执行并继续滑动结果页。
3. 目标门禁候选数改为按文档配置的 20-30 范围，默认 25。
4. `commerce-card-live-runner.test.js` 已更新为“跑满配置轮次后失败”的断言。

验证结果：
1. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，测试通过。
2. 已执行 `node --check mobile-agent/autojs/app/commerce-card-live-runner.js` 和 `node --check mobile-agent/autojs/platforms/douyin.js`，语法检查通过。
## 26. 2026-07-14 商品卡详情页推荐标题缺失导致后续商品识别不稳

现象：
1. 真机截图显示搜索 `夏橙` 后，“全部/综合”结果页已经展示商品卡瀑布流。
2. 进入商品详情后，下滑到详情或售后说明区域，再继续下滑会出现后续商品卡列表。
3. 抖音有时不展示“你可能还会喜欢”标题，但商品卡列表本身仍然存在。

影响：
1. 如果脚本把“你可能还会喜欢”作为推荐区必要标识，会在标题缺失时停止后续商品卡跳转。
2. 如果搜索结果页没有“商品”标签但有“店铺”标签，旧逻辑存在误点店铺页的风险，偏离商品卡养号流程。

根因：
1. `clickCommerceResultTabIfVisible()` 同时匹配 `商品|店铺`，没有把店铺排除在商品卡流程之外。
2. 商品详情页内的推荐商品继续点击条件依赖 `recommendationSignals`，没有直接识别后续商品卡列表。

处理结果：
1. 搜索结果页只点击“商品”；没有“商品”时保留或点击“全部/综合”，不再点击“店铺”。
2. 商品详情页新增后续商品卡识别：在屏幕中下部查找命中商品词的商品卡节点，并用价格、券后价、已售、进店、包邮、发货等商品特征辅助判断。
3. “你可能还会喜欢”保留为可用信号，但不再是继续点击后续商品卡的必要条件。

验证结果：
1. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，测试通过。
2. 已执行 `node --check mobile-agent/autojs/platforms/douyin.js` 和 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，语法检查通过。

## 27. 2026-07-14 商品卡养号误切其他结果标签和直播卡

现象：
1. 真机截图显示商城搜索结果页直接在 `全部/综合` 下展示商品卡瀑布流。
2. 抖音 UI 有时显示“全部”，有时显示“综合”，但商品卡都在当前结果流中。
3. 用户明确要求商品卡养号不要点击其他标签，不管直播间卡片或其他内容，只刷商品卡片。

影响：
1. 如果脚本主动切到“商品”或其他标签，可能离开当前可用商品卡结果流。
2. 如果商品浏览阶段进入直播卡，商品卡养号会偏离“只刷商品卡片”的业务口径。

根因：
1. `clickCommerceResultTabIfVisible()` 在 `1.0.46` 中仍优先匹配并点击“商品”标签。
2. `browseCommerceCards()` 的商品详情浏览阶段仍保留直播卡进入逻辑，虽然用于早期商品卡直播兼容，但不适合 V2 商品卡养号阶段。

处理结果：
1. 商城搜索结果页只保留 `全部/综合` 标签识别，不再主动点击“商品”“店铺”或“直播”。
2. V2 商品卡养号传入 `skipLiveCards=true`，商品卡浏览阶段遇到直播信号也不进入直播卡。
3. 手机端版本提升到 `1.0.47/83`，用于重新打包、发布和真机复测。

验证结果：
1. 已补充 `commerce-card-live-runner.test.js` 静态断言，防止商品卡入口重新匹配 `商品` 标签。
2. 已补充 V2 商品卡养号调用断言，确认传入 `skipLiveCards=true`。
3. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 和 `node mobile-agent/autojs/tests/control-loop.test.js`，测试通过。
4. 已执行 `node --check mobile-agent/autojs/platforms/douyin.js`、`node --check mobile-agent/autojs/app/commerce-card-live-runner.js` 和 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，语法检查通过。
5. 已打包并发布 `1.0.47/83`，APK 地址为 `http://106.54.41.106:18080/downloads/apk/liaoyuanxinghuo-1.0.47-inrt.apk`，热更新 manifest 地址为 `http://106.54.41.106:18080/downloads/agent/AgriVideoCollector-autojs-1.0.47.json`。

## 28. 2026-07-14 商品卡养号误点相关搜索和视频标题

现象：
1. 用户反馈 `1.0.47` 商品卡养号变成刷视频，并把真机日志粘贴到 `docs/info`。
2. 日志显示后台下发的 `searchKeywords` 仍为 `["夏橙"]`，并非后台搜索词被改。
3. 23:29 后脚本点击了 `综合` 标签；随后点击了“夏橙湖北秭归”相关搜索词、“走进橙都...”视频标题、评论区文本等非商品卡节点。

影响：
1. 商品卡养号可能从商城商品卡流跑到视频流、图文详情或评论区。
2. 用户看到的表现是搜索结果和后续浏览内容变成视频，不再按截图中的底部推荐商品卡继续刷。

根因：
1. `clickCommerceResultTabIfVisible()` 使用 `全部|综合` 同一个正则，控件查找可能先命中 `综合`，未强制优先 `全部`。
2. 商品卡点击只校验 `夏橙/秭归` 等关键词，没有要求节点或父级上下文包含价格、已售、包邮、发货等商品卡信号。
3. 点击逻辑没有排除 `相关搜索/大家都在搜/最近看过/评论/相关推荐/关注/回复/#/@/全屏观看` 等视频、评论和搜索建议形态。

处理结果：
1. 搜索结果入口改为先找 `全部`，只有找不到 `全部` 时才回退 `综合`。
2. 商品卡候选新增商品信号白名单，必须命中 `¥/券后价/到手价/已售/包邮/发货/现货/进店/店铺/水果/现摘/斤/箱` 等商品信号。
3. 商品卡候选新增非商品黑名单，命中相关搜索、视频标题、评论区和话题作者标识时不点击。
4. 手机端版本提升到 `1.0.48/84`，用于重新打包、发布和真机复测。

验证结果：
1. 已补充 `commerce-card-live-runner.test.js` 静态断言，确认 `全部` 优先于 `综合`。
2. 已补充商品卡点击断言，确认存在商品信号校验和非商品黑名单。
3. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 和 `node mobile-agent/autojs/tests/control-loop.test.js`，测试通过。
4. 已执行 `node --check mobile-agent/autojs/platforms/douyin.js`、`node --check mobile-agent/autojs/app/commerce-card-live-runner.js` 和 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，语法检查通过。
5. 已打包并发布 `1.0.48/84`，APK 地址为 `http://106.54.41.106:18080/downloads/apk/liaoyuanxinghuo-1.0.48-inrt.apk`，热更新 manifest 地址为 `http://106.54.41.106:18080/downloads/agent/AgriVideoCollector-autojs-1.0.48.json`。

## 29. 2026-07-15 商品卡养号入口固定为全部

现象：
1. 用户手动搜索确认当前抖音商城搜索结果一直显示并进入 `全部`。
2. 用户要求暂时不处理 `综合` 场景，只要保证一直刷商品卡，不再刷视频、相关搜索或评论区。

处理结果：
1. `clickCommerceResultTabIfVisible()` 去掉 `综合` 回退，只查找并点击 `全部`。
2. 保留 `1.0.48` 的商品卡候选过滤：必须有商品信号，且排除相关搜索、视频标题和评论区。
3. 手机端版本提升到 `1.0.49/85`，用于重新打包、发布和真机复测。

验证结果：
1. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 和 `node mobile-agent/autojs/tests/control-loop.test.js`，测试通过。
2. 已执行 `node --check mobile-agent/autojs/platforms/douyin.js` 和 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，语法检查通过。
3. 已打包并发布 `1.0.49/85`，APK 地址为 `http://106.54.41.106:18080/downloads/apk/liaoyuanxinghuo-1.0.49-inrt.apk`，热更新 manifest 地址为 `http://106.54.41.106:18080/downloads/agent/AgriVideoCollector-autojs-1.0.49.json`。

## 30. 2026-07-15 商品详情页未沿推荐商品卡继续刷

现象：
1. 用户反馈商品卡养号“刷到一半就退出去，然后找下一个商品卡刷”。
2. `docs/info` 中 `1.0.49` 日志显示，01:29:14 进入第一个商品详情，01:31:17 记录 `commerce card detail browsed`，01:31:21 已回到 `全部` 搜索结果页并点击第二个商品。
3. 同一段日志没有出现“点击详情页后续商品卡片区域”，说明详情页底部推荐商品卡没有被点击。

影响：
1. 商品卡养号没有按照“进入商品详情后下滑到底部，继续刷推荐商品卡”的口径执行。
2. 脚本会反复从搜索结果页选新商品，偏离用户截图中的详情页推荐商品链路。

根因：
1. `browseOpenedCommerceDetail()` 每 15 秒先执行下滑并 `continue`，推荐商品卡识别逻辑排在后面，导致下滑后没有机会立即判断并点击推荐商品。
2. 旧逻辑还限制 `recommendedClicks < 2`，即使识别成功也最多沿详情页推荐链路点两个商品。

处理结果：
1. 详情页内先判断当前屏幕是否有推荐商品卡，命中后直接点击进入下一个推荐商品，再重置当前详情停留计时。
2. 推荐商品链路不再固定最多 2 个，而是按本轮剩余商品卡数量连续点击。
3. 手机端版本提升到 `1.0.50/86`，用于重新打包、发布和真机复测。

验证结果：
1. 已补充 `commerce-card-live-runner.test.js` 静态断言，确保推荐商品卡判断排在继续下滑/返回搜索结果前。
2. 已补充断言防止恢复 `recommendedClicks < 2` 限制。
3. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 和 `node mobile-agent/autojs/tests/control-loop.test.js`，测试通过。
4. 已执行 `node --check mobile-agent/autojs/platforms/douyin.js` 和 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，语法检查通过。
5. 已打包并发布 `1.0.50/86`，APK 地址为 `http://106.54.41.106:18080/downloads/apk/liaoyuanxinghuo-1.0.50-inrt.apk`，热更新 manifest 地址为 `http://106.54.41.106:18080/downloads/agent/AgriVideoCollector-autojs-1.0.50.json`。

## 31. 2026-07-15 商品详情页仍回到搜索结果页继续刷

现象：
1. 用户复测 `1.0.50` 后反馈，商品卡养号仍会退出商品详情页，到综合/全部搜索结果页继续刷商品卡。
2. 期望流程是从搜索结果页进入首张夏橙商品卡后，在商品详情页内部一直往下刷；刷到详情页推荐的其他夏橙商品卡后点进去继续看。

影响：
1. 商品卡养号仍偏离“商品详情页推荐商品链路”，会回到搜索结果瀑布流找下一张卡。
2. 真机表现上仍像是在综合/全部里刷商品卡，而不是沿当前商品详情页底部推荐区刷。

根因：
1. `1.0.50` 只修复了详情页内推荐商品卡识别顺序，但外层 `browseCommerceCards()` 在详情页浏览返回后仍固定执行 `back()`。
2. `requireFullScan=true` 时，详情页浏览函数仍可能在单卡停留窗口结束后返回，外层随后继续按搜索结果页逻辑滑动和补卡。

处理结果：
1. `browseOpenedCommerceDetail()` 在 `requireFullScan` 场景下改为持续留在已打开的商品详情链路，直到本轮时间结束或收到控制指令。
2. 详情页单卡停留窗口结束但未找到后续商品卡时，继续在详情页下滑查找，并记录“商品详情页未点到后续商品卡，继续在详情页下滑查找”。
3. 全量 15 分钟模式下不再按计算出的 8 张商品卡上限停住，详情页内只要继续刷到夏橙相关商品卡就继续点。
4. 外层新增 `openedDetailThisAttempt`，打开商品详情后不再执行搜索结果页滑动，也不再固定 `back()` 回综合/全部结果页。
5. 手机端版本提升到 `1.0.51/87`。

验证结果：
1. 已补充 `commerce-card-live-runner.test.js` 静态断言，确保全量商品卡养号留在打开后的商品详情链路。
2. 已补充断言防止打开商品详情后继续执行搜索结果页滑动。
3. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 和 `node mobile-agent/autojs/tests/control-loop.test.js`，测试通过。
4. 已执行 `node --check mobile-agent/autojs/platforms/douyin.js` 和 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，语法检查通过。
5. 已打包并发布 `1.0.51/87`，APK 地址为 `http://106.54.41.106:18080/downloads/apk/liaoyuanxinghuo-1.0.51-inrt.apk`，热更新 manifest 地址为 `http://106.54.41.106:18080/downloads/agent/AgriVideoCollector-autojs-1.0.51.json`。

## 32. 2026-07-15 商品详情页重复点击当前商品区域

现象：
1. 用户导出的 `docs/info` 显示，`1.0.51` 进入商品详情页后长时间停留在 `ProductDetailActivity`。
2. 日志持续出现“点击详情页后续商品卡片区域”，bounds 固定为 `[55,1254][1025,1384]`，可见商品标题也保持为同一个夏橙商品。
3. 最终 `commerce card detail browsed` 中 `browsedCount` 增长到 46，但实际没有进入新的推荐商品，任务由人工 STOP 结束。

影响：
1. 商品卡养号看起来像“点进商品卡后不动”，实际是重复点当前详情页同一区域。
2. 已浏览商品数被误增，不能代表真实商品卡浏览数量。

根因：
1. `clickCommerceRelatedProductCard()` 只按关键词和商品信号筛选节点，可能把当前详情页标题/信息块当作推荐商品卡。
2. 调用方点击后没有验证页面是否切换，就直接增加浏览计数并重置停留窗口。

处理结果：
1. 详情页推荐商品点击新增 bounds 跳过表，点击后未跳转的同一区域不再重复点击。
2. 点击后新增详情页签名校验，只有确认进入新的商品详情且仍命中商品关键词时才计数。
3. 点击未跳转时记录 warning，并继续下滑查找真正的推荐商品卡。

验证结果：
1. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，通过。
2. 已执行 `node mobile-agent/autojs/tests/control-loop.test.js`，通过。
3. 已执行 `node --check mobile-agent/autojs/platforms/douyin.js` 和 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，通过。

## 33. 2026-07-15 商品详情推荐区未识别夏橙商品卡

现象：
1. 用户复测反馈，脚本不再回到 `全部` 搜索结果页，但进入商品详情后会一直下滑到“你可能想看”标签后面。
2. 用户判断该区域下方已经出现推荐商品卡，商品卡标题应能命中 `夏橙` 关键词，但脚本没有稳定点入下一张商品卡。

影响：
1. 商品卡养号仍不能稳定沿商品详情页推荐商品链路闭环执行。
2. 真机表现会像“已经滑到推荐区但不认识商品卡”，需要人工停止或等待超时。

根因：
1. `clickCommerceRelatedProductCard()` 只把单个命中关键词的节点作为点击候选，未把商品名节点提升到推荐商品卡容器。
2. 抖音详情页推荐区的商品名、价格、已售、图片和可点击区域可能拆在父级或相邻节点，单个标题节点 bounds 太小或不可点击，导致识别和点击都不稳定。

处理结果：
1. 新增推荐商品卡容器候选聚合，从关键词节点向上查找父级商品卡，按商品信号、推荐区信号、bounds 尺寸和位置打分。
2. 继续保留点击后详情页签名校验，只有确认页面切换且仍命中商品关键词才计数。
3. 点击日志新增 `matchedText`，后续真机日志可直接判断命中的是商品标题还是聚合后的商品卡容器。
4. 手机端版本提升到 `1.0.53/89`，并已生成本地热更新包和 APK，未发布线上 stable。

验证结果：
1. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，通过。
2. 已执行 `node mobile-agent/autojs/tests/control-loop.test.js`，通过。
3. 已执行 `node --check mobile-agent/autojs/platforms/douyin.js` 和 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，通过。
4. 已校验 `dist/apk/燎原星火-1.0.53-inrt.apk` 的 v2/v3 签名、zipalign、包名 `com.agri.video.collector`、版本 `1.0.53/89` 和注册密钥注入状态。

## 34. 2026-07-15 商品卡首卡和详情推荐卡仍可能点不到整张卡

现象：
1. 用户明确商品卡养号应先从商城搜索结果“全部”板块点击关键词匹配商品卡。
2. 进入商品详情后，应下滑到“你可能想看”等推荐商品卡列表，并继续点击关键词匹配商品卡循环。
3. 现有实现虽然已做推荐卡容器聚合，但真机仍可能滑过推荐标签后没有进入下一张商品卡。

影响：
1. 商品卡养号可能停留在单个商品详情页内下滑，不能形成“首卡 -> 详情推荐卡 -> 下一卡”的 15 分钟循环。
2. 如果点击候选停留在关键词标题碎片，仍可能因为不可点击或 bounds 太小导致无跳转。

根因：
1. 搜索结果首卡仍主要按关键词节点点击，没有统一提升到商品卡容器。
2. 详情页推荐卡只依赖配置中的 `recommendationSignals` 和商品信号，未内置识别“你可能想看”等真实页面文案。
3. 推荐区商品标题、价格、销量和可点击区域拆分时，单个关键词节点上下文可能拿不到完整商品信号，导致候选被过滤。

处理结果：
1. 新增搜索结果商品卡容器候选提升，首卡点击优先点商品卡容器。
2. 增加 `你可能想看`、`猜你喜欢`、`推荐商品`、`相关商品` 等内置推荐区识别。
3. 推荐区可见时允许标题型商品卡作为兜底候选，并放宽中下屏 bounds 过滤，避免底部商品卡被排除。
4. 手机端版本提升到 `1.0.54/90`。

验证结果：
1. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，通过。
2. 已执行 `node mobile-agent/autojs/tests/control-loop.test.js`，通过。
3. 已执行 `node --check mobile-agent/autojs/platforms/douyin.js`、`node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 和 `node --check mobile-agent/autojs/app/commerce-card-live-runner.js`，通过。
4. 已生成本地 `dist/AgriVideoCollector-autojs-1.0.54.zip` 和 `dist/apk/燎原星火-1.0.54-inrt.apk`；APK 已通过 v2/v3 签名、zipalign、包名版本和内嵌注册密钥一致性校验，未发布线上 stable。
## 35. 2026-07-15 商品详情推荐区识别与直播门禁异常分类

现象：
1. 真机已能从“全部”搜索结果进入商品详情，并能下滑到“你可能还会喜欢/推荐”附近，但没有稳定点击该区域下方关键词匹配的推荐商品卡。
2. 任务每轮商品卡养号后需要退出抖音、重新进入直播板块验证目标直播间；如果目标门禁异常，不能和“正常扫完但没命中目标直播间”混为一类。

影响：
1. 商品卡养号可能停留在详情页持续下滑，无法形成“首卡 -> 详情推荐卡 -> 下一张商品详情”的 15 分钟闭环。
2. 配置缺失、入口失败、暂停停止等异常如果被计入三轮未达标，会让后台状态误判为养号失败。

根因：
1. 推荐商品卡识别仍偏向全屏关键词节点，虽然会向父级找容器，但没有先把候选限定到详情页推荐商品列表区域。
2. 直播门禁返回 false 后，runner 之前没有区分 `target_live_room_not_found` 和其他异常原因。

处理结果：
1. `commerce-card-browser.js` 新增推荐区状态识别，按推荐标题、推荐 tab、商品卡结构兜底分层确认推荐列表，再只在该区域内做关键词匹配和商品卡容器候选提升。
2. 推荐卡候选过滤排除底部进店、客服、购物车、领券购买等动作栏，并继续保留点击后详情页签名校验与同 bounds 跳过机制。
3. `commerce-card-live/runner.js` 新增直播门禁业务未命中分类，只有 `target_live_room_not_found` 才进入下一轮商品卡养号；其他原因直接按异常或控制中止收尾。

验证结果：
1. 已执行 `node --check mobile-agent/autojs/platforms/douyin/commerce-card-browser.js`、`node --check mobile-agent/autojs/features/commerce-card-live/runner.js` 和 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，通过。
2. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`、`node mobile-agent/autojs/tests/control-loop.test.js` 和 `node mobile-agent/autojs/tests/live-comment-runner.test.js`，通过。
## 36. 2026-07-16 1.0.55 注册失败与热更新解压卡点

现象：
1. `docs/info` 显示 `1.0.54` 仍能注册设备、发送心跳和拉取配置，但尝试更新到 `1.0.55` 时报 `FileNotFoundException ... ENOTDIR (Not a directory)`。
2. 手动运行 `1.0.55` 后，设备注册请求返回 403，业务码为 `REGISTRATION_SECRET_INVALID`，后续版本检查出现 `SIGNATURE_INVALID`，心跳停止在“设备注册失败，等待重试”。

影响：
1. `1.0.55` 不能继续作为真机测试包使用，否则手机无法稳定注册后台，也无法验证商品卡推荐区点击逻辑。
2. 旧包自动更新到 `1.0.55` 可能被 ZIP 目录项解压问题阻断，导致设备仍停留在旧版本。

根因：
1. `1.0.55` 打包时使用了本地 `.env.production` 中的旧注册密钥，和服务器生产后台实际配置不一致。
2. `1.0.55` 热更新 ZIP 包含显式目录项，旧解压器对目录项和后续同级文件路径的兼容处理不足。

处理结果：
1. `scripts/package-autojs.ps1` 改为只向 ZIP 写入文件项，不写显式目录项。
2. `mobile-agent/autojs/core/uploader.js` 增强解压兜底，写文件前会处理目录项、父路径冲突和输出路径被目录占用的情况。
3. 版本提升到 `1.0.56/92`，并使用服务器生产配置重新打包，注册密钥只做哈希一致性校验，不记录明文。
4. 已上传并发布 stable Agent `1.0.56`，后台版本记录 ID 为 `04d4c315-4695-48e3-b42b-294d57529b62`。

验证结果：
1. 已执行 `node --check mobile-agent/autojs/core/uploader.js`、`node --check mobile-agent/autojs/platforms/douyin/commerce-card-browser.js`、`node --check mobile-agent/autojs/features/commerce-card-live/runner.js`，通过。
2. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`、`node mobile-agent/autojs/tests/control-loop.test.js`、`node mobile-agent/autojs/tests/live-comment-runner.test.js`，通过。
3. 已校验 `dist/AgriVideoCollector-autojs-1.0.56.zip` 不含目录项，SHA256 为 `5e9e484c1486f312973a40a7f2fd79033b912fdfb60fc75537f1270b8cc44aeb`。
4. 已校验 `dist/apk/燎原星火-1.0.56-inrt.apk` 的签名、zipalign、包名和版本，APK SHA256 为 `aef7d17b5b8488fd04a9d452ba09d55f128fa762bc386ca3a07f18754db3a52c`。
5. 公网 manifest 返回 `1.0.56`，生产 `/health`、`/ready` 正常，后台 Agent 版本列表最新记录为 `1.0.56 stable PUBLISHED`。

## 37. 2026-07-16 后台商品卡业务名与内部任务类型混用

现象：
1. 页面上存在“商品卡直播评论”“商品卡组合任务”等旧名称，但用户实际只维护 `商品卡养号` 和 `目标直播评论` 两个新功能。
2. 手机端日志和历史 assignment 仍会上报 `commerce_card_live_comment`，如果后台直接展示内部字段，会让运营误判为存在第三个功能。

影响：
1. 任务下发、日志排查和页面配置容易口径不一致。
2. 用户复盘日志时，很难区分商品卡养号阶段和目标直播评论阶段。

根因：
1. `commerce_card_live_comment` 是历史任务协议字段，后续业务拆成两个启动意图后，管理端没有同步做展示层映射。
2. 日志中心此前只按 phase 原样映射，未把旧手机文案归一化到页面业务名。

处理结果：
1. 管理端任务调度、设备页、直播目标配置和公共展示映射统一显示 `商品卡养号`。
2. 目标评论阶段、手动启动按钮和评论发送/结果日志统一显示 `目标直播评论`。
3. API 校验与错误提示改为 `商品卡养号` 口径；内部 `commerce_card_live_comment` 不改，避免破坏手机协议和历史数据。

验证结果：
1. 已执行 `bun test apps/api/src/services/task-assignment-types.test.ts`，通过。
2. 已执行 `bun run typecheck`，通过。
3. 已执行 `bun run build`，通过。
4. 已部署提交 `ef21765` 到生产后台，重建并重启 `api`、`web` 容器；生产 `/health`、`/ready` 正常，首页资产为 `/assets/index-mA7IFNaG.js`。

## 38. 2026-07-16 App 启动页偶发黑屏

现象：
1. 用户反馈点击 App 图标时偶发黑屏。
2. 按一次返回键后页面恢复，说明底层脚本或旧启动页仍在，只是最上层 Activity 显示异常。

影响：
1. 现场安装或复测时容易误以为 App 启动失败。
2. 如果用户不知道按返回可恢复，会影响后续启动脚本和查看状态。

根因：
1. `project.json` 的主入口是 `launcher.js`，负责渲染启动页并启动 `main.js`。
2. `main.js` 已有重复实例保护，但 `launcher.js` 之前没有清理其它启动页实例；重复点击图标或系统恢复 Activity 时，AutoJs6 壳层可能叠出空白启动页。

处理结果：
1. `launcher.js` 在 `ui.layout()` 前执行 `stopDuplicateLaunchers()`，只清理其它 `launcher.js` engine。
2. 不停止 `main.js` 和 `watchdog.js`，避免影响已经在跑的采集任务。
3. 手机端版本提升到 `1.0.57/93`，已生成本地热更新包和 APK。

验证结果：
1. 已执行 `node mobile-agent/autojs/tests/control-loop.test.js`，通过。
2. 已执行 `node --check mobile-agent/autojs/tests/control-loop.test.js`，通过。
3. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，通过。
4. 已校验 `dist/apk/燎原星火-1.0.57-inrt.apk` 的 v2/v3 签名、zipalign、包名 `com.agri.video.collector` 和版本 `1.0.57/93`。

## 39. 2026-07-16 商品卡养号主循环 ReferenceError 后暂停

现象：
1. `docs/info` 显示后台在 `01:59:44` 下发商品卡养号 `START`，手机端已执行并回执 `DONE`。
2. `02:00:36` 抖音打开后，任务准备交给商品卡 runner，主循环立即报 `ReferenceError: "isSearchResultPageText" 未定义.`。
3. 报错后心跳变为 `paused:true`、`status:"error"`、`lastMessage:"Agent异常，待命中"`，后续空闲同步继续保持暂停。

影响：
1. 商品卡养号还没进入实际商品卡浏览，就被主循环异常保护逻辑暂停。
2. 表现上容易误判为后台没有下发任务、网络异常或抖音 UI 识别失败；实际后台指令、注册和心跳在该时间点是正常的。

根因：
1. `platforms/douyin/commerce-card-browser.js` 的上下文恢复逻辑调用 `context.isSearchResultPageText(textSample)`。
2. `platforms/douyin/adapter.js` 装配商品卡浏览器上下文时写成 `isSearchResultPageText: isSearchResultPageText`，但本文件没有定义 `isSearchResultPageText`。
3. 适配器已有 `isSearchResultPage(visibleText)`，该函数正是可复用的搜索结果页判断方法；这是代码拆分后的上下文方法漏接，不是网络或后台问题。

处理结果：
1. `adapter.js` 将 `isSearchResultPageText` 绑定到现有 `isSearchResultPage`。
2. `commerce-card-live-runner.test.js` 增加源码级契约断言，要求商品卡浏览器上下文必须把搜索结果页判断绑定到已有适配器方法。
3. 本次未改商品卡识别策略、任务状态机、后台接口或打包脚本。

验证结果：
1. 已执行 `node --check mobile-agent/autojs/platforms/douyin/adapter.js`，通过。
2. 已执行 `node --check mobile-agent/autojs/platforms/douyin/commerce-card-browser.js`，通过。
3. 已执行 `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，通过。
4. 已执行 `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js`，通过。
5. 已执行 `node mobile-agent/autojs/tests/control-loop.test.js`，通过。
6. 已执行 `node mobile-agent/autojs/tests/live-comment-runner.test.js`，通过。

剩余风险：
1. 本次只修复主循环 `ReferenceError`，没有重新打包和发布 stable Agent，真机需要安装或热更新包含本次提交的版本后才会生效。
2. 商品卡推荐区点击是否稳定仍需真机继续回归，本次修复只解决“还没开始浏览就异常暂停”的阻断问题。

## 40. 2026-07-16 商品卡养号 ReferenceError 修复包打包
现象：
1. `1.0.57/93` 本地 APK 生成时间早于 `isSearchResultPageText` 未定义异常修复，继续使用该版本号会导致真机日志难以区分新旧包。
2. 本机打包环境没有直接设置 `MOBILE_REGISTRATION_SECRET`，直接执行 APK 打包会被脚本阻断。

处理结果：
1. 手机端版本提升到 `1.0.58/94`，用于承载 `51a359c` 的主循环异常修复。
2. 从既有已验证安装包的注入配置中读取生产注册密钥到当前打包进程，未输出或记录密钥明文。
3. 已生成 `dist/apk/燎原星火-1.0.58-inrt.apk`，SHA256 为 `DEEB09C706762D821064398A260141D61428E8356F4E63022142B2A0F1B78357`。

验证结果：
1. 已执行 `powershell -ExecutionPolicy Bypass -File .\scripts\package-autojs-apk.ps1`，通过。
2. 已执行 `apksigner verify --verbose --print-certs dist/apk/燎原星火-1.0.58-inrt.apk`，v2/v3 签名通过。
3. 已执行 `zipalign -c -p 4 dist/apk/燎原星火-1.0.58-inrt.apk`，通过。
4. 已通过临时 ASCII 路径执行 `aapt dump badging`，确认包名 `com.agri.video.collector`、版本 `1.0.58/94`。

剩余风险：
1. 本轮只生成本地 APK，未上传服务器下载目录，未发布 stable Agent。
2. 商品卡推荐区点击稳定性仍需安装 `1.0.58/94` 后真机回归验证。

## 41. 2026-07-16 App 卸载后后台停止任务仍卡执行中
现象：
1. 用户卸载手机端 App 后，后台任务仍显示执行中。
2. 页面点击停止后，后台最新命令变为 `STOP/PENDING`，但 assignment 仍保持 `RUNNING`。
3. 线上最新记录显示 `blockReason=stop_requested`，说明后台已经发起停止，但没有进入终态。

根因：
1. 旧逻辑把 STOP 当成需要手机端确认的控制命令，只创建 `STOP` 命令，不直接结束 assignment。
2. assignment 只有在手机端拉取 STOP 并 ACK `DONE` 后才会变成 `CANCELLED`。
3. App 卸载、离线、注册失败或网络不可达时，手机端不会再 ACK，后台就会一直卡在活跃状态，进而阻塞新任务启动。

处理结果：
1. 后台创建 STOP 控制命令时，立即将 assignment 置为 `CANCELLED`，写入 `terminalReason` 和 `completedAt`。
2. STOP 命令仍保留为 `PENDING`，手机在线时仍可拉取并执行本地停止；手机不在线时后台也不会继续等待。
3. 打包脚本增加本机已打包配置 fallback，避免每次打包都手动输入注册密钥；真实密钥不写入代码仓库。

验证结果：
1. 已执行后端定向测试，通过。
2. 已执行 `bun run typecheck`，通过。
3. 已执行两个打包脚本语法检查，通过。
4. 已在未设置 `MOBILE_REGISTRATION_SECRET` 的情况下执行热更新包打包和 APK `-SkipBuild`，均能自动注入本机注册密钥。

剩余风险：
1. 该后台修复需要部署到服务器后才会影响当前卡住的线上 assignment。
2. 已经处于 `STOP/PENDING` 的旧记录，需要部署后再次点击停止，或由后台补偿逻辑/人工 SQL 终止。
## 42. 2026-07-16 离线 STOP 修复部署与旧任务终止验证

现象：
1. 手机端 App 已卸载或离线后，后台旧 assignment 仍显示执行中。
2. 点击停止只创建 `STOP/PENDING` 命令，旧逻辑等待手机 ACK，导致 assignment 继续保持 `RUNNING`。

处理结果：
1. 已部署提交 `857ff0f` 到服务器 `/opt/auto-collection/account-data-platform`，远端重建并重启 `api` 容器。
2. 部署后公网 `/health` 和 `/ready` 均返回 200。
3. 对旧卡住 assignment `e29134db-655e-4edf-8307-b8d34906e9af` 重新发 STOP，状态从 `RUNNING` 变为 `CANCELLED`，`stateVersion` 从 `7` 变为 `8`，`terminalReason=manual_stop_after_offline_fix_deploy`。
4. STOP 命令本身仍保留为待手机拉取的命令，用于在线设备本地退出；后台不再依赖该 ACK 才结束 assignment。

验证结果：
1. 已重新打包 `dist/apk/燎原星火-1.0.58-inrt.apk`，版本 `1.0.58/94`，SHA256 为 `FBA9EBDDA9DC1C4E411C61C3761AE66A8B80958089C4ADB73C6C4154C81070B9`。
2. APK 已通过 v2/v3 签名、zipalign、包名和版本校验。
3. 本轮未上传 APK 到服务器下载目录，未发布 stable Agent。
## 43. 2026-07-16 商品详情推荐区识别后未点击关键词商品卡

现象：
1. 真机日志中反复出现 `识别到详情页推荐商品卡列表，准备按商品关键词匹配`。
2. 首次点击 `bounds=[55,1307][1025,1437]` 后提示 `commerce related product click did not open a new product`。
3. 后续继续出现推荐区识别日志，但没有新的 `点击详情页后续商品卡片区域`，表现为一直在详情页往下滑。

根因：
1. 旧逻辑看到顶部 `商品/评价/详情/推荐` 文案就进入 `recommend_tab` 识别，但当时日志文本仍包含 `详情 已选中`，说明 `推荐` 并未真正选中。
2. `structure` 兜底只凭当前详情页的价格、已售、店铺等商品信号即可激活，导致当前主商品标题被当成推荐商品卡候选。
3. 候选失败后缺少“推荐区已识别但当前屏没有关键词商品卡候选”的细分日志，不利于判断后续是没有关键词文本还是被筛选条件过滤。

处理结果：
1. `commerce-card-browser.js` 改为只有 `推荐已选中` 或识别到 `你可能还会喜欢` 等推荐区标题后，才在推荐区匹配商品卡。
2. `candidate-detector.js` 移除仅凭当前详情页商品信号激活推荐列表的兜底，避免误点当前商品标题。
3. 推荐区 active 但没有命中候选时新增日志，记录 `strategy/topY/keywordNodeCount`。
4. 手机端版本提升到 `1.0.59/95`。
5. 已重新打包 `dist/apk/燎原星火-1.0.59-inrt.apk`，SHA256 为 `B4D0117F0D4691530BCAFCFBD1B95AA7FE439344C780C440C58848CE41CE29CE`。

验证结果：
1. `node --check mobile-agent/autojs/platforms/douyin/commerce-card-browser.js` 通过。
2. `node --check mobile-agent/autojs/platforms/douyin/commerce-card/candidate-detector.js` 通过。
3. `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 通过。
4. `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 通过。
5. `node mobile-agent/autojs/tests/control-loop.test.js` 通过。
6. `apksigner verify --verbose --print-certs`、`zipalign -c -p 4` 和 `aapt dump badging` 均通过，确认版本 `1.0.59/95`。

## 44. 2026-07-16 1.0.59 推荐区已识别但关键词节点为 0

现象：
1. `docs/info` 显示 `1.0.59/95` 能从搜索结果进入商品详情，并能识别到 `你可能还会喜欢` 与顶部 `推荐 已选中`。
2. 推荐区识别后反复输出 `keywordNodeCount:0`，随后继续下滑，未点击 `夏橙/秭归` 匹配的推荐商品卡。
3. 手动截图能看到推荐区商品卡标题包含目标关键词，但这些标题没有稳定出现在 AutoX.js 无障碍文本节点中。

根因：
1. 当前推荐卡点击链路只从 `textMatches/descMatches` 获取关键词节点，再向父级找商品卡容器。
2. 抖音推荐商品卡的关键词可能渲染在图片或未暴露给无障碍树的标题区域内，导致候选节点为空。
3. 日志证明推荐区策略已经触发，问题是识别输入缺失，不是后台、网络或任务下发失败。

处理结果：
1. 商品卡浏览器接入共享 OCR 引擎，在推荐区 active 且无障碍关键词节点为 0 时才启动 OCR 兜底。
2. OCR 兜底按推荐区下方两列商品卡区域截图识别，命中 `matchKeywords` 后点击对应区域，并保留现有详情页跳转校验。
3. 新增 OCR 可用性、区域为空、命中、未命中的日志，便于下一轮真机判断是 OCR 不可用、未识别文字还是点击后未跳转。
4. 手机端版本提升到 `1.0.60/96`。

验证结果：
1. `node --check mobile-agent/autojs/platforms/douyin/commerce-card-browser.js` 通过。
2. `node --check mobile-agent/autojs/platforms/douyin/adapter.js` 通过。
3. `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 通过。
4. `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 通过。

剩余风险：
1. OCR 依赖真机截图权限和 OCR runtime 可用性，需安装 `1.0.60` 后看日志中的 OCR 命中/未命中结果。
2. 如果抖音推荐卡版式继续变化，可能需要按截图日志调整 OCR 区域切分高度或点击点。

## 45. 2026-07-16 1.0.60 OCR 修复包打包校验

处理结果：
1. 已生成 `dist/apk/燎原星火-1.0.60-inrt.apk`，用于验证推荐商品卡 OCR 兜底点击。
2. APK SHA256 为 `03DA66DD91D96D0029458BCB77D989B740555A93D90DCAA9D8D16357D62526BA`。
3. `aapt dump badging` 确认包名 `com.agri.video.collector`、版本 `1.0.60/96`、应用名 `燎原星火`。

验证结果：
1. `powershell -ExecutionPolicy Bypass -File .\scripts\package-autojs-apk.ps1` 通过。
2. `apksigner verify --verbose --print-certs` 通过 v2/v3 签名校验。
3. `zipalign -c -p 4` 通过。
4. `aapt dump badging` 通过。

发布状态：
1. 本轮只生成本地 APK，未上传服务器下载目录，未发布 stable Agent。
2. 后台代码无改动，不部署 API/Web。
## 46. 2026-07-16 商城搜索入口错误与推荐卡 OCR 不可用后持续下滑

现象：
1. 真机从商城搜索时，右上角放大镜进入的结果页会变成“综合/精选”，页面内混入视频卡和直播卡，影响商品卡养号首卡识别。
2. `1.0.60/96` 已能进入商品详情并识别“你可能还会喜欢/推荐”区域，但推荐区关键词节点数量为 0，OCR runtime 又不可用，脚本没有点击推荐商品卡，而是持续下滑直到任务结束。

影响：
1. 商品卡养号可能从错误搜索入口进入非商品结果页，增加误判和恢复成本。
2. 推荐区已出现商品卡时仍无法进入下一张商品详情，15 分钟养号链路难以形成“首卡 -> 推荐卡 -> 下一卡”的闭环。

根因：
1. 商品卡搜索复用了通用搜索入口，候选按钮可能命中商城右上角放大镜，而不是商城页内搜索框。
2. 推荐卡点击此前依赖无障碍关键词节点或 OCR 命中；当抖音商品标题不暴露给无障碍树且 AutoX.js 当前运行环境没有可用 OCR API 时，没有继续按可见商品卡结构或布局兜底点击。

处理结果：
1. 新增商城页内搜索入口，过滤右上角搜索图标区域，并让商品卡浏览器优先使用该入口；同时不再把通用“综合”搜索页当作商品卡搜索上下文。
2. 推荐区点击新增结构兜底：在推荐区 active 时扫描可见商品卡文本、价格、销量、店铺等商品结构信号，找到候选后按商品卡容器点击。
3. 推荐区点击新增布局兜底：无关键词节点、OCR 不可用或未命中、结构候选为空时，按推荐区双列商品卡区域点击第一张未跳过卡片，避免一直下滑。
4. 手机端版本提升到 `1.0.61/97`，已生成并校验 `dist/apk/燎原星火-1.0.61-inrt.apk`。

验证结果：
1. `node --check mobile-agent/autojs/platforms/douyin/adapter.js` 通过。
2. `node --check mobile-agent/autojs/platforms/douyin/commerce-card-browser.js` 通过。
3. `node --check mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 通过。
4. `node mobile-agent/autojs/tests/commerce-card-live-runner.test.js` 通过。
5. `node mobile-agent/autojs/tests/control-loop.test.js` 通过。
6. `node mobile-agent/autojs/tests/live-comment-runner.test.js` 通过。
7. APK 打包、v2/v3 签名、zipalign 和 `aapt dump badging` 均通过，版本为 `1.0.61/97`，SHA256 为 `ED7B37106BF571FC7D087BF7995281D1D4DA880861966087F6762A119740F169`。

剩余风险：
1. 布局兜底不具备 OCR 级关键词判断能力，只能在推荐区确认后点击可见商品卡，真机仍需观察是否稳定进入同类商品链路。
2. 商城页内搜索框坐标兜底依赖当前抖音商城顶部布局，后续 UI 改版时需要根据日志中的点击 bounds 调整。

## 47. 2026-07-27 存量 schema.ts 超过 400 行约束

现象：
1. 远程脚本模块节点1实施前，`packages/db/src/schema.ts` 已有 656 行，属于历史存量超限文件。
2. 节点1若继续把三张新表直接写入该文件，会扩大超限范围；若在本节点拆分全部存量表，又会引入与节点目标无关的大范围重构。

处理决策：
1. 经授权，400 行约束按本模块新建或修改文件执行，节点1禁止拆分存量 `schema.ts`。
2. 三张新表写入 `packages/db/src/schema-remote-script.ts`，该文件共 81 行；`schema.ts` 仅追加一行 re-export。
3. 存量 `schema.ts` 的领域拆分列为后续独立重构任务，不属于远程脚本模块任何节点。
4. 节点11继续使用同一决策：`publish_tasks` 放入 49 行独立 `publish-task-schema.ts`，`schema.ts` 只增加一行 re-export，完成时为 659 行。

## 48. 2026-07-27 本地数据库混有当前 Schema 之外的旧脚本表

现象：
1. 本地 `5432/auto_collection` 可正常连接，但存在另一套 `script_*` 表，不在当前项目 Drizzle Schema 中。
2. 首次执行 `db:push` 时，Drizzle 要求在“新建 remote_script_* 表”和“重命名旧 script_* 表”之间交互选择，非 TTY 验收会话因此等待。

处理结果：
1. 明确选择新建语义，在事务中应用生成的 `0023_remote_scripts.sql`，没有把旧表重命名为新表。
2. 随后原始 `bun run db:push` 退出码为 0；该命令按当前 Schema 同步时输出旧表级联删除 NOTICE，执行后 `script_*` 表数量为 0。
3. 本节点未修改 Web/API 地址或数据库连接配置；后续切换 Web/API 到当前项目时，应先确认各端口对应的数据库实例与数据保留策略，避免再次混用 Schema。

## 49. 2026-07-27 全量测试存在两个既有 Web 断言失败

现象：
1. `bun test` 总计 56 个测试，其中 54 个通过、2 个失败；节点4新增的校验器和注册表测试全部通过。
2. `apps/web/tests/live-targets-page.test.js:79` 的“直播目标配置页面管理目标、三阶段组合和默认关闭门禁”失败，断言为 `page should manage the commerce-card workflow`。
3. `apps/web/tests/navigation-dashboard-status.test.js:41` 的“搜索直播评论和商品卡直播评论是两个独立任务入口”失败，断言为 `scheduler must show a clearly named target live comment button`。

判断与处理：
1. 两个失败均为 Web 页面源码与既有文本断言不一致，和节点4新增的 API registry、JSON Schema 校验器及 definitions 路由无调用关系。
2. 经授权，节点4以定向测试全绿作为验收通过；本节点不修改 Web 页面或断言。
3. 标记为独立修复任务：后续单独核对当前产品命名与页面职责，再决定修正页面实现或更新过期断言，完成后恢复全量 `bun test` 全绿。

## 50. 2026-07-27 存量 api-client.ts 超过 400 行约束

现象：
1. 远程脚本模块节点6实施前，`apps/web/src/lib/api-client.ts` 已有 529 行，属于历史存量超限文件。
2. 节点6原方案要求在该文件增加 remote-scripts 请求；直接追加会扩大超限，搬运既有领域函数又会引入节点范围外重构。

处理决策：
1. 经授权，不搬运、不改写 `api-client.ts` 的任何既有函数；仅在 `request/mutate/patch/remove` 四个原有 helper 行补 `export`，文件行数保持 529。
2. 新增 `apps/web/src/lib/api-client-remote-scripts.ts` 承载 definitions 列表与 configs CRUD，请求函数直接复用上述 helper；节点6完成时该文件为 61 行。
3. 节点8的绑定管理请求继续放入 `api-client-remote-scripts.ts`，并保持该文件不超过 400 行；存量 `api-client.ts` 的领域拆分另行立项，不属于远程脚本模块节点6。

## 51. 2026-07-28 存量 control-loop.js 超过 400 行约束

现象：
1. 远程脚本模块节点8a实施前，`mobile-agent/autojs/app/control-loop.js` 已有 1441 行，属于历史存量超限文件。
2. 节点8要求识别 `SCRIPT_CONFIG_UPDATED`，若在本节点完整拆分 control-loop 会引入与离线下发无关的大范围手机运行时重构。

处理决策：
1. 经授权，新增 `app/remote-script-config-handler.js` 承载拉取、storage 缓存、生效和 ACK 全部逻辑；该文件完成时为 196 行并有独立 Node 测试。
2. `control-loop.js` 仅增加 handler factory/实例两行与命令分发四行，共 6 行，不搬移、不改写任何既有命令处理逻辑；完成时为 1447 行。
3. 存量 `control-loop.js` 的职责拆分列为后续独立重构任务，不属于节点8a或8b；真机 8b 只能验证本次接线，不得借机重构既有控制循环。

## 52. 2026-07-28 存量 DevicesPage.tsx 超过 400 行约束

现象：
1. 远程脚本模块节点10实施前，`apps/web/src/routes/DevicesPage.tsx` 已有 678 行，且当时未在 `App.tsx` 路由表挂载。
2. 节点10要求在设备行增加账号绑定入口；在本节点拆分整页会牵涉脚本参数、设备信息、控制指令和账号画像等范围外逻辑。

处理决策：
1. 按“新功能进新文件”先例，新建 84 行 `DeviceAccountBindingControl.tsx` 承载按钮、Modal、表单与保存状态，新建 11 行 `api-client-devices.ts` 承载请求。
2. `DevicesPage.tsx` 仅增加 1 行 import 和 4 行组件挂载，完成时为 683 行；不搬移、不改写任何既有设备控制或配置逻辑。
3. 在低于 400 行的 `App.tsx` 增加 `/devices` 和侧栏入口，使指定页面可以验收；存量设备页拆分列为后续独立重构任务。

## 53. 2026-07-28 N6 存量 control-loop 文件超过 400 行约束

现象：
1. `mobile-agent/autojs/app/control-loop.js` 与 `mobile-agent/autojs/tests/control-loop.test.js` 均为历史存量超限文件，前者已有节点8a的超限登记。
2. N6 真机联调仅需为 `PUBLISH_VIDEO_TASK` 增加执行器启动日志及对应断言；拆分两份存量文件会扩大本次热更新 BOM 修复范围。

处理决策：
1. 本次 Git diff 中，`control-loop.js` 仅新增 1 行“发布执行器启动”日志；该入口属于 APK 基座，下次 APK 构建生效。
2. 本次 Git diff 中，`control-loop.test.js` 仅新增 3 行对应存在性断言；不重构既有测试组织。
3. `control-loop.js` 与其测试的领域拆分继续作为独立重构任务，本次以发布素材、看门狗和热更新包回归为边界。

### 2026-07-29 发布设置保存卡顿一期修复

- 已按保存链路修复前端宽泛缓存失效、请求无超时和后端 PATCH 同步设备通知阻塞；验证时需关注首次 POST 与编辑 PATCH 的响应时间。
- `account-data-platform/apps/web/src/lib/api-client.ts` 为既有 400 行以上文件，本节点因统一网络超时必须最小修改；未做无关拆分，后续可拆出请求层以恢复单文件行数约束。

## 54. 2026-07-29 后续节点08：快速粘贴话题数量仍硬编码

现象：
1. account-data-platform/apps/web/src/lib/quick-paste-publish.ts 的 parseQuickPasteText 当前调用默认话题校验，固定按 5 个话题判断。
2. 发布配置已支持 expectedTopicCount，因此配置为非 5 时，快速粘贴预检与实际执行要求可能不一致。

处理决策：
1. 节点06只处理时间闸口秒与毫秒转换，不扩大到快速粘贴业务规则。
2. 节点08应先取得默认执行配置的 expectedTopicCount，再传入快速粘贴解析与校验；缺失时才回退为 5，并补覆盖非 5 配置的前端测试。


### 2026-07-29 节点10离线验收发现问题（已解决）
- 现象：Web 设备绑定测试仍断言已移除的 bindingAccountName 私有函数名，导致节点10 Web 验收失败。
- 根因：节点08 将设备绑定展示重构为 bindingStatus，以抖音账号和视频号能力为用户可见语义，测试未同步。
- 修复：测试改为断言 bindingStatus、抖音和视频号能力文案。
- 关联发现：remote-script 配置更新过程会对已存储 payload 执行不必要强校验，遇到旧形态或不完整值会崩溃；已改为仅校验新传入 payload，并由测试覆盖。
- 验证：相关定向测试、全仓 Bun 测试、类型检查通过。

## 55. 2026-08-03 外部接口发布存在无账号 claim 与双时间源（已解决）

现象：
1. `claimPublishTaskPayloadSchema` 允许省略 `accountName`，旧匹配服务、管理端领取按钮和旧自动调度可只按平台领取任务。
2. 新设备计划已使用 `publishDeviceSchedules.timeWindows`，但总调度器仍会读取旧配置 `publishTimeSlots`，远程脚本表单也继续把该字段列为外部配置必填项。

根因：
1. 新的“设备 + 平台 + 账号绑定”调度链路接入后，旧领取入口和旧配置调度分支没有同步退役。
2. 运行时 Zod schema 已为旧时间点提供默认空数组，但动态表单注册表和旧测试仍保留原必填语义，形成两个配置来源。

处理结果：
1. claim schema 与内部调用统一要求非空 `accountName`，并在客户端发请求前拦截无账号 payload。
2. 删除旧领取/立即调度 API 与 Web 入口；已入库任务直发不触发新 claim，因此继续保留。
3. 删除旧 `publishTimeSlots` 自动调度分支；设备计划的 `timeWindows` 成为唯一调度时间来源。旧字段保留为默认空数组的兼容字段，不再必填。
4. 新增架构回归测试，阻止旧路由和旧时间点调度代码重新出现。

验证结果：
1. 全仓 `bun test` 为 `205 pass / 0 fail / 524 expect`。
2. `bun run typecheck` 与 `bun run lint` 均退出码 0。
3. 未进行真实外部接口调用、外部状态 PATCH、数据库迁移或真机操作。

## 56. 2026-08-03 成功任务结束后发布锁持续返回 PUBLISH_BUSY（离线修复）

现象：
1. 最近成功任务的后端命令为 `DONE`、发布任务为 `REPORTED`、手机结果为 `SUCCEEDED`，说明结果上报和 ACK 已完成。
2. 约 20 分钟后多条新任务仍立即返回 `PUBLISH_BUSY / 上一发布任务仍在执行`，未进入素材下载和抖音动作。
3. 生产 APK 不可 `run-as`，无法直接读取 `AgriVideoPublishTaskLock.active`；现场没有清锁、清应用数据或重启 Agent。

根因与风险：
1. 旧终态顺序为“点击返回燎原星火后再 finish，最后 finally 释放锁”，存在 ACK 成功后、锁释放前脚本环境切换的窄窗口。
2. 旧锁只保存 ownerId 与 timestamp，无法定位持锁任务；未来时间戳满足 `now - timestamp < TTL`，可能长期阻塞。
3. 同一 handler 并发进入时，未持锁的 PUBLISH_BUSY 分支绝不能调用共享锁 release，否则会误释放正在执行任务。

处理结果：
1. 新增幂等终态模块，在点击 Agent 卡片前完成 `report → ACK → 素材清理 → release lock → complete watchdog`。
2. 锁记录增加任务、命令和到期信息，并对旧格式、过期、无效及未来时间戳自恢复。
3. 每次 handle 记录本次是否真正获取锁，只有持锁者能够清素材和释放锁。
4. 新模块加入业务脚本预加载与打包清单，并新增终态顺序、幂等、上报失败释放、异常锁恢复和并发保护测试。

验证与剩余事项：
1. 本次发布相关定向测试 `40/40` 通过，Bun 全仓 `205/205` 通过，语法和差异检查通过。
2. AutoJS 全量仍有 5 项既有话题续传/基线模块断言失败，未在本次锁修复中扩大范围。
3. 修复尚未打包发布到手机；需要新 biz-scripts 版本真机验证连续两条发布任务。

## 57. 2026-08-04 control-loop.js 存量超限阻碍新命令直接接线（已规避，待专项治理）

现象：
1. `mobile-agent/autojs/app/control-loop.js` 当前为 1493 行，远超远程脚本模块单文件 400 行上限。
2. 目标直播间养号需要新增长任务命令和停止命令；直接修改该文件会违反当前节点硬约束，也会扩大节点 09 暂存文件的变更边界。

处理：
1. 本节点没有修改 `control-loop.js`。
2. 新增 97 行 `account-warmup-command-bridge.js` 包装 uploader 命令轮询，只拦截养号命令，其余命令原样交给现有控制循环。
3. 养号任务在独立线程执行，原控制循环继续轮询 STOP；重复返回同一个 FETCHED 命令时不会创建第二个线程。

后续：
1. 将 `control-loop.js` 按命令路由、后端同步、任务分配状态和维护命令拆分应单独立项，不在养号节点内顺手重构。
2. 拆分前新增命令优先采用隔离桥接器或已有注册机制，并保持每个新增文件不超过 400 行。

## 58. 2026-08-04 粘贴发布预览地址推导出不存在的 OSS 封面（已解决）

现象：
1. 粘贴 `/public/downloads/{草稿ID}.mp4` 后，视频可转换为腾讯 COS `/videos/{ID}.mp4`，封面却被转换为阿里 OSS `/covers/{ID}.jpg`。
2. 实例 `9e2a3957-f0e1-48ff-9eba-6d8a9ae052d0` 的 COS 封面返回 HTTP 200、`image/jpeg`，旧 OSS 地址返回 HTTP 404，页面因此显示“封面不存在”。

根因：
1. Web `media-url-pair.ts` 和 API `publish-material.service.ts` 分别硬编码了旧 OSS 封面基础地址。
2. 当前真实资源布局已统一为腾讯 COS：视频位于 `/videos/{ID}.mp4`，封面位于 `/covers/{ID}.jpg`。

处理与验证：
1. Web/API 的预览地址转换规则统一改为同一 COS 桶；显式粘贴的封面 URL 仍原样保留。
2. 粘贴发布提示文案改为“推导同一 COS 存储中的封面地址”。
3. 新增 API 精确 UUID 回归测试，并更新 Web 媒体解析与快速粘贴断言；定向 `25/25`、全仓 Bun `218/218`、typecheck、lint、生产构建和差异检查通过。

## 59. 2026-08-04 养号搜索约 90 秒且批次停止反馈不明确（代码与部署已解决，真机待手动验收）

现象：
1. 目标直播间养号复用通用 `openSearch()`，从抖音首页进入搜索结果约需 90 秒，反复真机调试成本过高。
2. 原批次按钮文案为“全部停止”，点击后缺少即时“停止中”反馈；手机只在动作边界处理 STOP，操作人员难以判断指令是否已经发出。

根因：
1. 通用搜索包含适用于多个业务的宽泛等待和恢复逻辑，不适合养号已知首页场景；随机等待并非主要耗时来源。
2. 前端只展示服务端命令状态，没有本地停止请求状态；桥接器完成 STOP 终态后也没有明确约束“先 ACK、后返回 Agent”。

处理结果：
1. 新增养号专用快速搜索模块，限定各阶段截止时间并记录入口、输入、提交、结果确认四段耗时；通用抖音适配器保持不变。
2. 前端批次按钮改为“停止任务”，使用本地命令 ID 集合即时显示“停止中”，并按设备保留成功下发、恢复失败下发。
3. 桥接器在运行命令 ACK 成功后才返回燎原星火；ACK 失败保留终态等待重试。
4. 修复已通过 `biz-scripts 1.3.11.20260804144730` 与 APK `1.0.69 / 105` 部署到设备。

验证与剩余事项：
1. 快速搜索、注册表、目标任务和命令桥接器定向测试通过；Web 页面 `6/6`、相关脚本语法和差异检查通过。
2. 自动化用例证明受控等待最坏约 9 秒，但真实抖音页面的 8～15 秒目标必须以用户手动测试日志为准。
3. 用户手动测试时需核对四段搜索耗时、按钮即时状态、运行命令结果 `STOPPED`，以及 ACK 日志先于“养号停止后已返回 Agent”。

## 60. 2026-08-04 养号打开抖音与输入关键词时序不符合手工测试节奏（代码已修复，未发布）

现象：打开抖音后使用随机 5～10 秒等待；点击搜索入口后仅等待 250 毫秒就输入关键词，与要求的固定动作节奏不一致。

处理：打开抖音后固定等待 7 秒再点击搜索；点击搜索入口后固定等待 3 秒，再输入命令 payload 中的 `targetKeyword`。精确事件顺序测试已先失败后通过。

边界：本次不处理搜索结果后的直播栏、进房、OCR 和观看流程；没有发布或更新手机脚本。

## 61. 2026-08-04 养号搜索结果直播入口返回 0 候选并退出抖音（当前动作代码已修复，未发布）

现象：快速搜索成功后，旧 `openLiveRoomFromCurrentScreen()` 在直播结果页记录 `total=0`，返回 `LIVE_ENTRY_NOT_FOUND`；候选失败恢复随即移除抖音，表现为下滑后直接退出。

本步处理：按手工验收动作改为“输入后等 3 秒 → 点击搜索 → 点击顶部直播栏 → 等 3 秒 → 点击第一直播结果卡片”，首个卡片不再经过旧候选识别。后续直播内容校验与失败恢复不在本步修改。

验证边界：离线定向测试通过；未发布到手机，真实坐标与进房结果等待用户按步骤验证。

## 62. 2026-08-04 养号直播间缺少相关名词与商业直播优先判断（代码已实现，未发布）

现象：进入直播间后只合并目标搜索词、相关名词及无障碍/OCR 文本；没有检测评论框旁小黄车，也会在每个候选失败后退出抖音重搜。

处理：OCR 改为只匹配前端相关名词；同一截图的底部固定区域按橙色像素簇检测小黄车。小黄车分支覆盖相关名词命中并调用 `nextVideo()` 下滑，同轮后续直播间继续重复判断。适配器 `nextVideo()` 完成动作但返回 `undefined`，调用包装器将“未显式返回 false”视为成功，避免滑动后误触发恢复。

边界：本次只实现判断和下滑分支，未实现观看时长、主播下播续看或真机标定；未发布手机脚本。

## 63. 2026-08-04 养号点赞节点需求取消（已清理）

处理：删除本轮新增的点赞模块、测试、注册表接线、目标结果计划字段，以及未完成的后台确认命令契约和测试。没有保留不可达代码或半成品命令类型。

验证：仓库范围搜索无养号点赞引用；现有养号 AutoJS、API 和共享契约定向测试通过。未影响仓库其他既有业务模块，未发布手机脚本。

## 64. 2026-08-04 养号点赞间隔规则重新拆分实施（时序完成，动作待后续节点）

处理：重新建立独立纯时序模块，只计算普通 1200～5400 毫秒间隔和每三次后的 5255～7565 毫秒长停顿；不恢复此前已删除的计划器、确认命令或触控逻辑。

边界：点赞次数与真实动作尚未定义，因此当前模块未被注册表加载，也不会在目标直播间命中后执行。未发布手机脚本。

## 65. 2026-08-04 养号点赞计划移除随机滑动（dry-run 完成，未接线）

处理：按用户收敛后的规则仅生成 7～17 个红框内不重复双击坐标，删除随机滑动要求。计划项只有 `double_tap`，并挂载已验证的间隔类型和毫秒值。

边界：模块不调用触控接口，不自动执行计划，也未接入目标直播间任务。真实动作仍未实现或发布。

## 66. 2026-08-04 养号评论词库前端配置先行（配置完成，发送待后续节点）

处理：前端新增可增删评论标签输入，共享契约新增规范化后的 `commentLibrary` 空数组默认值，命令构造同步保存该配置。

边界：当前只有配置数据，不包含评论数量、时序或任何自动发送操作。用户提供的 3026 实页截图已确认字段位置和页面布局正常；代码测试、typecheck 和页面 HTTP 可访问性已验证。

## 67. 2026-08-05 养号直播间 OCR 始终为空（根因已修复，待新 APK 真机验证）

现象：已发布业务脚本 `1.3.12.2026080501` 能执行搜索和候选下滑，但每次稳定区域识别都记录“当前运行环境未发现可用 OCR API”，`ocrTextSample` 为空，相关名词无法命中，因此任务不能进入点赞和评论阶段。

根因：当前 AutoJs6 APK 已内置中文 ML Kit OCR，全局接口为 `ocr.recognizeText()`；业务适配器只探测不存在的 `ocr.recognize()`，将方法名不匹配误报为能力缺失。

处理：OCR 引擎按 AutoJs6 ML Kit、旧版 `ocr.recognize`、Paddle、`$ocr` 的顺序探测；调用失败日志记录引擎和重试次数，只有全部能力不存在时才报告 unavailable。适配测试、语法检查和养号目标直播/互动回归通过，版本提升到 `1.0.70/106`。

边界：`core/ocr.js` 属于 APK 基础层，不能通过现有业务脚本包更新；本轮未打 APK。用户完成打包和覆盖安装后，仍需真机确认 `autojs6_mlkit` 日志、非空中文 OCR 文本及相关词命中。

## 68. 2026-08-05 养号后续直播间绕过校验、评论输入阻塞与 STOP 延迟（代码已修复，待真机验证）

现象：本轮真机实际完成 14 次点赞并点击评论框，但评论文字输入后没有完成日志；手动停止只能设置停止标记，阻塞中的评论调用无法及时返回。会话切换到第二个及后续直播间时还会直接互动，不再检查相关名词。

根因：点赞计划上限误写为 17；直播间校验只位于首房搜索入口，`live-session-runner` 的后续切房循环未接校验回调；评论适配器直接调用可能阻塞的全局 `setText()`；STOP 清理依赖业务线程返回 `STOPPED` 后才执行。

处理：点赞范围收口为 7～14；每次新房先 OCR/小黄车校验，失败只切房不互动；评论优先对聚焦输入节点调用 `setText` 并记录输入阶段；STOP 轮询路径直接中断业务线程并执行最近任务清理和 Agent 返回，然后收口停止命令与原任务回执。

边界：`adapter.js` 与命令桥接器属于 APK 基础层，必须安装 `1.0.71/107` 后真机验证。需重点观察逐房 `新直播间相关名词校验通过/未命中`、`直播评论文本输入开始/完成`、STOP 后最近任务左滑和燎原星火恢复。

## 69. 2026-08-05 STOP 后进入 SplashActivity 黑屏（根因已修复，待 1.0.72 真机复测）

现象：`1.0.71` 真机收到停止命令后没有打开最近任务，直接进入燎原星火黑屏。停止回执记录 `cleanup.reason=STOP_CLEANUP_UNAVAILABLE`，当前 Activity 为 `org.autojs.autojs.inrt.SplashActivity`。

根因：覆盖安装保留了旧业务脚本 `1.3.12.2026080501`；停止桥接器从业务注册表获取清理器，旧注册表没有 `cleanupAfterStop`，随后调用 `app.launchPackage()` 兜底，AutoJs6 inrt 包入口落入 SplashActivity。

处理：桥接器固定通过 `loadBaselineScript` 从 APK 加载最近任务清理器；无论业务脚本新旧都先中断任务，再点击最近任务、左滑抖音并点击燎原星火。移除包名启动兜底，并增加桌面燎原星火图标点击回退。业务脚本同步发布为 `1.3.13.20260805130913`。

## 70. 2026-08-05 直播观看中出现“开启日历提醒”遮罩（热更新已实现）

现象：直播观看期间抖音弹出“开启日历提醒”，需要点击“以后再说”；原逻辑只在首次启动抖音时清理同名按钮，观看计时循环不会处理突发弹窗。

处理：新增养号直播临时弹窗模块，要求标题“开启日历提醒”和按钮“以后再说”同时存在才点击；在每次直播状态判断前执行，关闭后继续当前直播间和原计时。

边界：本次只处理图示日历提醒，不点击“好的”，不改变相关词校验、点赞评论和直播时长。属于 `features/` 热更新，无需重打 APK。

## 71. 2026-08-12 设备状态与远程唤醒页面需在底座功能完成后统一改版（已标记，暂不处理）

现象：Agent 心跳语义改造后，远程唤醒页仍沿用旧的设备列表布局。当前截图中 `agent_unreachable` 原始状态枚举直接显示；设备状态、最后 Agent 心跳、手动唤醒进度和失败原因被挤在同一行，难以快速判断设备当前实际处于何种状态。

计划：待原生底座心跳、Agent 存活检测和后台启动 Agent 闭环完成后，统一重做该页面的信息层级与视觉状态。届时应将“手机底座状态”和“Agent 状态”分开显示，突出唯一的可操作动作，并将恢复过程改为清晰的阶段进度，不在本轮零散调整样式。

本轮边界：只保留该改版标记，不改变页面结构、配色、组件布局或任务逻辑。`agent_unreachable` 的用户可读文案映射属于当前语义功能缺口，后续以独立小节点修正。

## 72. 2026-08-18 MIUI 最近任务卡未暴露抖音标题

现象：停止或风控收尾时，ADB 仍能看到抖音任务，但无障碍树的最近任务卡没有“抖音”文字或描述，旧代码误判为找不到卡片，手机没有完成左滑退出。

根因：清理器将“无障碍树中存在抖音标题”错误地等同于“抖音任务存在”；不同 MIUI 渲染状态下，任务卡标题并不稳定暴露。

处理：在“抖音刚刚位于前台”与“当前页已进入 MIUI 最近任务”双重确认后，允许通过居中的 `com.miui.home:id/task_view_thumbnail` 定位并左滑。识别失败时记录当前包名、前台确认结果和卡片数量；不采集页面内容。

交付边界：`1.0.90/126` 桥接器只读取 APK 内置清理器，业务热更新无法覆盖此文件；需构建并覆盖安装 `1.0.91/127` 后真机复测。新桥接器优先读取业务脚本热更新层，后续仅改清理器时无需再因该加载边界重装 APK。

## 73. 2026-08-18 抓取评论词页面打开即重复弹出历史异常

现象：用户没有新建任务，只打开“抓取评论词”页面，页面恢复上一次失败批次后再次弹出平台验证或人数检测警告。

根因：历史批次 ID 通过浏览器存储恢复，告警弹窗副作用没有区分“页面初次恢复”与“当前页面新任务”。

处理：首次恢复历史批次时将已有终态告警标记为已读；新批次仍保留实时告警。历史异常继续显示在进度和异常设备区域，不会被清除。

## 74. 2026-08-18 直播间人数 OCR 误读页面其他数字

现象：直播间人数检测曾同时扫描右上角外的多个区域以及全屏、无障碍文本，评论数量、商品价格或标题数字可能被误作为观众人数。

处理：OCR 范围收紧为用户标注的右上角红框，并仅解析该区域内人数数字；不再使用其他区域或全屏结果作为回退。

## 75. 2026-08-18 退出 Agent 后设备接入状态短暂断联

现象：执行“停止 Agent、退出页面并锁屏”后，设备接入可能短暂显示断联；同时 App 页面可能仍显示前台、屏幕状态与退出结果不一致。

根因：退出流程完成后没有主动刷新外层心跳；另有 App 状态检测把底座前台服务带来的进程优先级错误解释为 App 页面前台。

处理：退出流程统一请求原生底座即时上报；App 前台改为只依据当前顶层 Activity。Agent 停止、页面移除和锁屏三个原有步骤不变。

## 76. 2026-08-19 抓取评论词结束直播页未被识别

现象：进入搜索结果中的已结束直播间时，顶部显示“直播已结束”。该页面可能不满足无障碍直播间标记，若先调用 `isLiveRoom` 会被误判为入口失败；人数红框也没有可用在线人数。

处理：同一张截图 OCR 两个限定区域，先匹配“直播已结束”，命中后滑到下一候选；未命中才解析右上角人数。人数下限为 0 时仍检查结束页。单个结束直播间不算设备失败，切换失败或候选耗尽才进入红色失败状态。

交付边界：本轮只修改抓取评论词业务脚本及其页面状态映射，未修改 APK 底座、评论提取、词库或其他养号功能；真机业务测试由用户手动执行。`live-comment-entry.js` 为历史已超 400 行的存量脚本，本轮保持最小改动，未进行跨功能重构。
## 2026-08-13 远程打开偶发黑屏与开机后状态延迟

- 已修复：移除原生与内层对同一打开命令的双执行，并避免通过透明启动页和 `CLEAR_TOP` 重建任务栈。
- 已修复：开机首次上报失败后可在网络恢复时立即重试，不再只能等待下一固定心跳周期。
- 待真机验收：锁屏且 Agent 在线时从管理后台打开不再黑屏；重启后无需进入 App，设备先恢复在线，Agent 保持未开启且屏幕状态实时更新。

## 77. 2026-08-20 抓取评论词 MVP 收尾与 APK/热更新边界

现状：抓取评论词 N0-N9 代码链路已完成，评论红框 OCR、5 次上滑、候选展示、人工确认入库、多设备批次和退出收尾均有自动化回归；最新业务包已生成但未发布。

已处理：采集阶段回执携带最新候选；手动 STOP 时桥接器复用最近候选并标记 `LIVE_COMMENT_ENTRY_PARTIAL`，不再用空结果覆盖已抓评论；人数/结束页限定 OCR 截图读取后立即释放，避免连续重试或切房累积截图对象。业务包 `1.3.21.20260820160500` 已完成 SHA、manifest、逐文件哈希、无 BOM、ASCII 路径校验。

边界：`scripts/bundle-autojs.ps1` 只收集 `features/` 和 `domain/`，不包含 `app/account-warmup-command-bridge.js`。因此评论采集主体、人数/结束 OCR、前端页面可通过更新中心；手动 STOP 候选保留逻辑必须随 APK 基座覆盖安装后才会在设备生效。本轮只完成本地构建，禁止自动发布、下发或真机业务测试。

交付：已构建 `dist/apk/燎原星火-1.0.92-inrt.apk`，SHA-256 `3aeff1dfb2f6147c8e1c87c2c7e95994238b01e491310035bcb450a1bef6d557`。APK 签名、对齐、包名/版本、与旧版证书一致性、关键脚本逐文件哈希和 OCR native library 均校验通过；已保留数据覆盖安装到 `ce3eb0bf / MI 8`，安装后确认版本 `1.0.92/128`、首次安装时间未变化且 App 保持停止，尚未进行真机业务验收。

残留：API 全量 typecheck 仍有与本模块无关的 `publish-interface` 测试夹具 `externalAccountKey` 类型错误；真机需用户确认实际脚本版本、右上角人数 OCR、结束页跳过、评论 OCR/上滑、平台验证收尾及停止时部分候选保留。

## 78. 2026-08-20 抓取评论词停止本批任务后未退出抖音

现象：同批一台设备因平台验证正常退出；另一台设备在前端点击“停止本批任务”后，仍停留在抖音，未返回燎原星火。

证据：故障设备 `device_9c1f0572419de612` 的 `ACCOUNT_WARMUP_STOP` 已由后台创建，随后被手机领取并回执 `DONE`；回执清理结果明确为 `LIVE_COMMENT_ENTRY_CLEANUP_SKIPPED`。因此故障不在前端按钮、命令创建或设备领取，而在手机停止桥接器。

根因：`account-warmup-command-bridge.js` 对 `live_comment_entry` 手动 STOP 使用伪完成结果，明确跳过现有最近任务清理器，却仍将 STOP 和目标 RUN 标记为完成。

处理：手动 STOP 统一执行“设置停止标记、打断业务线程、运行一次真实清理、回执 STOP、收口目标 RUN”；清理结果按任务缓存，防止中断线程回卷时重复清理，并保留停止前已抓候选。评论翻页同时收敛为最多 5 次、连续两页无新增提前结束。

交付边界：故障设备仍为 APK `1.0.91`，本次桥接器修复属于 APK 基础层，更新中心无法覆盖。已构建并校验 `1.0.93/129`，但故障设备当前未通过 ADB 连接，必须保留数据覆盖安装后再由用户手动验收 STOP 退出动作。

## 79. 2026-08-27 抓取评论词隔离重构 Task 6 全量验证遗留门禁

现象：

- 新评论隔离链路与旧链路回归均通过，但工作区全量静态/测试门禁仍有既有失败。
- `bun run lint` 失败 `695 errors`；报错集中在未参与本轮的存量测试和被 Git 忽略的 `account-data-platform/tmp/admin-service-check.js`（该文件最后修改时间为 2026-08-12）。本轮 8 个 Web/共享契约变更文件定向 ESLint 通过。
- `bun run typecheck` 失败 `23` 条诊断，均为接口发布相关旧测试夹具缺少必填 `externalAccountKey` 或可选性不匹配。
- `bun test` 共 `595` 项，`588` 通过、`7` 失败；定向复跑稳定复现 2 个接口发布数据库夹具的 `external_account_key` 非空约束错误、3 个绑定服务旧夹具缺字段、1 个领取服务旧账号字段断言和 1 个远程脚本 revision 2 命令断言。

根因判断：上述失败发生在 `f384680..HEAD` 之外的接口发布/远程脚本既有数据模型与测试夹具，不依赖 `isolated_live_comment_entry` 实现；新评论聚焦测试 `126/126` 和手机端旧链路回归 `19/19` 均通过。

处理与边界：

- 已完成 29 个变更源码文件的 400 行门禁、隔离目录禁引检查和旧桥/uploader blob 一致性核验；新生产目录 12 个文件无养号/发布视频导入。
- Task 5 的精确 STOP、互斥、部分结果保留、清理幂等与 ACK outbox 关键回执保留/重试已由新评论命令桥及回归测试覆盖；未改动旧测试或旧业务代码。
- 本节点不处理上述存量失败，不将 lint/typecheck/全量测试标记为通过；APK 构建、安装、ADB、真机 UI/OCR 与平台验证场景仍未执行。

### 2026-08-28 抓取评论词结束后按钮灰色

现象：评论抓取任务已结束并退出，但开始和停止按钮均不可用。

根因：前端页面将历史批次的恢复状态和活动行直接用于整体锁定；设备查询异常也会持续触发恢复锁。终态任务未优先释放页面锁定。

处理：终态批次优先释放锁定，查询错误不再单独造成永久锁定；保留终态结果和告警展示。

### 2026-08-28 手机 Agent 停止后前端仍可操作业务

风险：手机端停止 Agent 后不再轮询后台，前端若只依据数据库业务命令状态，可能继续显示可启动或可停止状态。

处理：前端以设备 `agentReachable`、`agentStatus`、`desiredAgentState` 统一判断命令通道；通道关闭时不再选择该设备下发任务，也不再发送无效停止命令。

### 2026-08-28 内部 Agent 断联后的安全收口

根因：手机停止按钮会直接结束内部脚本，原有业务命令和前端状态没有一个可靠的实际生命周期信号；同时底座虽然在线，但没有自动触发最后的退出动作。

处理：新增实际生命周期心跳和会话 ID；停止心跳事务化终结活动命令并创建 BASE 收口命令；外部 Agent 复用最近任务清理逻辑退出抖音、返回燎原星火并回传清理阶段。任务历史保留，仅清理运行态和旧命令领取资格。

验证限制：本轮未连接 Android 设备，无法完成 APK 安装、无障碍权限和真机三次以上停止/重启验收。
